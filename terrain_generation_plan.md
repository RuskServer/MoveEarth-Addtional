# MoveEarth 自作地形生成実装計画

更新日: 2026-09-18
対象: Minecraft 1.21.1 / NeoForge 21.1.238 / MoveEarth-Addtional
関連: [resource_region_plan.md](resource_region_plan.md)（地方別資源システム。本計画のL1を置き換える）

## 1. 目的と要件

MoveEarthサーバーの地形を自前で生成する。既存のworldgen MODで要件を満たせないため自作する。

**必須要件**

| 要件 | 内容 |
|---|---|
| アースマップ的生成 | まとまった大陸と本物の海がある。陸:海はおおむね3:7 |
| ハイキングシミュレーターにならない | 大陸スケールを上げても局所地形が間延びしない |
| 幻想的な景色 | 意図して配置できる。乱数任せにしない |
| 拡張可能 | 将来ボーダーを広げられる |
| 他MODと共存 | Create、TaCZ、S2システムを壊さない |

**境界**

- 構造物の生成は本計画の範囲外とする。バニラおよび既存MODのものを使う。
- 無限ワールドは目指さない。本サーバーはワールドボーダーを持つ。
- ネザーとエンドは変更しない。
- 本計画は資源の配置を扱わない。それは`resource_region_plan.md`の担当である。

## 2. なぜ既存MODで満たせないか

既存のworldgen MODは無限procedural生成を解いている。無限である以上、大陸はノイズの塊にしかならない。
加えてバニラのdensity functionは、**大陸スケールと局所起伏スケールが同じ座標系に乗っている**。
大陸と海を出すために`continents`のスケールを上げると、山や谷も一緒に引き伸びる。
「広大にすると全部が間延びする」のはチューニングの失敗ではなく構造的な帰結である。

さらに、地図が地球に見える最大の要因は**山脈が線状であること**と**大陸棚があること**だが、
これは侵食と造山運動の結果であり、等方的なノイズからは原理的に出ない。

要件は地形生成の問題ではなく**地図製作の問題**である。本サーバーはボーダーを持つため、地図製作が成立する。

## 3. 方式

地形をオフラインで事前計算し、その結果をバニラの生成器へ差し込む。カスタム`ChunkGenerator`は書かない。

```
[オフライン]  地質シミュレーション  →  タイル資産（高度・気候・河川・地方）
[ゲーム内]    DensityFunction 1種   →  バニラ noise_settings の入力として使う
```

バニラの`minecraft:noise`生成器をそのまま使うため、サーフェスルール・帯水層・洞窟・カーバー・
構造物配置・バイオームシステムが全て従来どおり機能する。書くJavaは`DensityFunction`実装1個だけである。

### 3.1 スケールの分離

要件「間延びさせない」はこの方式で自動的に満たされる。

- **大スケール**（大陸形状・山脈帯・海盆）はタイル資産が供給する。解像度4ブロック/pxで、周波数はシミュレーションが決める。
- **小スケール**（岩肌の凹凸、崖、局所的な起伏）はバニラのノイズをそのまま加算する。周波数はバニラのままでよい。

両者が独立した供給源から来るため、大陸を広げても局所地形の手触りは変わらない。

## 4. タイル方式

### 4.1 なぜタイルか

事前計算方式の弱点はボーダー拡張である。水理侵食は大域的かつカオス的なので、
領域を広げて再計算すると既存地形が変わる。既存領域を固定して外周だけ侵食させることは可能だが、
旧領域の縁は「そこが世界の端＝海面が侵食基準面」として侵食済みのため、
外側に陸を足すと**旧海岸線が内陸の崖線として残る**。

**新しい陸を地続きにせず、海で隔てる。** これで問題そのものが消える。
旧領域の縁は元から海岸線として侵食されているので、そこに水を置けば何も不自然でない。

結果として新大陸は旧大陸と物理的に無関係になり、**独立にシミュレーションして隣へ置くだけでよい**。
既存タイルはファイルが1バイトも変わらない。

### 4.2 タイル仕様

| 項目 | 値（初期案） |
|---|---|
| タイル1枚の範囲 | 8192×8192ブロック |
| 解像度 | 4ブロック/px（2048×2048px） |
| 外洋マージン | タイル内周512ブロックは必ず海 |
| 初期ワールド | タイル1枚、ボーダーは8000×8000 |

外洋マージンには2つの役割がある。双線形補間がタイル境界を越えないことの保証と、
陸塊がタイルをまたがないことの保証である。

### 4.3 レイヤー

| レイヤー | 型 | 用途 |
|---|---|---|
| `height` | int16 | 地形高度 |
| `temperature` | int8 | 気候（バイオーム選択） |
| `humidity` | int8 | 気候（バイオーム選択） |
| `river` | uint8 | 河川幅。0は非河川 |
| `region` | uint16 | 地方ID。資源システムが使う |
| `continent` | uint8 | 大陸ID |

1タイルあたり 2048² × 9バイト ≈ 38MB（非圧縮）。可逆圧縮で1/3〜1/4を見込む。
**このアセットはサーバー側にのみ必要である。** worldgenはサーバーで完結するため、
公開するクライアントjarへは1バイトも含めない（`resource_region_plan.md`のサーバー分離方針と同じ）。

### 4.4 拡張の運用

1. 新しいタイル領域を決める。既存タイルとの間に200〜2000ブロックの可変幅の海を空ける。
2. 新タイルを独立にシミュレーションする。既存タイルには触れない。
3. タイル索引へ追加し、ワールドボーダーを広げる。
4. 資源システムが新大陸を自動検出し、新しい地方へプロファイルを割り当てる。

水路が一定幅の長方形だと人工物に見えるが、旧タイルの外側海岸も新タイルの内側海岸も
シミュレーション由来で不規則なため、隙間は自動的に不規則になる。幅を一定にしない限り問題ない。

地続きにしたい箇所があれば、タイル境界のブレンド帯へ陸橋を手で描く。シミュレーションは関与しない。

## 5. オフラインシミュレーション

Python（numpy、必要ならcupy/torchでGPU）で実装する。ゲームとは完全に独立したツールとする。

### 5.1 段階

1. **プレートと隆起場**
   ランダム点をLloyd緩和したボロノイでプレートに分割し、プレートごとに速度ベクトルと
   「大陸性か海洋性か」のフラグを与える。境界での相対収束量から隆起場を作り、距離減衰で広げる。
   - 収束境界 → **線状の山脈帯**。地図が地球に見える最大の要因はこれである。
   - 発散境界 → 地溝と海盆。
   - 大陸性プレート → 基準高度を海面より上へ。海洋性 → 下へ。これで大陸と海盆、および**大陸棚**が出る。

2. **熱侵食**
   安息角を超えた斜面を緩和する。安価で、非現実的な尖りを除く。

3. **水理侵食**
   粒子法（水滴）ではなく**流量集積＋stream power law**を使う。
   - 窪地埋め（Priority-Flood）→ 流向（D8またはD∞）→ 流量集積 A を計算する。
   - 侵食量 E = K·A^m·S^n で高度を更新する。
   - 粒子法より収束が速く、結果が粒状にならない。そして**河川網が副産物として直接得られる**。

4. **河川刻線**
   流量集積が閾値を超えた経路を河川とする。川幅は A^0.5 に比例させる。
   下流方向の単調減少を強制し、`river`レイヤーへ幅を書く。

5. **高度分布の写像**
   隆起と侵食のパラメータを押し合って「低地・丘陵・山地」の配分を作るのは分が悪い。
   隆起を強めれば大陸が一様な台地になり、弱めれば一様な平原になる。配分は直接与えるほうが早い。
   陸と海のそれぞれについて、**地球の高度分布曲線へランク写像する**。
   - 陸: 面積の約半分が全高度域の下位12%に収まる強い偏りを与える。広い平野と鋭い山岳が同時に出る。
   - 海: 浅い側の7.5%を大陸棚、そこから急な大陸斜面、残りを深海平原とする。
     **大陸棚と大陸斜面はこの写像が直接作る**。地殻の段差をぼかす方法よりも確実である。
   写像は各領域内で狭義単調なので、**水系は一切変化しない**。全セルの流下先は同じまま、
   陸が海になることもなく、変わるのは鉛直方向の間隔だけである。

6. **気候場**
   - 気温: 赤道を中央に置いた南北勾配（地球らしさの主要因）＋ 高度による逓減。
   - 湿度: 卓越風で海から水分を移流させ、山越えで絞り出す。**雨陰ができる**。
     風向は緯度で変える（低緯度は貿易風で逆向き、中緯度は偏西風）。雨陰が地図全体で
     同じ向きを向くのを避けるため。
   - 湿度には**帯状の供給場**を掛ける。赤道は湿潤、亜熱帯は乾燥、中緯度は storm track で湿潤。
     砂漠・サバンナ・森林・タイガが帯として並ぶかどうかはこの項で決まる。
     内陸勾配だけでは「海岸が湿って内陸が乾く」以上のものにならない。
   - 湿度の正規化は**陸地基準**で行う。外洋では水分が飽和するため、地図全体の分位点で
     正規化すると陸のセルが全て下位数%に潰れ、場が二値になる。

7. **地方と大陸の確定**
   - 大陸: 陸セルの連結成分。
   - 地方: **流域（drainage basin）** を単位とする。目標面積に届くまで隣接流域を併合する。
     分水嶺と山脈が地方境界になる。人類史で実際に政治的境界だったものと同じ論理である。
   - 面積が下限未満の流域は隣接地方へ併合する。

8. **手による加筆**
   幻想的な景観を意図的に置く。巨大な孤峰、環礁、内海、断崖の海岸線など。
   高度レイヤーへのマスク加筆として行い、加筆後に熱侵食だけ軽くかけて馴染ませる。

### 5.2 出力物

- タイル資産（4.3のレイヤー群）
- 検証用の可視化PNG: 陰影起伏、高度カラーマップ、河川網、気候場、地方分割、大陸分割
- パラメータと乱数シードを記録したメタデータ

### 5.3 再現性

シミュレーションのパラメータと乱数シードを記録し、**同じ入力から同じタイルが出ることを保証する**。
ただし運用上はタイル資産そのものが正であり、再実行は行わない。アセットをバージョン管理下に置く。

## 6. ゲーム内実装

### 6.1 DensityFunction

カスタム`DensityFunction`型を1つ登録する。

```json
{ "type": "moveearth_addtional:map_field", "field": "height" }
```

- `field`でどのレイヤーを読むかを選ぶ。1クラスで全レイヤーを賄う。
- 実装する契約は `compute(FunctionContext)` / `fillArray(double[], ContextProvider)` / `mapAll(Visitor)` /
  `minValue()` / `maxValue()` / `codec()`。`codec()`の戻り値は`MapCodec`ではなく
  **`KeyDispatchDataCodec<? extends DensityFunction>`** である。
- `FunctionContext`は`blockX()` / `blockY()` / `blockZ()`をブロック座標で返す。タイル参照にそのまま使える。
- 登録先は`Registries.DENSITY_FUNCTION_TYPE`（`worldgen/density_function_type`、
  `MapCodec<? extends DensityFunction>`のレジストリ）。NeoForgeは`BuiltInRegistries`の全レジストリへ
  `RegisterEvent`を発火するため、`DeferredRegister`で登録できる。
- タイル索引から座標を含むタイルを引き、双線形補間で値を返す。
- タイル外は外洋とし、深度は手続き的なノイズで与える。境界数百ブロックでブレンドする。
- 戻り値はルーターが期待する範囲へ正規化する。

性能上の注意。`DensityFunction`は地形生成中に毎セル呼ばれる。双線形補間は多octaveのパーリンより安価なので、
バニラより速くなる見込みである。ただし実測する。タイルはmemmapで開き、ヒープを圧迫しない。
スレッド安全にする（読み取り専用なので、初期化を生成開始前に済ませれば足りる）。

### 6.2 noise_settingsへの差し込み

バニラの`noise_router`の構造を保ったまま、入力の一部を`map_field`に差し替える。

| ルーターの入力 | 差し替え内容 |
|---|---|
| `continents` | `height`から導出。海が負、陸が正になるよう正規化する。これで`multi_noise`のバイオーム選択が実際の海岸線に従う |
| `erosion` | 局所勾配または起伏量から導出する |
| `temperature` / `vegetation` | 気候レイヤーから直接与える。**これで雨陰や南北の気候帯がバイオーム分布へそのまま反映される** |
| `ridges` | `river`レイヤーから導出する。バニラの河川バイオーム選択が実際の河川へ乗る |
| `depth` / `final_density` | バニラの形を保ちつつ、基準高度を`height`から取り、**局所の細部ノイズはバニラ周波数のまま加算する**（3.1） |

`multi_noise`バイオームソースはルーターの気候値を読むため、バイオームソース側を自作する必要はない。
バイオーム一覧はバニラのものから始め、必要に応じて自前バイオームを足す。

**照合済み (Phase 0)。** `RandomState`は`Climate.Sampler`をルーターの各フィールドからそのまま組む。
名称が一致しない2つに注意する。

| ルーターのJSONフィールド | `Climate.Sampler`のパラメータ |
|---|---|
| `temperature` | temperature |
| `vegetation` | **humidity** |
| `continents` | continentalness |
| `erosion` | erosion |
| `depth` | depth |
| `ridges` | **weirdness** |

ルーター15フィールドのJSON名は `barrier` / `fluid_level_floodedness` / `fluid_level_spread` / `lava` /
`temperature` / `vegetation` / `continents` / `erosion` / `depth` / `ridges` /
`initial_density_without_jaggedness` / `final_density` / `vein_toggle` / `vein_ridged` / `vein_gap`。

### 6.3 初期化順序

タイル資産は最初のチャンクが生成されるより前に読み込み済みでなければならない。
`resource_region_plan.md` §8.3と同じ問題であり、**同じ初期化点を共有する**。
未読み込み時は例外を投げて停止する。中立値で進行させてはならない（地形が壊れたまま世界が確定する）。

**照合済み (Phase 0、2026-09-18に訂正)。** 構築点は`ServerAboutToStartEvent`である。
`initServer`が`loadLevel`を呼ぶ直前に発火するため、`ServerLevel`が1つも構築されていない時点で走る。

当初は`LevelEvent.Load`を想定していたが、**これは誤りだった**。`levels.put`の直後に発火するので
一見十分に早いが、`ServerLevel`のコンストラクタ自身が`ensureStructuresGenerated()`を呼び、
要塞リングの配置計算を共通プールへ投げる。そのタスクはコンストラクタの実行中に
バイオームソース経由でdensity functionを叩く。実機で
`ChunkGeneratorStructureState.generateRingPositions`から例外が上がって発覚した。

同期的な文の並びだけを追って、コンストラクタが非同期処理を開始する点を見落としたのが原因である。
**「イベントの発火位置」ではなく「最初に密度関数が評価される瞬間」を基準に選ぶこと。**

タイルの読み込みはレベルを必要としない。ディレクトリはサーバーのワールドパスから決まり、
それは`MinecraftServer`のコンストラクタで設定済みである。

## 7. 資源システムとの接続

`resource_region_plan.md`のL1地域マップは、本計画が成立する場合**丸ごと不要になる**。

| 項目 | 現行計画（L1） | 本計画採用後 |
|---|---|---|
| 陸海判定 | `getBaseHeight`で全域走査 | 不要。シミュレーションが知っている |
| 大陸検出 | flood fill | 不要。`continent`レイヤー |
| 地方分割 | k-means＋セントロイド | 不要。`region`レイヤー（流域由来） |
| 実行時判定 | 最近傍セントロイド | 配列の直接参照 |
| 地方境界の性質 | Voronoiの直線 | **分水嶺と山脈** |

走査もflood fillもk-meansも消え、地方境界は地形に沿った自然なものになる。
`RegionResolver`はタイルの`region`レイヤーを引くだけになる。

新大陸タイルを追加した場合も、`continent`と`region`レイヤーが付いてくるため、資源システム側の変更は不要である。

## 8. フェーズと完了条件

各フェーズは前フェーズの成果だけに依存し、**途中で中止しても損失が限定される**よう順序を組む。

### Phase 0 API照合

- `DensityFunction`のカスタム型登録方法、`NoiseRouter`の各入力の正確な名称と意味、`multi_noise`が読む気候値の経路を実物のAPIで確認する。
- `getBaseHeight`が本方式でどう振る舞うか、コストがどうなるかを確認する。
- 完了条件: §6.2の対応表が実APIに基づいて確定している。

**実施済み (2026-09-18)**。照合には`build/moddev/artifacts/neoforge-21.1.238-sources.jar`
（Parchmentマップ済み）を使用した。

| 確認項目 | 結果 |
|---|---|
| `NoiseRouter`のJSONフィールド名 | 計画どおり。15フィールドを§6.2に列挙 |
| `Climate.Sampler`への経路 | `RandomState`がルーターから直接組む。**`vegetation`→humidity、`ridges`→weirdness**の名称差あり |
| `DensityFunction`の契約 | §6.1に記載。`codec()`は`KeyDispatchDataCodec`を返す |
| カスタム型の登録可否 | 可。`RegisterEvent`は`BuiltInRegistries`の全レジストリへ発火する |
| `ChunkGenerator#getSeaLevel` / `getBaseHeight` | どちらも`public abstract`。生成器非依存に使える |
| `getBaseHeight`のコスト | **`iterateNoiseColumn`で列を走査する。配列参照ではない**。§7のとおりL1はタイルを直接読むべきで、この方式でも`getBaseHeight`経由の全域走査は避ける |
| 初期化点 | §6.3のとおり`ServerAboutToStartEvent`。当初`LevelEvent.Load`と結論したが実機で誤りと判明し訂正した |
| MixinExtras | NeoForgeに`mixinextras-neoforge-0.5.3`がjarjarで同梱済み。`@WrapOperation`・`@Local`が使える |

### Phase 1 オフラインシミュレーション

**ゲーム側には何も作らない。** ここで品質が出なければ本計画を中止でき、損失はこのフェーズだけに留まる。

- §5.1の1〜6を実装し、タイル1枚を生成する。
- 完了条件: 陰影起伏PNGを見て、(a) 線状の山脈帯がある、(b) 大陸棚から深海への遷移がある、(c) 河川が上流から下流へ合流しながら海へ到達している、(d) 雨陰がある、(e) 陸:海がおおむね3:7、(f) 大陸内を歩いて横断する距離が過大でない。同じシードとパラメータから同じタイルが出る。

**実施結果 (2026-09-17、`--size 512 --blocks-per-cell 16`、8192ブロック四方、所要約3.5分)**

| 指標 | 目安 | 実測 | |
|---|---|---|---|
| `land_fraction` | 0.30 | 0.2995 | 海面を分位点で決めるため構成的に満たす |
| `ridge_elongation` | 2.0以上 | 3.158 | 線状の山脈帯 |
| `shelf_share_of_ocean` | 0.07〜0.15 | 0.1332 | 地球の大陸棚は海面積の約7.5%。当初の「0.15以上」は過大だったので目安を修正した |
| `river_reaches_sea` | 1.0 | 1.0 | |
| `rain_shadow_ratio` | 0.2〜0.5 | 0.25 | |
| `largest_continent_span_blocks` | — | 5536 | 徒歩横断可能 |
| `continents` / `regions` | — | 3 / 6 | |

同一シードでの2回実行で全6レイヤーのハッシュが一致することを確認済み。

**この段階で見つかった実装上の落とし穴**（再発防止のため記録する）

1. **D8の同順位解決による平行直線の河川**。平坦な海岸平野ではD8が常に同じ向きを選び、河川が格子軸に沿う。微小ジッタで解決するが、**ジッタは窪地埋めの前に入れなければならない**。埋め立て後に足すと、ジッタが埋め立てのepsilon勾配より桁違いに大きいため、埋めたはずの窪地を掘り直して河川が内陸に取り残される。`river_reaches_sea` がこれを検出した（0.54まで低下）。
2. **陰影起伏の鉛直誇張**。標高はシミュレーション単位なので、そのまま勾配を取ると傾斜がほぼ0になり、起伏があるのに「真っ平ら」に見える。正規化して明示的に誇張する。
3. **ボロノイ多角形が海岸線に透ける**。プレート境界の直線がそのまま海岸になり、最も人工的に見える。プレート割り当ての座標にドメインワープをかけて解決した。

### Phase 2 地形の接続

- `map_field` `DensityFunction`、タイルローダー、`noise_settings`を実装する。
- 完了条件: ゲーム内でPhase 1の地形が再現される。生成速度がバニラ比で許容範囲。タイル外で外洋へブレンドされる。サーフェスルール・洞窟・帯水層が破綻しない。

**実施済み (2026-09-18)**

照合の結果、当初の想定より介入が小さくて済むことが分かった。**独自の`noise_settings`は地形のためには不要である。**

バニラの`overworld/depth`は`y_clamped_gradient(1.5 @ -64 .. -1.5 @ 320) + overworld/offset`であり、
地表は`y = 128 + 128 * offset`付近に来る。つまり**`offset`がそのまま基準高度である**。
そして`initial_density_without_jaggedness`と`final_density`はどちらも`overworld/depth`・`overworld/factor`・
`overworld/sloped_cheese`を**参照**しており、スプラインをインライン展開していない。
したがって`offset`を差し替えるだけで、洞窟・帯水層・サーフェスルール・構造物配置に一切触れずに
整形パイプライン全体が追従する。

差し替えたデータパックエントリ（`data/minecraft/worldgen/density_function/overworld/`）:

| ファイル | 内容 |
|---|---|
| `offset.json` | `map_field(offset)`。高度レイヤーを`(y - 128) / 128`で写す |
| `continents.json` | `map_field(continents)`。海で負、陸で正 |
| `erosion.json` | `map_field(erosion)`。局所起伏から導出したレイヤー |
| `ridges.json` | `map_field(river_gate)` × 稜線ノイズ（Phase 4を参照） |
| `factor.json` | 定数 3.5 |
| `jaggedness.json` | 定数 0.0 |

`factor`は**バニラ細部ノイズの振幅を決める係数**である。地表付近では
`sloped_cheese ≈ 4·(depth·factor) + base_3d_noise`なので、ノイズによる高さのずれは
`Δy ≈ 32 · noise / factor`。3.5なら±3〜4ブロック程度で、これが計画§3.1の言う
「大スケールはタイル、小スケールはバニラノイズ」を実現している。

XZのみに依存する関数はバニラに倣って全て`minecraft:flat_cache`で包んだ。列ごとに1回の評価で済む。

| 検証 | 結果 |
|---|---|
| `compileJava --offline` | 成功 |
| `assemble --offline` | 成功 |
| `terrain`パッケージの単体テスト | 12件成功（`TerrainFieldTest` 7、`TerrainLayerTest` 5） |
| 実機でのチャンク生成 | **未実施**。dev serverの起動にEULA同意が必要なため保留 |

### Phase 3 気候とバイオーム

- 気候レイヤーをルーターへ接続する。
- 完了条件: 南北の気候帯と雨陰がバイオーム分布に見える。海岸・山地・内陸のバイオームが破綻しない。

**実施済み (2026-09-18)**

`RandomState`が`Climate.Sampler`をルーターから直接組むため、ルーターの`temperature`と`vegetation`を
差し替えれば気候レイヤーがそのままバイオーム選択へ流れる。ただしこの2つだけはルーターに
**インラインで書かれている**ため、ここで初めて独自の`noise_settings`が必要になる。

手書きはしない。[tools/terrain/make_noise_settings.py](tools/terrain/make_noise_settings.py) が
開発環境のバニラ資産から`minecraft:overworld`のnoise settingsを読み、**当該2エントリだけを差し替えて**
`data/minecraft/worldgen/noise_settings/overworld.json`へ書き出す。
サーフェスルール・洞窟・帯水層・spawn targetはバニラのまま残る。
生成物はバニラのサーフェスルールを含むため`.gitignore`対象とし、コミットせず再生成する。

`erosion`レイヤーをオフラインツールへ追加した。局所起伏からの導出で、
バニラの慣例（侵食が高い＝平坦、低い＝山岳）に合わせて符号を反転してある。

### Phase 4 河川

- `river`レイヤーによる刻線と、河川バイオームの接続を実装する。
- 完了条件: 川が上流から海まで連続し、途中で途切れない。洞窟やカーバーが河床を貫通して水を抜かない。

### Phase 5 資源システムL1の置き換え

- `resource_region_plan.md`のL1を`region`/`continent`レイヤー参照へ置き換える。
- 完了条件: 地方境界が分水嶺と山脈に沿っている。資源システムの上位層（L2以降）に変更が不要である。

### Phase 6 拡張運用

- タイル追加手順を確立し、2枚目のタイルで実際に試す。
- 完了条件: 既存タイルが不変のまま新大陸が追加され、海峡が人工物に見えず、資源システムが新地方を自動で認識する。

**実施済み (2026-09-18)**

河川は2つの経路で効く。**地形としての河谷**はハイトフィールドに刻線済みなので`offset`が運ぶ。
**河川バイオームの配置**はweirdnessチャンネルで行う。

ここで素朴な実装は失敗する。バニラはweirdnessが0付近の場所に河川バイオームを置くが、
weirdnessは**バイオームの変種選択にも使われている**。河川以外を定数にすると、
世界中のバイオームが1つの変種クラスへ潰れる。

そこでタイルの`river`レイヤーは**0〜1のゲート**として公開し（`river_gate`、河道で0、離れると1）、
データパック側で稜線ノイズに掛ける。

```
ridges = flat_cache( map_field(river_gate) × shifted_noise(minecraft:ridge) )
```

河道ではweirdnessが0になって河川バイオームが乗り、それ以外ではノイズがそのまま残って変種が保たれる。

既知の副作用として、ノイズが偶然0付近になる地点にも河川バイオームが出る。
その場所に河道は刻まれていないため「乾いた河川バイオームの帯」になる。実害は小さいので当面許容し、
実機確認後に必要なら詰める。

### 2026-09-18 崖と斜面の作り分け（岩相）

**要求**: 「石である必要がある部分と土である部分がかみ合ってない」。原因は
バニラの`steep`サーフェスルールが傾斜2.0ブロック/ブロック以上を要求するのに対し、
陸地の0.7%しか到達していないこと。

**測定で判明した本質**（推測ではなく実測）:

| 条件 | p50 | p90 | p99 | max | >=2.0 |
|---|---|---|---|---|---|
| 現行 (16ブロック/セル) | 0.09 | 0.47 | 1.68 | 4.50 | 0.72% |
| 8ブロック/セル | 0.09 | 0.46 | 1.55 | 5.99 | 0.65% |
| 安息角を岩相連動 (30〜78°) | 0.09 | 0.46 | 1.70 | 5.36 | 0.73% |
| **ハイプソメトリー再マップなし** | 0.13 | 0.56 | 0.94 | **1.86** | **0.00%** |

最下段が決定的。**現在の急斜面はすべてハイプソメトリー再マップが作っている**。
この再マップは順位ベースで、「何セルが下にあるか」しか見ず「どれだけ下か」を捨てる。
つまり侵食シミュレーションが刻んだ落差は出力に一切届かない。安息角を上げても
解像度を上げても`steep`が改善しなかったのはこのため。

さらに根本的な制約として、**縦の予算**がある。世界高は328ブロック、タイルは
8192ブロック四方。セル解像度の高さマップで2:1傾斜を広く作ることは原理的にできず、
再マップは低地の高低差を山に付け替えることでそれを捻出している。

**試して捨てた案**: 場を大スケールと局所に分解し、曲線は大スケールにだけ適用して
侵食の細部を足し戻す。ぼかしが峰の高さを先に落とすため順位が下がり、山が丘に
なった（残差を曲線の局所伸長率でスケールしても復元しない）。実装は破棄し、
所見を`hypsometry.remap`のdocstringに残した。

**採用した方針**: 崖はブロックスケールでしか作れないので、唯一ブロックスケールで
効く`jaggedness`を岩相でゲートする。

1. `tectonics`が既に持っていた隆起ノイズを**岩盤硬度**として明示的に返す。
   fbmは平均付近に偏るので順位正規化し、硬い岩の面積割合を固定する。
2. 硬度が安息角（30°〜78°）と侵食係数（硬岩は0.30倍）を駆動する。
   傾斜統計はほぼ変わらないが、リッジの筋の出方は明確に改善した。
3. 硬度を`hardness`レイヤーとしてタイルに出力し、ゲーム内の`jaggedness`を
   `jagged_soft + (1 - jagged_soft) * hardness^2`で倍率調整する。
   `jagged_max`は0.18→0.45。軟岩は実効0.09で**以前より穏やか**、硬岩は0.45。
   二乗は最も鋭い設定を少数派に限定するため。

**要実機確認**: `jagged_max`を上げているため、「尖らせすぎ」の再発がないか。
再発した場合の調整順は`jagged_max`を下げる → `jagged_soft`を下げる
（軟岩をさらに穏やかにして差を保つ）。

### 2026-09-18 サーフェスルールによるブロック差し替えを撤去

`make_noise_settings.py`が入れていた山岳の層状岩（標高帯ごとに安山岩・花崗岩・
凝灰岩などをノイズで斑にする規則）と`steep`面の岩規則を**削除**した。

理由: ブロックをルールで選ぶのは地形の材質感の作り方ではない。Tectonicの
サーフェスルールを実測したところ31,978バイト・24種で、バニラの32,237バイト・
24種と同等であり、あちらの見栄えはルールではなく**地形の形**から来ている。

結果、`surface_rule`はバニラとの差分が粉雪のY>=150ゲート4箇所のみになった
（構造比較で確認済、ブロック出現数は全てバニラと一致）。
`overworld.json`は125,177→111,610バイト。

**再導入しないこと。** 「高度や傾斜でブロックを選ぶサーフェスルール」は
棄却済であり、必要なら地形の形の側で解決する。

### 2026-09-18 水位が変わる位置の草ブロック・石ブロック

**症状**: 川の水位が段になるところで草ブロックや石ブロックが置かれる。

**原因**（バニラ`Aquifer`のソースで確認）: 帯水層の水位を持ち上げて水を置かせる
という従来方式が、帯水層の二つの仕組みと原理的に噛み合っていなかった。

1. **1セル1サンプル**: `NoiseBasedAquifer`は16x12x16セルごとにジッタ付きの1点を
   選び、その点の水位をセル全体に適用する。川はセルより細いのでサンプルは大抵
   川の脇に落ち、セルは海面高を答えて水が置かれない。`SurfaceSystem`は生成済み
   カラムの水を見てルールを適用するので、水がなければ**草が生える**。
2. **`calculatePressure`の隔壁**: 隣接セルが水位で食い違うと、バニラは意図的に
   その間を石で塞ぐ。下る川は定義上どこでも食い違うので、これは調整で消える
   類のものではない。y=120の川とy=63の海を持ち上げで繋ぐと、中点の
   **y=91付近一帯に石**が入る。

**対処**: 川の水位を持ち上げるのをやめ、水路の中身を帯水層に渡さず直接決める。

- 削除: `RiverFluidPicker`、`NoiseChunkGeneratorFluidMixin`（水位持ち上げ方式）
- 追加: `RiverChannelWater`（純粋な判定）+ `AquiferRiverChannelMixin`
  （`Aquifer$NoiseBasedAquifer.computeSubstance`のHEADに注入）
- 判定は`channelInfluence > 0`（掘った器の内側、堤も含む）かつ
  `waterLevel - 32 <= y < waterLevel`なら水、`y >= waterLevel`なら空気、
  それ以外は帯水層に委ねる。サンプリングも圧力計算も通らない。
- 水面より上を明示的に空気にするのは、帯水層が川の上に水溜まりや石の栓を
  作るのを止めるため。
- `shouldScheduleFluidUpdate = false`。水は既に川の高さにあるので落ち着き先がなく、
  流体tickも要らない。段差は静的な1ブロックの落差として残る。

**次元ガード**: タイルのx/z範囲はどの次元にも存在し、ネザーとエンドも同じ
ジェネレータクラスを通るので、タイル内にいることは何の証明にもならない。
`globalFluidPicker`に**海面高での**答えを聞き、そこが水である次元だけを対象とする
（ネザーは y<32 が溶岩で海面高は空気、エンドは空気）。位置のyで聞くと
バニラは深部で溶岩を答えるが、それは深さであって次元ではない。

### 2026-09-18 バイオーム構成: 陸の半分が湿地だった

`predict_biomes.py`を実用速度にして本番タイルを測ったところ、陸の**48%**が
`swamp` + `mangrove_swamp`だった。

**原因**（ダンプしたバイオームテーブルで確定）: バニラの`swamp` /
`mangrove_swamp`は**湿度を一切見ない**。条件は内陸
(continentalness >= -0.11) かつ **erosion が最上位帯 (+0.55〜+1.0)** だけ。
うちのerosionは局所粗さ由来で、タイルは大半が低地なので、**陸の70%が
その1帯に固まっていた**。

**対処**: 気温・植生と同じ順位再マップをerosionにも適用
(`climate_bands.EROSION_CURVE`)。最上位帯の占有率を70%→8%に設計し、
残りをバニラの7帯へ配分した。順位なので地形との対応関係は不変。

| 帯 | 意味 | 目標占有率 |
|---|---|---|
| 0 (-1.00〜-0.78) | 尖峰 | 5% |
| 1 (-0.78〜-0.375) | 山地 | 14% |
| 2 (-0.375〜-0.2225) | 丘陵 | 10% |
| 3 (-0.2225〜+0.05) | 起伏 | 22% |
| 4 (+0.05〜+0.45) | 平野 | 36% |
| 5 (+0.45〜+0.55) | 遷移帯 | 5% |
| 6 (+0.55〜+1.00) | 平坦低地 (= 湿地帯) | 8% |

**副作用への対処**: `jaggedness`はerosionレイヤーから粗さを逆算していたが、
再配分後のerosionは粗さを測っていないので、生の粗さを`roughness`レイヤーとして
別に出力し、Java側はそちらを見る（旧タイルはerosionへフォールバック）。

**結果** (本番タイル1024セル、陸306021セル、39バイオーム):

```
14.25% river      13.96% plains     11.44% forest     10.12% desert
 5.35% swamp       4.27% jungle      3.98% savanna     3.02% snowy_plains
 1.51% mangrove_swamp  1.28% frozen_river
```

湿地48%→6.9%、雪原25.7%→3.0%、砂漠・サバンナ・ジャングルが常識的な比率で出現。

**未解決**: `river`が陸の14%とやや広い。河道自体は陸セルの1.8%しかなく、
差は`TerrainField.RIVER_BIOME_RADIUS`(14ブロック)の回廊幅による。
不自然と感じたらこの値を下げる。

**予測器の高速化**: 7593個の境界ボックス×100万セルで(n, boxes, 6)配列を作ると
1チャンクあたり数GBになり実質完走しなかった。パラメータごとに逐次加算する形へ
変更して33秒（stride 2）。これで気候調整をゲーム起動なしで回せる。

### 2026-09-18 河川の本数を半減

「川自体はこのくらい大きいほうが存在感があってよいが、本数が多すぎる」との評価。

`river_threshold`を上げれば本数は減るが、`channel_width`が幅の基準に同じ値を
使っていたため、**残った川も一緒に細くなる**作りだった。要求はその逆なので、
幅の基準を`river_width_reference`(0.0004固定)として切り離し、`river_threshold`
だけを0.0004→0.0009へ上げた。

本番タイル: 河道セル 5415 → 2868。幅の最大値は72ブロックのまま不変
（512での測定でも閾値0.0004〜0.0030の全てでmax 72）。消えるのは源流の細流だけ。

さらに減らすなら0.0016(陸の1.36%)、0.0030(0.91%)。増やすなら0.0004に戻す。

### 2026-09-18 事前生成が遅い (解決)

Chunky の事前生成が 4.5〜13 cps。通常のバニラは数十 cps 出るので明らかに遅い。

**確認したこと**: 密度関数は全て `flat_cache` に包まれているのでノイズセル単位
(4x4 ブロック) でしか評価されない。ここは問題ない。

**見つけて直した経路**: `Aquifer.computeSubstance` はチャンクの**全ブロック**
(y = -64〜320 の 384 × 256 = 約98000回) 呼ばれる。ここから
`channelInfluence` と `riverWaterLevel` を毎回呼んでおり、1回ごとに
`RiverNetwork.sample` の `Map<Long,...>` 参照 (long のボクシング) と
`Sample` レコード確保が2組発生していた。チャンクあたり約40万アロケーション。

x/z のみの関数なのでカラム単位にキャッシュした (aquifer インスタンスは
チャンク単位なので 256 エントリの固定表で足りる)。この経路の参照回数は
約1/380になる。**出力は変わらない**ので、生成済みチャンクを捨てる必要はない。

**結果**: 4.5〜12.7 cps → **26 cps で安定** (実機、Chunky)。これが主因だった。

この毎ブロック経路は今回の mixin 以前から存在していた (旧 `RiverFluidPicker` も
`computeFluid` 経由で毎ブロック `riverWaterLevel` を呼んでいた) ので、
今日の変更による退行ではなく、元からあったコストが顕在化したもの。
事前生成を回すまで気づく機会がなかった。

同時に疑った広葉樹 (樹冠が大きく `TreeFeature` の葉の距離更新が重いはず) は、
26 cps 出ている以上、問題になる水準ではなかった。

**教訓**: `Aquifer.computeSubstance` はチャンクの全ブロックで呼ばれる。
ここに x/z のみの関数を置くなら、必ずカラム単位にキャッシュする。
密度関数側は `flat_cache` が同じ役割を果たしているが、aquifer には無い。

## 8b. 連結成分ラベリングの不具合と差し替え (2026-09-20)

**症状**: `/moveearth region export`のPNGで、地方境界のうち数本が地形と無関係な**直線**になっていた。
`regions.py`には直線を引く処理が存在しないため、上流の`grid.connected_components`を調べた。

**原因**: 旧実装は「見える中で最小のラベル」を伝播させる方式で、64ラウンドで打ち切っていた。
**収束しなくても例外を出さずそのまま返す**作りだったため、1つの陸塊が複数の「大陸」に分割され、
伝播フロントが停止した位置に直線の継ぎ目ができていた。

| 実装 | dev タイル(512²)の結果 |
|---|---|
| 旧（64ラウンド上限） | **3大陸** {39505, 20855, 18282} ← 未収束のまま返却 |
| 同じ方式・91ラウンド | 2大陸 {60360, 18282}（収束） |
| 塗りつぶし参照実装 | 2大陸 {60360, 18282} |

39505 + 20855 = 60360。地続きの1陸塊が2つに割られていた。
docstringは「O(log n)で収束」と書いていたが、ノードを最小ラベルへ向けるだけで
**根同士をフックしていない**ため、実際には伝播距離に比例したラウンド数を要していた。

**影響**: 大陸が偽物なら、大陸ごとに決まる地方数（`target = 面積 / region_unit_area_blocks`、上限6）も
地方境界も偽物になる。`spawn_anchors`は大陸ごとに1点なのでスポーン地点も影響を受ける。

**差し替え**: エッジリスト上のunion-findへ変更し、**根を根へフック**して不動点まで回す。
収束しなかった場合は例外を投げる（黙って誤った結果を返さない）。numpyのみでscipyは使わない。
512²で0.04秒、1024²で0.11秒。`tools/terrain/test_grid_components.py`で塗りつぶし参照と照合する。
テストには長い渦巻き（伝播方式が必ず失敗する形）を含めた。

**地面と川は動かないが、気候は動く（本番タイルでの実測、上の見込みを訂正）**:
再生成したタイルを旧タイルとバイト比較した結果は次のとおり。

| 一致 | 変化 |
|---|---|
| height / hardness / roughness | continent / region |
| river_dist / river_width / river_water_y / river_segments.json | temperature 14.05% |
| | humidity 7.90%、erosion 5.86%、continentalness 3.45% |

devタイルでは`land = cont > 0`が集合として不変だったため「気候も不変」と見込んだが、
**本番タイルでは2888セル増えた**（減少はゼロ）。割られた断片が島の足切り（3906セル）を
下回って海扱いされており、1つの陸塊に戻ると足切りを免れるためである。
増えた2888セルのうち2744セルはもともと海抜以上の地面で、地面ではなく気候の分類が誤っていた。

**本番タイルの差し替え実施 (2026-09-20)**: seed 20260917 で再生成し
`build/terrain/moveearth_terrain.tar.gz` を更新した（旧版は同ディレクトリに`.bak-20260920`で保存）。
大陸4→2、地方7→5、異大陸が隣接するセル対426→0。既存チャンクは変わらないため、
探索済み領域の境界にバイオームの継ぎ目が出ることを承知の上での判断である。

## 9. リスク

### 2026-09-18 河川品質の再評価と修正

Phase 4は未完了とする。上記の実施済み記録はバイオーム経路の接続を指し、
実機で連続した水流・河床を確認したことを意味しない。

判明した問題と今回の修正:

- 凹地・流量集中のフィルターを全セルへ適用すると河道の途中も消える。
  フィルターを河川候補の選択に使い、採用した流路はreceiverを辿って海岸まで接続する。
- 旧 `river_reaches_sea` は非河川セルも辿るため、川が切れていても1.0になる。
  実際に出力する河道の途中が乾いたセルなら到達失敗とする検証へ変更した。
- `river_dist` は符号なし距離である。例えば16ブロック格子の対角に河道がある場合、
  中間点の双線形補間値は0にならず、細い川が切れる。
  新タイルは `river_segments.json` に接続線分・端点幅・端点水位を保存する。
  Javaは空間インデックスから線分距離を評価し、河道・水面・バイオームの参照を揃える。
  検索半径は実線分の最大幅/2＋岸幅、水位提供半径、バイオーム半径40の最大値とする。
  旧ラスタのクランプ値200は検索半径へ流用しない。河床・水位の各計算は
  距離・幅・水位を一回の検索で取得する。狭い川と広い川の最近傍選択を保つため
  半径はネットワーク内で共通とする。
- 流向は窪地埋め後の高度から作るので、元の地形高度からfreeboardを引くだけでは
  下流への単調性は保証されない。合流を含む河道グラフ上で下流水位を制約する。
- 最小河川は深さ2とfreeboard2が相殺され、水深がなくなる。
  河床を `河川水位 - 河川深さ` から計算する。幅広の川の水位提供範囲も半幅以上にする。

今回の検証範囲はPythonの流路接続・合流水位・循環拒否、Javaの斜め線分・
インデックス境界・高地河床、少量反復のタイル生成、およびビルドである。
低解像度のスモーク生成を景観品質の合格判定に使わない。

残るPhase 4の合格条件:

1. 新規ワールドで上流・合流・河口を確認する。バニラ密度補間と細部ノイズによる
   3ブロック級水路の閉塞、水面の段差、帯水層の空洞・堰を確認する。
2. 必要に応じ河道近傍の細部ノイズ抑制と河床の厚み保証、洞窟・カーバー貫通対策を加える。
3. D8由来の45度折れ線を滑らかにする際は、合流点を共有し、山越えや流路切断を防ぐ。
4. 地図PNGだけでなく、ブロック解像度の河道連続率と実生成後の水深を測る。

新形式はタイルの再生成が必要。既存形式は読み込めるが、旧距離場の斜め河川欠損は残る。
新しい水位・河床規則も生成済みチャンクを書き換えないため、既存ワールドへ適用すると
新旧チャンク境界で段差が出る可能性がある。既存タイルを上書きせず新規テストワールドで確認する。

| 項目 | 内容 | 対応 |
|---|---|---|
| シミュレーション品質 | 最大のリスク。地形の出来はサーバー体験を決定づける | Phase 1をゲームから完全に独立させ、PNGだけで判断できるようにする。品質が出なければそこで中止し、Terralith継続を選べる |
| シード固定 | 全ワールドが同じ地図になる。新シードでのリセットができない | 永続サーバーでは利点になりうる（実際の地図・地名・wikiが作れる）。リセットが必要ならアセットを再生成する |
| DensityFunctionの性能 | 毎セル呼ばれる。memmapのページフォルトが効く可能性 | Phase 2で実測。必要ならタイルを常駐ヒープへ載せる（1タイル38MB、非圧縮でも許容範囲） |
| バニラ機能との相性 | 洞窟が河床を貫通する、帯水層が海面下で破綻する等 | Phase 2とPhase 4の完了条件に含める。カーバーの適用範囲をdensity functionで抑制できる |
| API変更 | `DensityFunction`周辺はバージョン間で変わりうる | MCバージョンを固定する。更新時はPhase 0を再実行する |
| アセット容量 | 1タイル非圧縮38MB | サーバー側のみ。クライアントjarには載せない |
| 構造物の配置 | 事前計算地形の上でバニラ構造物が浮く・埋まる可能性 | 範囲外としたが、Phase 2で目視確認し、問題があれば別途扱う |
| 工数 | 本計画は資源システムより大きい | 資源システムのブロッカーにしない。並行トラックとし、完成品を見てから採否を決める |

## 10. 資源システムとの進め方

**本計画の成否に関わらず資源システムは進行できる。** 資源システムは`resource_region_plan.md`の設計により
worldgen非依存であり、Terralithでも本計画の地形でも同じコードが動く。

したがって次の順序を推奨する。

1. 資源システムを現行計画どおりTerralith上で進める（ライセンスはサーバー分離型で明文上クリア）。
2. 本計画のPhase 1を並行で走らせ、地形の品質を見る。
3. 品質が出たらPhase 2以降へ進み、最後に資源システムのL1を置き換える。
4. 品質が出なければ本計画を破棄する。資源システムは無傷で残る。

本計画を採用した場合、Terralithへの依存が消えるためライセンス上の論点も同時に消滅する。
