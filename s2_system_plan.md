# S2 国家・領土・Siegeシステム仕様草案

## 1. 文書の位置づけ

本書は、S2で導入する国家、領土、補強、Siege（包囲戦）の基本方針をまとめた仕様草案である。

既存の `停止中_領土システム構想.md` および `停止中_領土システム実装計画.md` とは別の新方針として扱う。具体的な耐久値、維持費、各種タイマーなどのバランス値は、プレイテストを通して決定する。

## 2. 基本コンセプト

- 国家を、メンバー、領土、権限、外交関係を束ねる中心的な単位とする。
- 国家は領土コアを設置し、そのコアを中心としたチャンクを領土として宣言する。
- 領土の広さに応じて維持費が増加する。
- 領土内の建造物は素材を消費して補強できる。
- 補強された拠点は通常のTNTや手掘りでは容易に攻略できず、Create Big Cannons（CBC）の大砲などを用いたSiegeを基本的な攻略手段とする。
- オフライン襲撃は禁止しない。ただし、防衛側が不在の場合は防御性能を高める。
- 長期間活動のない国家の領土は段階的に弱体化し、放棄された領土が永久に残らないようにする。
- プレイヤーが日常的に行う国家、領土、補強、外交、Siege操作は、コマンドではなく統一されたゲーム内GUIを主経路とする。
- GUIは既存の `/pvp` 画面と同じ視覚言語と操作感を採用し、補強状態はワールド上のオーバーレイでも直感的に確認できるようにする。

## 3. 国家

### 3.1 国家が管理する情報

国家は少なくとも次の情報を持つ。

- 一意な国家ID
- 表示名
- 所属メンバー
- 国主
- 役職と権限
- 所有する首都コアと拠点コア
- 他国との外交関係
- 最終オンライン日時
- Siege状態
- 将来的な国家資金または国庫

領土コア、補強、プレイヤー検知ブロックなどの所有権は、原則として個人ではなく国家に帰属させる。

### 3.2 所属メンバーの可視化

国家の所属メンバーと役職は、国家GUIから確認できるようにする。コマンド出力は管理、復旧、ログ確認用の補助経路に限定する。最低限、次の状態を区別する。

- オンライン
- オフライン
- 役職
- 最終オンライン日時

### 3.3 役職と権限

役職名へ処理を直接固定せず、役職ごとに権限を割り当てる方式とする。権限の候補は次のとおり。

- メンバーの招待、加入承認、追放
- 役職の作成、編集、付与
- 領土コアの設置、拡張、縮小
- 補強の施工、修復、解除
- プレイヤー検知ブロックの設定
- 同盟の申請、承認、解除
- 敵対指定およびSiegeの開始
- 国家資金の使用
- Siege中の防衛設定および降伏操作

初期役職として国主、管理者、建築担当、軍事担当、一般メンバーなどを用意できるが、名称と権限構成は今後調整する。

## 4. 外交関係

国家間には、少なくとも次の関係を持たせる。

- 同盟
- 中立
- 敵対

同盟は双方の合意によって成立する相互関係とする。敵対は一方的な指定を可能とするが、敵対指定だけで即座に戦闘上の特権を与えるかは今後決定する。

プレイヤーのネームタグには国家タグを `[TAG] PlayerName` 形式で付加し、閲覧者から見た関係に応じて自国民をシアン、同盟国民を緑、敵対国民を赤、中立国民を灰色で表示する。国家への加入・脱退、同盟・敵対関係の変更はサーバー権威の差分同期後に動的反映する。PvP専用試合中は位置秘匿と味方識別を優先し、既存のPvPネームタグ表示へ制御を譲る。

実際の領土攻略は通常の戦争ではなく、明示的なSiegeとして扱う。

## 5. 領土

### 5.1 領土コア

- 領土は領土コアによって宣言する。
- 領土コアは「首都コア」と「拠点コア」に分ける。
- 国家が最初に設置し、閉鎖判定を通過して稼働した領土コアを、自動的にその国家の首都コアとする。
- 首都コアは1国家につき1個だけとし、国家の敗北、再建、金庫チャンクの基準に使用する。
- 拠点コアは別拠点、植民地、前線基地などを建設する余地として複数設置できる。
- 首都コア確定後に追加で設置・稼働した領土コアは、すべて拠点コアとして登録する。
- 拠点コアの最大数は設定可能にし、初期上限は今後決定する。国家メンバー数を直接上限に使う方式は、サブアカウントによる水増しを招くため採用しない。
- 国家の完全陥落判定は首都コアを基準とし、隠された拠点コアを残すことで敗北判定を回避できないようにする。
- 領土コアの設置時または設定時に、領土とする半径チャンク数を指定する。
- 1個の領土コアが確保できる半径は最大4チャンクとする。
- 領土は中心チャンクを基準とする正方形とする。
- 半径を `r` とした一辺は `2r + 1` チャンクとなり、最大範囲は9×9の81チャンクとなる。
- 同じ領土コアについては、X方向とZ方向に同じ半径を適用する。
- 首都コアの移転処理は保留とし、初期実装には含めない。
- 移転仕様が決まるまでは、首都コアを任意に撤去・移動したり、拠点コアを首都へ昇格させたりできないものとする。
- Siegeや完全陥落に伴う首都コアの停止・再起動は移転として扱わず、同じ首都コアの指定を維持する。

### 5.2 コアの閉鎖空間要求

- 領土コアは、補強ブロックによって外部から完全に閉じられた空間内に設置しなければならない。
- コアを置いただけでは領土を有効化せず、閉鎖判定に合格した時点で初めて稼働可能にする。
- コアを含む内部空間からFlood Fillを行い、外部へ到達できる経路があるかを判定する。
- 閉鎖判定では、補強された完全ブロックだけを通過不能な外殻として扱う。土、石などの未補強ブロックは採掘可能であるため、閉鎖壁として認めない。
- 補強済みのドアまたはハッチは、開閉状態にかかわらず通過不能な外殻として扱う。見た目と通行は通常の開閉状態を維持するが、閉鎖診断では補強境界を継続する。
- 補強されていないドア・ハッチは、開閉状態にかかわらず通過可能として扱う。
- 2ブロックで構成されるドアは上下両方が補強されている場合に限り、開閉状態にかかわらず外殻として認める。
- フェンス、板ガラス、ハーフブロックなど、空間を完全には閉じない形状は原則として外殻に算入しない。個別対応する形状は許可リストで管理する。
- 穴や未補強箇所などを経由して外部へ到達できる場合は閉鎖不成立とする。
- コア周囲には、コアを含む最低3×3×3の内部空間を要求する。
- 判定探索の初期上限はコアから各軸32ブロック、探索可能な内部空間は最大32,768ブロックとする。範囲外へ到達した場合や上限を超えた場合は閉鎖不成立とする。数値は設定可能にする。
- 閉鎖状態はキャッシュし、コアの新規設置・移転時のGUI検証も処理キューへ即時登録する。稼働後の周辺ブロック変化ではコアを再判定待ちとして記録し、短時間の変更をまとめて遅延再計算する。いずれの探索もキュー・訪問済みセル・親経路を保持してtickをまたいで再開し、サーバー全体で最大4,096セル/tick、1コア最大1,024セル/回の予算でラウンドロビン処理する。
- 再判定待ちへ移行した時点から、コアを暫定的な「露出状態」として扱う。疑わしい状態を閉鎖済みとして扱わないfail-safe方式とする。
- 暫定露出中も領土とBastion効果は維持し、コア自体を停止・消滅させないが、コアへの攻撃は可能にする。
- 暫定露出中にコアが受けたダメージは通常どおり反映し、再判定後に閉鎖成立へ戻っても巻き戻さない。
- 再判定の結果が閉鎖成立なら通常状態へ復帰し、閉鎖不成立なら露出状態を継続する。
- 遅延再計算は連続する変更をまとめるが、最初に再判定待ちとなってから最大5秒以内に計算を開始する。時間は設定可能にする。
- 毎tickおよび無関係なブロック変更のたびに、32,768ブロックのFlood Fillを同期実行しない。
- 稼働後に閉鎖が破られた場合はコアを「露出状態」とし、領土を即時消滅させず、攻撃可能な状態として扱う。

### 5.3 維持費

- 領土として確保するチャンク数に応じて維持費が変動する。
- 領土を広く取るほど維持費を高くする。
- 拠点コアには領土面積とは別の基礎維持費を課し、追加するたびに段階的に高くする。
- 同一国家の領土が重複する場合、チャンク部分の維持費は重複加算せず、国家が実際に所有するユニークなチャンク数から算出する。各コアの基礎維持費は別途発生する。
- 維持費は指定したLightman's Currency口座から24時間ごとに支払い、失敗時は1時間ごとに再試行する。口座未設定・自動支払い停止も支払期限後は未払いとして扱う。値はサーバー設定から変更可能にする。
- 初期のGUI見積もり値は、稼働中またはEXPOSEDのユニーク領土8チャンクごとに金貨1枚とし、稼働拠点コアには1個目8枚、2個目16枚、3個目24枚のように段階加算する。これはプレイテスト用の暫定値であり、支払周期・国家口座・未払い処理を接続する前にサーバー設定へ移す。
- 国家ハブの国家口座画面から、`MANAGE_TREASURY` 権限者が自分のアクセス可能なLightman's Currency口座を引き落とし元として指定する。初期支払周期は24時間とし、支払い失敗時は領土効果を即停止せず1時間後に再試行する。連続失敗回数と未払い開始時刻を保存する。
- 未払い開始から24時間は猶予期間とし、領土効果を維持したまま国家メンバーへ警告する。24時間後は補強がCBC等の攻城兵器から受けるダメージを初期値2倍へ増加させ、72時間後は補強の爆発・採掘保護とBastionを停止する。支払い成功時は未払い状態とペナルティを即時解除する。各時間と倍率はサーバー設定から変更可能にする。
- 大国による無制限な領土確保を防ぐため、一定面積を超えると1チャンク当たりの費用が段階的に増える方式も候補とする。

### 5.4 領土コアのHP

- 領土コアはHPを持つ。
- コアの最大HPは、外殻を構成する補強ブロックの数や残存率を加算しない固定値とする。
- 首都コアの初期最大HPは2,000、拠点コアは1,000とし、それぞれサーバー設定から変更可能にする。
- 拠点の総防御力は「コアへ到達するまでの各補強ブロックの耐久値＋固定コアHP」で構成する。
- 外殻補強は到達までの障害として既に耐久値を持つため、コアHPへも加算すると同じ防御を二重計上することになる。固定HPとすることで、壁を大量に詰め込むだけでコアHPが増える不透明な設計も避ける。
- 攻撃側は外側の補強を破壊し、領土コアへ到達してHPを削る。
- 領土コアのHPが0になった時点でSiegeの陥落条件を満たす。
- 領土コアのHPはアイテムによる即時修復ではなく、時間経過によって回復する。
- 最後にダメージを受けてから一定時間は回復を停止し、その後徐々に回復する。
- 初期案として、通常時は最後の被ダメージから5分後に回復を開始し、最大HPの1%を1分ごとに回復する。値は設定可能にする。
- ローリングSiegeタイマーが有効な間は、領土コアの時間回復を完全に停止する。
- Siege終了後に改めて回復待機時間を開始し、その待機時間が経過するまで回復しない。
- 回復待機時間と実際の回復処理は、サーバー開放時間だけを経過時間として数える。サーバー閉鎖中は回復せず、待機時間も進行しない。
- HPが0になった後は即座に領土を消滅させず、陥落後タイマーと反攻戦の猶予を設ける。
- コアの残存HPに応じて警告、煙、火花、国家メンバーへの通知などを行う案を検討する。
- コアHPは領土レジストリへ永続保存し、クライアントへ差分同期する。EXPOSED状態だけを攻撃可能とし、25%・50%の閾値とHP 0到達時に国家メンバーへ警告する。HP 0では自動回復せず、次段階のSiege陥落処理による確定を待つ。

## 6. ブロック補強

### 6.1 補強方法

- 領土コアが所有する領土内でブロックを補強できる。
- 溶接機をメインハンドに持ち、補強素材をオフハンドに持って対象ブロックへ施工する。
- 補強時には素材を消費する想定とする。
- 使用可能な素材は丸石、銅、鉄、金、ダイヤとする。
- 素材ごとに補強レベルまたは性能を変える。

### 6.2 補強の基本性能

- 補強されたブロックは、通常のTNTによる攻撃では原則として無傷とする。
- 外部のプレイヤーが通常の採掘で破壊しようとした場合、数十回から数百回の採掘を必要とする。
- CBCの大砲など、攻城用兵器による破壊を基本的な攻略手段とする。
- 攻撃を受けるたびに補強耐久値が減少し、防衛側が溶接機と素材を使って修復できる方式とする。
- 通常採掘による有効な破壊試行1回を、補強耐久値に対する1ダメージとして扱うことを初期基準とする。
- 採掘速度による極端な連打を防ぐため、同一プレイヤーから同一ブロックへの有効判定には短いクールダウンを設ける。
- CBC 5.11.6以降の公式`ProjectileDamageEvent`を任意依存ブリッジで受け、直接命中と爆発の双方を補強耐久へ反映する。CBCが未導入でも本Modを読み込める構成を維持する。
- CBCの初期ダメージは通常弾48、AP弾96、HE弾40、AP榴弾72、迫撃砲30、破片1発1、AP機関砲1発3、機関銃弾1発1、煙・液体等の補助弾1とする。全値はサーバー設定から変更可能にし、未払い弱体化倍率を適用する。破片Burstは各破片の命中位置だけを処理し、弾体の爆発範囲と混同して範囲ダメージを多重適用しない。領土コアへ有効なダメージを与えられるのは通常弾、AP弾、HE弾、AP榴弾、迫撃砲の重砲弾に限定し、機関砲、機関銃、破片、補助弾は露出済みコアに命中してもHPを減らさない。重砲弾のコアダメージ倍率は補強ダメージから独立したサーバー設定とし、初期値1.0倍とする。
- 修復速度と修復コストは現行の素材1個につき最大耐久の25%回復を初期値として継続し、プレイテストで調整する。

### 6.3 素材ごとの補強耐久値

初期バランス値を次のとおり定める。すべてサーバー設定から変更可能にする。

| 素材 | 補強耐久値 | 通常採掘の必要回数目安 | 位置づけ |
| --- | ---: | ---: | --- |
| 丸石 | 32 | 32回 | 仮設壁や小規模拠点向けの最低限の補強 |
| 銅 | 64 | 64回 | 安価な常設防壁 |
| 鉄 | 128 | 128回 | 標準的な軍事防壁 |
| 金 | 192 | 192回 | 高コストな上位防壁 |
| ダイヤ | 320 | 320回 | 重要区画向けの最高級防壁 |

値をおおむね倍増させることで素材更新の効果を明確にしつつ、最高級だけが突出しすぎないようにする。金の特殊性能は初期実装へ含めず、まず耐久値の差だけでバランスを検証する。

### 6.4 補強状態のワールドオーバーレイ

補強済みと未補強のブロックを、元のブロックテクスチャを置換せず、クライアント側の薄い表面被膜と詳細オーバーレイで判別できるようにする。通常プレイ中に常時大量描画はせず、溶接機を持って施工または点検の意図がある場合に表示する。

#### 表示を開始する操作

- 溶接機を持った時は、照準中ブロックの補強状態、素材、現在耐久値、最大耐久値、施工または修復に必要な素材を右中央の小型HUDへ表示し、ホットバー、アクションバー、左下チャットとの重なりを避ける。
- 溶接機を持っている間は、周辺の補強済みブロックへ素材別の薄い表面被膜を表示する。損傷時は赤方向へ色を寄せ、被膜を少し強くしてブロック自体の見た目から判別可能にする。
- 溶接機を持った状態で専用キーを押すと「補強詳細モード」を切り替え、表面被膜を強めて損傷パルスと照準記号を追加する。キー割り当てはMinecraftのキー設定から変更可能にする。
- 領土コアGUIの「外殻を点検」から「閉鎖診断モード」を開始し、対象コアの閉鎖判定に関係する外殻と問題箇所を表示する。
- GUIと専用キーを主経路とし、オーバーレイ表示のためにコマンド入力を要求しない。
- モードが有効な間は画面端へ状態名、対象コア、凡例、終了キーを表示し、プレイヤーが表示中であることを見失わないようにする。

#### 状態ごとの表現

| 状態 | 基本色 | 表示 | 意味 |
| --- | --- | --- | --- |
| 補強済み・健全 | シアンまたは緑 | 細い輪郭＋薄い面 | 有効な補強が存在する |
| 補強済み・損傷 | 黄～赤 | 輪郭＋損傷率に応じたパルス | 修復が必要。照準時は耐久ゲージも表示 |
| 未補強 | 赤橙 | 斜線または `×` パターン付きの面 | 対象ブロックに補強データがない |
| 補強効果が無効 | 紫 | 破線輪郭 | 陥落、長期不在、設定などにより補強効果が停止中 |
| 施工不可 | 灰 | 点線輪郭 | 形状、権限、領土条件などにより補強できない |
| 閉鎖判定の問題箇所 | 赤 | 強調輪郭＋外向きマーカー | 穴、未補強箇所、開いたドアなど、外部到達経路の原因候補 |

- 色だけに依存せず、実線、破線、斜線、パルス、記号を併用し、色覚差や明るいテクスチャ上でも区別できるようにする。
- 素材種別は基本状態色を壊さない範囲で、照準HUDの素材アイコンと名称、および輪郭の副色で示す。
- ドアは上下を1組として扱い、片側だけ補強されている場合は両方へ未完了マーカーを表示する。ハッチや開閉ブロックは現在の開閉状態も表示する。
- 補強成功、修復、解除時は対象ブロックだけ短い色変化と音で即時フィードバックし、サーバー確定後に正式状態へ更新する。

#### 表示範囲、権限、負荷対策

- 通常の施工表示はプレイヤー周辺かつ描画距離内だけを対象とし、初期表示半径は32ブロックとする。薄い表面被膜は近い順に最大192ブロック、詳細モードは最大512ブロックを1フレームの描画上限とし、値は将来クライアント設定で縮小可能にする。
- 未補強表示で周辺の地形すべてを赤く塗らない。通常施工モードでは「照準中ブロックと、その面から連続する近傍の施工候補」を未補強表示の対象とし、閉鎖診断モードではサーバーが返した外殻候補だけを対象とする。「補強済みのみ」「未施工・損傷を優先」「対象範囲をすべて」の表示フィルターを切り替え可能にする。
- 閉鎖診断モードは選択した自国コア1個だけを対象とし、Flood Fill結果から外殻候補と問題箇所だけを表示する。無関係な全ブロックを走査・転送しない。
- 面オーバーレイは原則として深度判定を行う。閉鎖診断の問題箇所だけは、遮蔽物越しに低い透明度のシルエットと距離を表示できるようにする。
- 敵国の補強構成を透視する機能にしない。詳細オーバーレイは自国領土内で補強閲覧権限を持つプレイヤーにだけ許可し、サーバーも許可された状態データだけを送信する。
- 補強状態は変更座標だけを1tick単位で集約した差分パケットで同期し、クライアントは影響チャンクと境界で接する隣接チャンクだけを再メッシュする。初回、ディメンション変更、8ブロック以上の移動時と10秒ごとだけ半径内の完全スナップショットを取得し、取りこぼしを自己修復する。施工進捗は建設中エントリだけを毎秒差分更新し、完成済み補強や全領土を定期走査しない。
- 視錐台外、距離外、背面の面を描画対象から除外し、1フレームの最大描画数を設定可能にする。上限超過時は近いブロックと問題箇所を優先する。
- オーバーレイを無効化するクライアント設定、透明度、パルスの強さ、色プリセットを用意する。

### 6.5 Bastion相当の領土内制限

Bastion相当の制限は、領土・補強システムの初期実装（Phase 1相当）に含める。

領土内で権限を持たない外部プレイヤーに対して、平時とSiege中の両方で次の行為を禁止する。

- ブロックの設置
- 水、溶岩などの流体設置
- 足場、はしご、ボートなど、設置物を利用した即席の侵入経路作成
- 領土内を着地点とするエンダーパールのテレポート

これにより、pillarで壁を越える、コア上空から降下用の足場を作る、エンダーパールで外殻を迂回するといった、補強を攻略せずに侵入する経路を防ぐ。自国民、許可された役職、同盟国などへの例外は国家権限から設定可能にする。

該当チャンクが正式に陥落して所有権を失った場合は、旧国家によるBastion制限を解除するか、新しい所有国家の権限へ切り替える。

飛行船、車両、Createの移動体などについては、領土へ侵入したという理由だけで停止、排除、移動禁止にはしない。移動体に乗ったまま敵領土へ侵入、通過、輸送、砲撃することは可能とする。

ただし、外殻を迂回してコアへ直接侵入する行為を防ぐため、シーズン2初期実装では次の暫定制限を適用する。

- 敵対国家の領土内では、権限を持たないプレイヤーがcontraptionから任意に降板、下船、ディスマウントできない。
- 通常の騎乗解除だけでなく、Aeronauticsなどの物理体からワールド側へ離脱する操作も同じ降板として扱う。
- 移動体の破壊などによって強制的に降板させられた場合は、プレイヤーを最後に記録した領土外の安全地点へ移動する。保存地点が消失・封鎖されて利用できない場合は、敵領土外であることを確認したうえでオーバーワールドの共有スポーンを退避先とする。共有スポーンも敵領土内の場合は自動転送せず、管理者へ連絡するよう通知する。
- ログアウトや再接続によって降板制限を回避できないよう、最後の領土外安全地点と搭乗状態を保存する。
- 該当領土が陥落・占領されてBastion制限が解除された場合は降板可能にする。
- 詳細な着陸、上陸装備、空挺、正式参戦などの戦術設計は後から追加する。

## 7. Siege（包囲戦）

### 7.1 基本フロー

戦争はSiegeとして扱い、基本的には次の流れで進行する。

```text
Siege開始
  ↓
攻撃側がCBCなどで外側の補強を破壊
  ↓
防衛線を突破して領土コアへ到達
  ↓
領土コアのHPを削る
  ↓
コア陥落
  ↓
反攻戦、講和、または外周領土の喪失・占領処理
```

防衛側は補強の修復、物資補給、砲撃への対抗によってコアを守る。

### 7.2 Siegeタイマー

- Siegeタイマーは、国家同士が現在包囲戦を行っているかを判定するための状態タイマーとする。
- ある国家が敵国家の領土内にある補強ブロックまたは領土コアに対して、破壊につながり得る行為を行った時点で自動的に開始する。
- 実際にダメージが通ったか、ブロックが破壊されたかは開始条件にしない。TNTが無効化された場合や補強が耐えた場合でも、敵対的な破壊試行として帰属できれば開始する。
- 直接採掘、爆発物、投射物、CBC、他Modの兵器や機械など、破壊手段は問わない。
- 自然発生または実行国家を特定できない環境破壊は、Siege開始条件に含めない。
- Siegeは攻撃国家、防衛国家、対象領土コアの組み合わせで管理し、別領土への攻撃と混同しない。
- Siege開始条件とSiegeタイマーの延長条件は分離する。
- 最初の破壊試行では、実ダメージの有無にかかわらず5分間の初動ロックを開始する。
- 初動ロック中もSiege状態として扱い、攻撃側と防衛側の両国家に操作制限を適用する。
- 初動ロックは、有効な攻城行為が発生しなければ5分後に終了する。
- 初動ロック中の追加の破壊試行は、対象ブロックが異なっていても初動ロックを延長・再発行しない。
- 有効な攻城行為がないまま初動ロックが終了した場合、同じ攻撃国家と防衛国家の組み合わせに60分間の初動ロック再発動クールダウンを設ける。
- 再発動クールダウンは対象ブロックや対象コアではなく国家ペア単位で管理し、別の壁や別拠点を殴ることで回避できないようにする。
- 再発動クールダウン中の実ダメージを伴わない破壊試行は記録だけを行い、新しい初動ロック、操作制限、Discord通知を発生させない。
- 再発動クールダウン中でも有効な攻城行為が発生した場合はクールダウンを無視し、対象コアに対するローリングSiegeを即座に開始する。
- 有効な攻城行為が発生した場合、Siegeタイマーを残り30分まで更新するローリング方式とする。
- 最後の有効な攻城行為から30分間、新たな有効な攻城行為がなければSiegeを終了する。
- ローリングSiegeタイマーの30分はサーバー開放時間だけを消費する。サーバーが閉鎖または停止している間は残り時間を保存して一時停止し、次回開放時に残り時間から再開する。
- 単に補強ブロックやコアを殴るだけで実ダメージが発生しない破壊試行は、Siege開始の契機にはなるがタイマーを延長しない。
- 同一プレイヤーから同一ブロックへの重複ログ記録には60秒のクールダウンを設ける。
- 初動ロック5分、継続タイマー30分、国家ペア単位の再発動クールダウン60分、同一対象のログクールダウン60秒は、すべてサーバー設定から変更可能にする。
- Siegeタイマー発動中、攻撃側と防衛側の両国家について解散と脱退を禁止する。
- 初動ロックおよびローリングSiege中は、国家解散、メンバーの自主脱退、対象コアの撤去、対象領土の縮小・放棄・譲渡、首都指定の変更を禁止する。
- 新規加入、追放、役職変更、同盟・敵対関係の変更は原則として許可する。
- Siege開始時の攻撃国家、防衛国家、対象コアは固定し、途中の追放、役職変更、外交関係変更によって既存Siegeを終了、無効化、別国家へ付け替えできないようにする。
- タイマーは主にSiege状態と操作制限の判定に使用し、それ自体を勝利条件にはしない。
- 有効な攻城行為が続く限りSiegeの継続時間に固定上限は設けない。実際に資材を消費して攻撃が継続している状態は、進行中の包囲戦として扱う。
- Siegeの勝利条件、敗北時の処理、再攻撃クールダウンは今後決定する。

#### 戦闘関連タイマーの時間基準

初動ロック、初動ロック再発動クールダウン、ローリングSiegeタイマー、陥落後タイマー、反攻戦の確保時間、コアの回復待機・自然回復は、すべてサーバー開放時間だけを経過時間として数える。

- サーバーの閉鎖・停止中は残り時間と進捗を保存し、時間を進めない。
- 次回開放時は、保存した残り時間と進捗から再開する。
- 長期不在判定に使用する最終オンラインからの経過日数は、この戦闘時間基準とは別に実時間で計測する。

#### 有効な攻城行為

Siegeタイマーを延長できる有効な攻城行為は、攻撃国家へ帰属でき、次のいずれかを満たすものとする。

- 補強耐久値へ実際にダメージを与えた。
- 補強ブロックを破壊した。
- 領土コアのHPへ実際にダメージを与えた。
- CBCの有効弾が補強ブロックまたは領土コアへ命中し、ダメージを与えた。
- 攻城手段として許可された爆発物、投射物、機械または他Modの兵器が、補強またはコアへ実際にダメージを与えた。

これにより、敵領土を一度攻撃すれば即座に離脱不能となる一方、ダメージのない壁殴りだけで国家を永久に拘束することはできない。

### 7.3 陥落後タイマーと反攻戦

- 領土コアのHPが0になると、その領土を陥落状態へ移行させ、通常のSiegeタイマーとは別に陥落後タイマーを開始する。
- 陥落直後に全補強と領土を消滅させず、外周から中心へ段階的に補強効果を失わせる。
- 初期案では陥落後タイマーを30分とし、10分ごとに次の段階へ進める。
- 陥落後タイマーはサーバー開放時間だけ進行し、サーバー閉鎖・停止中は残り時間を保存して停止する。
- 陥落状態は継続中の戦闘として扱い、防衛国家が全員オフラインでも補強およびコアへオフライン3倍防御を適用しない。
- 陥落したコアは、陥落後タイマー中に時間回復しない。

| 陥落後の経過時間 | 状態 |
| --- | --- |
| 0～10分 | コア停止。領土と残存補強は維持され、旧所有国は反攻戦を行える |
| 10～20分 | 外周2チャンクの補強効果を無効化する |
| 20～30分 | コア周辺を除く残りの補強効果を無効化する |
| 30分経過 | コア周辺の補強効果も無効化し、対象が首都コアか拠点コアかに応じて領土の喪失または占領処理を確定する |

- 陥落後タイマー中、旧所有国が領土コア周辺を奪還し、一定時間継続して確保できれば反攻戦成功とする案を採用する。
- 反攻戦の確保進捗は、旧所有国のプレイヤーが指定範囲内に存在し、かつ攻撃国プレイヤーが指定範囲内にいない場合だけ増加する。
- 両国のプレイヤーが指定範囲内にいる場合は競合状態とし、確保進捗を停止する。
- 初期実装の確保判定へ参加するのは、Siege開始時に固定された旧所有国と攻撃国の2国家だけとする。
- 旧所有国の同盟国、中立国、その他の第三国プレイヤーは、範囲内に存在しても確保進捗を増加・停止・減少させない。
- 同盟国が戦闘そのものへ協力できるかはPvPと外交権限で別途判定するが、同盟関係だけで自動的に反攻戦の当事国にはしない。
- 将来、正式参戦システムを導入した場合に限り、明示的に参戦した国家を確保判定へ加えられるよう拡張する。
- 旧所有国のプレイヤーが指定範囲からいなくなった場合、確保進捗を初期案として獲得速度の半分で0まで徐々に減少させる。減少速度は設定可能にする。
- 反攻戦の確保進捗もサーバー開放時間だけ進行・減少し、サーバー閉鎖・停止中は保存した値のまま停止する。
- 陥落後タイマー自体は旧所有国・攻撃国のオンライン状況や確保範囲への滞在にかかわらず、サーバー開放中は進行する。旧所有国が不在でも占領確定へ向けた時間は進む。
- 陥落後タイマーが満了するまでに確保を完了できなければ、進行途中であっても反攻戦失敗とする。
- 反攻戦成功時はコアを最大HPの25%で再起動し、陥落後タイマーを解除する初期案とする。
- 確保判定の方法、必要時間、攻撃側による占領方法、最終的な領土移転条件は今後決定する。
- 無効化された補強はブロック自体を即時破壊せず、まず補強効果だけを失わせる。実際のブロック消去や崩壊演出を行う場合は、負荷と建築物保護を考慮して別途決定する。

### 7.4 講和

Siegeの膠着を終わらせるため、コア陥落以外の出口として双方合意による講和を設ける。

講和の最小構成は次のとおりとする。

- 双方が保持する捕虜の返還
- 一方から他方へ支払う金貨賠償
- 対象となるSiegeおよび陥落後タイマーの終了
- 講和成立後の一定期間の再攻撃禁止

- 講和条件は外交権限を持つプレイヤーが提示し、相手国の外交権限者が承認した時点で成立する。
- 捕虜返還、金貨移動、Siege終了は一括で処理し、一部だけが実行された状態を残さない。
- 支払国の残高不足、捕虜情報の不整合などで全条件を実行できない場合、講和を成立させない。
- 講和成立後の再攻撃禁止時間と、将来的に領土条件を講和へ含めるかは今後決定する。

### 7.5 完全陥落と敗者の再建

- 国家の完全陥落は首都コアの陥落後タイマーが完了した時点で判定する。拠点コアが残っていても完全陥落判定を妨げない。
- 拠点コアが陥落した場合は、その拠点コアが形成する領土だけを喪失・占領処理の対象とし、国家全体の完全陥落にはしない。
- 完全陥落しても、国家そのもの、所属メンバー、役職、外交情報を消滅させない。
- 首都コアが存在する首都チャンクは割譲不可とする。拠点コアのチャンクは割譲・占領対象にできる。
- 国家が指定できる金庫チャンクも1国家につき1チャンクまでとし、割譲不可とする。
- 首都チャンクと金庫チャンク以外の外周領土および拠点領土は、中立化、占領または講和による割譲の対象にできる。
- 完全陥落処理の確定後、首都チャンクと金庫チャンクの所有権を敗戦国へ戻し、再建用の最低領域として残す。
- 完全陥落時に残存する拠点コアを自動削除・自動譲渡はしない。各拠点は個別Siegeまたは講和条件によって処理するが、敗戦国の再建猶予中にどの保護を維持するかは今後決定する。
- 「リセット」は国家と再建領域の所有権を回復させることを意味する。内部の建築物、コンテナ、アイテムを消去するかどうかは別途決定し、暗黙に削除しない。
- 完全陥落後は、同じ国家に対して一定期間新しいSiegeを開始できない再建猶予を設ける。
- 反攻戦失敗後にも、同じ領土または国家への再攻撃クールダウンを適用する。
- これにより戦争による領土・資産上の敗北は成立させつつ、制度上の完全駆逐を防ぎ、敗戦国が再建できる余地を保証する。

## 8. オンライン・オフライン防御

### 8.1 短期的なオフライン防御

- 国家メンバーがオンラインの場合を通常耐久とする。
- 国家メンバーが全員オフラインの場合、補強と領土コアへの被ダメージを通常の3分の1にする案を基本とする。
- 結果として、オフライン時は実質的に約3倍の耐久力となる。
- HPそのものを増減させず被ダメージ倍率を変更することで、ログイン・ログアウト時の不自然なHP変動を避ける。
- 戦闘中のログアウトによる即時強化を防ぐため、最後の国家メンバーがログアウトしてから一定時間後にオフライン防御へ移行する。初期案は20分とする。
- オンライン判定には領土を所有する自国のメンバーだけを使い、同盟国のログイン状態は含めない。
- Siege開始時点ですでに防衛国家が20分以上無人でオフライン防御が有効だった場合は、そのSiege記録へ状態を固定し、ローリング移行後も3倍防御を維持する。これにより元から無人の国家に対する襲撃でも短期オフライン防御を実際に機能させる。
- オンライン中に始まったローリングSiegeでは、防衛国家の全員が途中でログアウトしてもオフライン3倍防御を適用しない。Siege中のログアウトによって進行中の攻城で補強やコアが急に硬くなることはない。
- コア陥落によってローリングSiegeから陥落後タイマーへ移行した場合も、陥落処理が確定または反攻戦が成功するまでオフライン3倍防御を適用しない。
- コアが陥落せずローリングSiegeタイマーが終了した場合、または反攻戦成功によって通常状態へ戻った場合は通常のオンライン判定へ戻し、防衛国家がオフラインであれば3倍防御を適用する。
- 5分間の初動ロックだけが有効で、まだ有効な攻城行為が発生していない段階では、通常のオフライン防御判定を維持する。

### 8.2 長期不在による補強の弱体化

放棄された国家の領土が永久に強固な状態で残ることを防ぐため、国家メンバーのオンラインが一定期間ない場合は補強を段階的に弱体化する。

- 最後に国家メンバーがオンラインだった時刻から経過時間を計測する。
- 最初の1週間は通常の補強性能を維持する。
- 1週間を超えると補強性能の段階的な低下を開始する。
- 1か月経過時点で補強効果を完全に失わせる。
- 30日経過だけでは補強データと保存HPを削除せず、防御効果だけを停止する。ブロックが実際に破壊された場合は対応する補強データを通常どおり削除する。
- 7日を超えた長期不在弱体化中は短期的なオフライン3倍防御を重ねない。国民が1人でもログインすれば即座に通常強度へ復帰する。

弱体化の初期案は次のとおりとする。数値は設定可能にする。

| 最終オンラインからの経過時間 | 補強性能の目安 |
| --- | ---: |
| 0～7日 | 100% |
| 8～14日 | 75%（被ダメージ4/3倍） |
| 15～21日 | 50%（被ダメージ2倍） |
| 22～29日 | 25%（被ダメージ4倍） |
| 30日以上 | 0% |

段階的な被ダメージ倍率は補強と領土コアの両方へ適用する。30日到達時は補強保護を停止し、コアへの4倍ダメージは維持する。各日数境界はサーバー設定から変更可能にする。

## 9. プレイヤー検知ブロック

プレイヤー検知ブロックの既存ホワイトリスト機能は、国家システムの所属、役職、外交関係を利用する共通アクセス制御へ移管する。

検知ブロックには、個々のメンバーUUIDを複製して保持するのではなく、次のような許可規則を設定する。

- 自国民全員
- 指定した自国の役職
- 同盟国民
- 指定した国家
- 指定した個人
- 全プレイヤー
- 許可なし

メンバーの加入・脱退、役職変更、同盟の締結・解除は、検知時に国家情報を参照して自動的に反映する。

拒否規則を導入する場合は、個人拒否、国家・役職拒否、個人許可、自国・役職許可、同盟国許可、デフォルト設定のように評価順を明確にする。

## 10. ゲーム内GUIと操作方針

### 10.1 UIの基本方針

- S2の通常操作は「画面を開く→状態を確認する→カードまたはボタンを選ぶ→必要なら確認する」で完結させ、コマンド文字列や引数の暗記を要求しない。
- デザインは `PvpScreen` および `PvpTaskScreen` を基準とし、暗色グラデーション背景、半透明パネル、細い境界線、左アクセント付きカード、タブ、ホバー、選択状態、無効状態、スクロール領域を共通化する。
- 基準色は本文 `#E8EDF3`、補助文 `#8F9AA8`、パネル `#12161D`、カード `#1B222C`、ホバー `#242E3B`、シアン `#5DCBFF`、成功 `#68E09B`、危険 `#FF6577`、警告・費用 `#FFB454` とする。画面ごとの独自色は国家色や状態表示など意味がある箇所だけに使う。
- 一覧はカード、機能の切り替えはタブ、主要操作は画面下部のフッター、閉じる操作は右上の `×` とEscへ統一する。
- 単発のチャット通知とトーストは、緑系グラデーションの `[MoveEarth]`、灰色系グラデーションの `>>>`、状態記号、本文の順に統一する。本文は情報を白灰、成功を緑、警告を橙、失敗を赤とし、一覧・複数行レポートでは各行への接頭辞反復を避ける。
- 危険操作は赤いアクションとして分離し、対象、結果、取り消し可否を示す専用確認モーダルを挟む。確認のためにチャットへ文字列を入力させない。
- 国家・領土・PvPなどの全画面型MoveEarth GUIを開いている間は、後から描画されるバニラのチャットレイヤーを抑止し、カードや操作ボタンの前面をチャットが覆わないようにする。通常HUDおよび対象外画面ではチャットを抑止しない。
- 画面サイズに応じてパネル、列数、1ページの件数を縮小し、小さい画面でも操作対象が重ならないようにする。マウスホイール、ドラッグ可能なスクロールバー、キーボード、ゲームパッド相当のフォーカス移動を考慮する。

### 10.2 バニラGUIコンポーネントの扱い

- `Screen`、文字描画、アイテム描画、ツールチップ、ナレーションなどMinecraftとの統合に必要な基盤は利用するが、見た目と操作部品はS2共通コンポーネントとして実装する。
- 通常の `Button`、チェスト型メニュー、ホッパー型メニュー、看板入力、金床入力、チャットクリックコマンドをS2の主要UIとして流用しない。
- カード、タブ、ボタン、トグル、チェック項目、ラジオ選択、ドロップダウン、スライダー、耐久ゲージ、検索欄、確認モーダル、トーストを `GuiGraphics` ベースの独自部品として共通化する。
- 自由入力が本当に必要な国家名、役職名、確認コードなどに限り文字入力機構を使用する。この場合もバニラ標準の外観を露出させず、S2テーマの枠、ラベル、エラー、文字数、確定状態を描画する。
- 入力値検証は送信前にクライアントで分かりやすく表示し、サーバー側でも必ず再検証する。無効な操作はボタンを無効表示し、ホバーまたは直下の説明で理由を示す。

### 10.3 国家ハブと画面構成

専用キー（初期候補 `N`、変更可能）で国家ハブを開き、所属状態と権限に応じて次の画面へ移動できるようにする。`S2` は開発上のシーズン区分であり、プレイヤー向け画面名やコマンド名には使用しない。

| 画面 | 主な内容と操作 |
| --- | --- |
| 概要 | 国家概要、オンライン人数、維持費、コア状態、進行中Siege、重要通知をカード表示 |
| 国家 | 国家作成、名称・説明確認、国主情報、国家設定、解散または再建 |
| メンバー・役職 | オンライン状態、最終オンライン、招待、承認、追放、役職付与、権限編集 |
| 領土・コア | 首都・拠点コア一覧、半径、維持費、HP、閉鎖状態、領土プレビュー、外殻点検開始 |
| 補強 | 素材別凡例、施工権限、周辺統計、損傷一覧、施工モード切替、修復対象への案内 |
| 外交 | 同盟申請、中立・敵対状態、受信申請、変更結果、再操作可能時刻 |
| Siege | 攻撃側・防衛側、対象コア、段階、残り時間、HP、最近の有効攻城行為、降伏・講和操作 |
| 検知設定 | プレイヤー検知ブロック一覧、許可規則、役職・国家・個人の優先順位 |
| 通知・Discord | ゲーム内通知、座標公開、Discord連携状態、確認コード入力、テスト通知、解除 |

- 未所属プレイヤーには国家一覧と国家作成または招待確認を最初に表示し、所属済みプレイヤーには国家概要を最初に表示する。
- 権限のないタブは隠して存在を分かりにくくするのではなく、閲覧可能な情報は表示し、操作だけを無効化して必要権限を説明する。機密情報はサーバーから送らない。
- 領土コアを操作した場合は国家ハブの該当コア詳細を直接開く。プレイヤー検知ブロックも同様に該当設定画面を直接開き、汎用コンテナ画面を挟まない。
- Siege通知、コア警告、招待、外交申請は、通知カードまたはチャット内の明示的な「画面を開く」アクションから該当画面へ遷移できるようにする。状態変更そのものをチャットクリックだけで確定させない。
- 直前に開いていたタブ、一覧のスクロール位置、オーバーレイ設定はクライアント内で保持する。ただし権限やサーバー状態は画面を開くたびに再取得する。

### 10.4 コア設置と領土設定のGUIフロー

領土コア設置後の設定はウィザード形式とし、チャットコマンドによる座標・半径入力を不要にする。

```text
コアを設置して操作
  ↓
コア種別と所有国家を確認
  ↓
半径をカードまたはスライダーで選択
  ↓
ワールド上へ領土境界と維持費見積もりをプレビュー
  ↓
閉鎖診断モードで未補強箇所を確認・施工
  ↓
再判定結果、維持費、重複領土、権限を最終確認
  ↓
稼働を確定
```

- 半径変更中は対象チャンクの境界を柱状または地表輪郭で表示し、自国、同盟国、他国、重複、設置不可を色とパターンで区別する。
- 閉鎖不成立時は単なるエラー文だけで終わらせず、「外殻を点検」ボタンから問題箇所オーバーレイへ移行できるようにする。
- 維持費、追加コア費用、確保チャンク数、重複によって増えないチャンク数を確定前に表示する。

### 10.5 補強施工のGUI・HUDフロー

- 溶接機の照準HUDは、対象ブロックの状態、素材、耐久値、必要素材、操作可能かを1か所に表示する。
- 通常クリック操作は施工または修復へ割り当て、補強解除、素材の置換、詳細確認など競合する操作は溶接機用の小型ラジアルメニューまたはコンテキストパネルから選択する。
- 溶接機を持った状態のマウススクロールは施工ブラシ変更へ割り当て、`1×1`、`3×3`、`5×5`を切り替える。施工対象は照準した面と平行な正方形とし、壁の裏や床下まで立方体状に巻き込まない。
- 一括施工でも各ブロックについて国家、領土、権限、形状、素材をサーバー側で再検証する。最大処理数は5×5の25ブロックとし、素材不足時は所持数まで処理して停止する。
- オフハンド素材が複数スタックに分散していても、使用予定素材と消費数をHUDへ明示する。素材不足、権限不足、領土外、Siege中の制限は実行前に表示する。
- 連続・一括施工では同じ成功演出を過剰に重ねず、「新規施工数・修復数・スキップ数」を1件のリッチメッセージへ集約表示する。
- 新規補強は施工直後から防御力を持たせず、30秒の硬化待ちを経て有効化する。有効化後は60秒かけて素材ごとの最大HPまで徐々に充填し、再起動をまたいでも時刻と進捗を保存する。旧形式の補強データは完成済みとして移行する。
- 硬化待ち、HP充填中、完成済み、損傷中はブロック表面の色と点滅周期を変えて区別する。照準HUDには段階名、残り秒数、現在HPと横型進捗バーを表示し、詳細オーバーレイではブロック上にも小型バーを表示する。
- 補強オーバーレイは半径64ブロック、最大8192件まで同期し、通常表示4096件・詳細表示8192件を上限とする。施工ブラシ内は未補強を赤、硬化待ちを紫、HP充填中を橙、完成済みを緑で明確に塗り分け、選択面全体の外周には暗緑と高輝度緑の二重枠を表示する。
- 補強オーバーレイ描画はOpenGL 4.5環境で永続マップ三重バッファと面単位のインスタンシングを使用し、隣接補強に完全に隠れる内部面を同期更新時に除外したうえで最大6回の描画呼び出しへ集約する。OpenGL 4.5非対応または初期化失敗時はMinecraft標準のRenderType経路へ自動退避し、三重バッファが一時的に使用中なら重い互換描画へ切り替えず、そのオーバーレイフレームだけを省略する。描画状態の変更と復元はRenderSystem経由で行い、毎フレームの同期的なGL状態取得を行わない。描画Modやドライバとの相性確認用にJVMプロパティ `moveearth.reinforcement.forceCompatibilityRenderer=true` でも互換経路を強制できる。両経路でチャンク単位の視錐台判定、距離判定、近距離優先の描画上限を共有し、同期元もチャンク索引と内容署名を使って全件走査と未変更スナップショット送信を抑える。
- Jade導入環境では照準中の全ブロックに補強の有無を表示し、補強済みなら硬化待ち、HP充填中、損傷、有効の状態、素材、現在HP、最大HP、進捗バーをサーバー権威データから表示する。Jadeは任意依存とし、未導入環境の起動を妨げない。
- 施工音は周囲約50ブロックへ届く強さとし、有効化時と完成時にも段階音を鳴らす。一括施工の同時音はチャンク単位で集約して音割れを防止する。
- 損傷ブロック一覧のカードを選択すると、同じディメンションかつ情報公開が許可される範囲で距離と方向を表示し、閉鎖診断または修復オーバーレイへつなげる。

### 10.6 コマンドの位置づけ

- プレイヤー向けの国家作成、招待、役職、領土、補強、外交、Siege、講和、通知設定は、すべてゲーム内GUIから実行可能にする。
- `/nation` は国家ハブを開く短い入口としてのみ残してよいが、通常利用でサブコマンドや座標、国家ID、プレイヤー名の手入力を要求しない。
- 詳細コマンドはサーバーコンソール、権限管理プラグイン、自動化、障害復旧、デバッグ用に限定する。GUIとコマンドは同じサーバーサービス層を呼び出し、検証規則や権限判定を二重実装しない。
- 管理者向け操作も可能な限りS2管理GUIへ収める。大量処理、監査ログ出力、壊れたデータの復旧など、GUI化すると誤操作が増える処理だけをコマンドに残す。
- コマンド失敗文にコマンドの再入力だけを案内せず、可能なら該当GUIを開く導線を提示する。

### 10.7 通信、整合性、操作フィードバック

- 画面へ渡す状態はサーバー権威とし、クライアントからは「選択したIDと意図」だけをC2Sパケットで送る。国家、役職、コア、領土、Siegeの最終状態をクライアント入力から信用しない。
- 画面を開くS2Cスナップショットと、画面表示中の差分更新パケットを分離する。メンバー状態、Siegeタイマー、コアHPなど変化する情報は画面を閉じずに更新する。
- 操作要求には成功、拒否理由、最新リビジョンを返し、二重クリック、古い画面、パケット再送による重複処理を防ぐ。
- 保存中は対象操作だけを一時無効化し、成功時はカード更新と小型トースト、失敗時は入力箇所に近いエラー表示を行う。原則として操作のたびに画面を閉じない。
- 削除、解散、降伏、領土放棄、Discord解除などの危険操作は、サーバー状態を再確認したうえで確認モーダルを表示する。

### 10.8 実装順序と受け入れ基準

1. **実装済み:** `/pvp` 系画面から色、パネル、カード、独自ボタン、タブ、スクロール、モーダル、トーストをS2共通UI部品として切り出す。
2. **実装済み:** 国家ハブ、国家概要、メンバー・役職画面、GUIとサーバー権限サービス間のパケット規約、国家作成GUI、国家・所属の永続保存、初期Owner/Member役職、オンラインプレイヤーへの招待、招待の承認・辞退、一般メンバーの脱退、権限保持者による追放、カスタム役職の作成・名称／権限編集・メンバーへの付与を実装した。役職IDは名称変更に影響されないUUIDベースとし、Owner権限はカスタム役職へ付与できない。所属・役職変更はUUID、権限、最新リビジョンをサーバー側で再検証し、招待先には画面を強制表示せず通知する。カスタム役職の確認付き削除、削除対象役職の全メンバーを標準Memberへ再割当するサーバー処理、ログイン／ログアウト時の最終オンライン日時の永続記録とメンバー一覧表示を備える。国家設定GUIから国名・タグ変更、国主の明示、確認付き国主移譲、確認付き国家解散を行える。Siege中の国主移譲・国家解散・当事国メンバーの自主脱退をサーバー側で拒否し、捕虜が残る国家の解散も拒否する。解散時は所属、招待、外交、領土コア、国家金庫、維持費、講和、Siege、停戦、補強、捕虜帰還予約を一括整理し、物理コアブロックと孤立データを残さない。
3. **実装済み:** 領土コア設定ウィザード、チャンク境界プレビュー、補強施工・修復、閉鎖診断オーバーレイ、床置き領土コア、ACTIVE／EXPOSED再判定、Bastion、外交、関係別ネームタグ、国家口座と維持費を実装した。閉鎖診断では外部までの漏出経路に加え、経路上にある「壁として使用可能だが補強データが一切ないブロック」を抽出し、赤紫の全面点滅、二重枠、ビーコン、座標付きHUDで強調する。補強済みドア／トラップドアは開閉状態にかかわらず閉鎖外殻として扱い、閉鎖を破る変更は即時EXPOSED化して負荷制限付き遅延再診断を行う。領土コアには首都・拠点別の固定HP、永続化、サーバー開放時間だけで進む遅延回復、GUI内HPバー、差分同期、閾値警告を追加した。CBC 5.11.6以降の公式ダメージフックを任意依存で接続し、弾種別の補強・EXPOSEDコアダメージを実装した。CBC爆発が爆発対象確定前に石系ブロックを丸石へ変換する処理との競合を避けるため、爆発弾は公式フックの段階で設定範囲内の補強・コアへダメージを適用し、保護対象を含む爆発の地形変換を先に停止する。維持費は口座未設定を含めて期限を管理し、猶予、補強弱体化、補強保護・Bastion停止の段階的ペナルティと復旧を実装した。第三段階の数値は専用サーバー設定へ移し、単体テストとオフラインassembleで検証する。
4. **実装済み:** 攻撃国家・防衛国家・対象コアを固定したSiege状態を永続化し、敵補強またはコアへの帰属可能な破壊試行による5分初動ロック、実ダメージによる30分ローリング移行・延長、無効終了後の国家ペア単位60分クールダウンを実装した。タイマーはサーバー開放tickだけで進み、ローリング中は対象コアの自然回復を停止する。対象コアの撤去・半径変更と当事国からの自主脱退を禁止し、国家ハブの専用Siegeタブで攻守、相手国、残り時間、対象座標、コアHPを表示する。コアHPが0になると30分の陥落後タイマーへ移行し、10分ごとに外周2チャンク、中心以外、全域の順で補強効果を停止する。補強データと保存HPは消さず、ワールドオーバーレイとJadeでSiege停止状態を赤く区別する。防衛国だけがコア周囲を確保すると反攻進捗が増加し、攻撃国との競合中は停止、不在時は半速で減少する。確保完了時は設定可能なHP割合でコアを再起動する。タイマー満了時は、首都なら国家を残したまま中心1チャンクへ縮小して再建状態へ戻し、拠点なら攻撃国へ占領移管する。他国領との重複を避けるため占領半径は自動縮小し、中心チャンクすら競合する場合は中立停止とする。建築物、コンテナ、アイテムは削除しない。首都は防衛国単位、占領拠点はコア単位で設定可能な再攻撃猶予を永続化し、サーバー開放tickだけで進める。最後の国民のログアウトから設定可能な20分経過後は補強・コア被ダメージを1/3にし、小数相当の端数を命中ごとに蓄積して小ダメージにも実質3倍耐久を適用する。Siege開始時点ですでにオフライン防御が有効ならローリング中も防御を維持し、オンライン中に始まったSiegeからの途中ログアウトでは有効化しない。陥落後タイマー中は常に無効化する。最終オンラインから7・14・21・30日の実時間境界で防御性能を75・50・25・0%へ落とし、7日以降は短期オフライン防御を重ねず、復帰時は通常強度へ即時復旧する。金庫は自国の稼働領土内かつ首都以外の1チャンクを国家ハブから指定し、Siege中の変更禁止、変更確認、サーバー開放時間で進む再変更待機、完全陥落後の留保、領土プレビュー上の明緑マーカーを実装した。講和は当事国間の期限付き提案、提案国から相手国へのLightman's Currency賠償移動、双方の捕虜返還、双方のSiege終了、コアの最低回復、国家ペア単位の再攻撃禁止を一取引として処理し、提案・承認・拒否・取下げと停戦残り時間をSiege GUIへ統合した。捕虜はSiege当事国のプレイヤーがダウン中の敵へ空手でスニーク右クリックして確保し、国家ID・収容国・確保者を永続化して収容国の金庫チャンク、未設定時は首都チャンクへ移送する。拘束中は収容チャンク外への離脱、建築、破壊、攻撃を禁止し、当事国の捕虜一覧をSiege GUIへ表示する。講和承認前に全捕虜の所属と帰還先を検証し、不整合時は金貨決済前に拒否する。オンライン捕虜は成立時に即時帰還し、オフライン捕虜は次回ログイン時の帰還を永続予約する。攻撃側の撤退、防衛側の降伏には確認モーダルを要求し、陥落前後で対応するSiege終了・即時陥落確定処理を行う。停戦中は通常採掘・爆発・CBC公式イベント経路の補強およびコア攻撃を遮断する。
5. 外交、Siege、講和、検知設定、通知・Discord設定を同じ画面体系へ追加する。
6. 管理GUIを追加し、通常プレイヤー向け詳細コマンドを補助経路へ降格する。

初期リリースの受け入れ基準は次のとおりとする。

- 新規プレイヤーがコマンド一覧を読まずに、GUIだけで国家作成または招待承認、コア設定、領土プレビュー、補強、メンバー確認まで到達できる。
- 溶接機を持ったプレイヤーが、照準中ブロックの補強有無と耐久状態を1秒以内に視認できる。
- 閉鎖不成立時に、GUIから診断モードを開始し、未補強箇所または外部到達経路の原因候補をワールド上で識別できる。
- 補強済み、未補強、損傷、無効、施工不可を、色設定をモノクロ相当にしてもパターンまたは記号で区別できる。
- 権限のないプレイヤーへ敵国の詳細補強データが送信されず、オーバーレイを敵拠点の透視に利用できない。
- 主要画面が `/pvp` と同じ配色、カード、ホバー、選択、無効、閉じる操作を持ち、通常操作でバニラのチェスト・看板・金床GUIを開かない。
- 主要操作にサーバー側の権限再検証、二重送信防止、成功・失敗フィードバックがある。
- 設定した最大表示範囲内でオーバーレイがフレームごとに全領土走査を行わず、描画上限超過時にも問題箇所を優先表示する。

## 11. 通知システムとDiscord Bot連携

### 11.1 基本構成

- Minecraftサーバー側は、Siegeや国家に関する出来事を共通の通知イベントとして発行する。
- MoveEarth Mod jarへJDAを同梱し、Minecraft専用サーバープロセス内でDiscord Botを稼働させる。外部の連携バックエンドは必須構成にしない。
- サーバー運営者はサーバー専用configでBot機能の有効・無効とBotトークンを設定し、各利用者はそのBotを自分が管理するDiscordサーバーへ招待して利用する。
- 1つのMinecraftサーバー内Botが複数のDiscordサーバーを安全に分離して扱える構成とする。複数Minecraftサーバーを1つのBotプロセスへ集約することは初期仕様に含めない。
- JDAはDiscord GatewayとREST APIへ外向きTLS通信を行い、Discord側からMinecraftサーバーへの直接接続やポート開放を要求しない。
- Botは連携済みのDiscordサーバーにある、設定された特定チャンネルへ通知を投稿する。
- ゲーム内通知とDiscord通知は同じ通知イベントを元に生成し、内容や発生条件の食い違いを防ぐ。
- JDAのログイン失敗、切断、レート制限、Discord API障害があっても、Minecraftサーバー本体とSiege処理は継続できる構成にする。

配布物は原則1つのMod jarとするが、Minecraftの通知生成、永続アウトボックス、Discord連携状態、JDA配送処理は内部サービスとして分離する。JDAと推移依存ライブラリはjarへ同梱し、競合する場合はrelocateする。JDAはDedicated Server側だけで初期化し、クライアントではクラスのロードや接続を行わない。

Botトークンはサーバー専用configだけから読み込み、クライアント同期、SavedData、パケット、GUI、ログ、クラッシュ情報へ含めない。トークン未設定・不正時はDiscord機能だけを無効化して警告し、Mod全体の起動失敗にはしない。サーバー停止時はJDAと配送ワーカーを正常終了する。

Bot設定は接続クライアントへ同期される可能性があるNeoForge共通設定から分離し、Dedicated Serverローカルだけで読む `config/moveearth_addtional-discord.toml` 相当の非同期ファイルとする。初期項目は次のとおりとする。

| 設定 | 初期値 | 用途 |
| --- | --- | --- |
| `enabled` | `false` | JDA Botの起動可否 |
| `botToken` | 空 | Discord Developer Portalで発行したBotトークン |
| `linkCodeExpirySeconds` | `600` | 一度限りの確認コードの有効期間 |
| `deliveryIntervalTicks` | `20` | 永続アウトボックスを確認する間隔 |
| `deliveryBatchSize` | `25` | 1回に配送する最大イベント数 |

- `botToken` が空ならJDAを初期化しない。設定値や例外へトークンを連結せず、ログには設定済みか否かだけを出す。
- 設定ファイルは可能な環境ではサーバー実行ユーザーだけが読める権限を推奨し、Git、バックアップ公開物、クライアントModpackへ含めない。
- 初期実装ではスラッシュコマンドとボタン操作に必要な最小限のGateway Intentだけを有効化し、`MESSAGE_CONTENT` や全Guild Member取得などの特権Intentは要求しない。

### 11.2 MinecraftサーバーとDiscordサーバーの連携

Discord上の操作だけで第三者のMinecraftサーバーを勝手に登録できないよう、Discordとゲーム内の両方で確認するペアリング方式とする。

初回連携の基本フローは次のとおり。

```text
Discord管理者がBotを自分のDiscordサーバーへ招待
  ↓
Discordの通知先チャンネルで /moveearth nation link を実行
  ↓
BotがDiscordサーバー、実行者、通知先に紐づく一度限りの確認コードを発行
  ↓
Minecraftサーバー内で権限を持つプレイヤーがS2 GUIのDiscord連携画面へ確認コードを入力
  ↓
Mod内のJDA連携サービスがDiscordサーバー、チャンネル、Botの送信権限を再確認して連携を確定
```

- Discord側のサーバー連携操作には `Manage Guild` 相当の管理権限を要求する。
- ゲーム内の国家連携操作には、その国家の `MANAGE_NOTIFICATIONS` 権限を要求する。
- 確認コードは短時間で失効し、1回だけ使用できるものとする。初期案は10分とする。
- 確認コードそのものを恒久的な認証情報として使用しない。
- 確認コードはメモリ上の短期データとして管理し、平文をSavedDataやログへ恒久保存しない。再起動した場合は未確定コードを失効させる。
- Discordサーバー連携だけでは国家通知を受信する権限を与えない。国家通知には別途、ゲーム内での国家連携承認を必要とする。
- DiscordサーバーID、承認済み国家ID、通知チャンネルID、ロールID、通知設定をMinecraft側SavedDataへ保存し、Discordクライアントから送られたIDだけを信用せず、操作時にJDAからGuild、権限、チャンネル、ロールの存在を再確認する。
- 1つのDiscordサーバーが通知対象として連携できる国家は、初期仕様では1国家だけとする。別国家へ変更する場合は現在の国家連携を解除し、新しい国家側で再承認する。
- DiscordからBotが削除された場合、またはゲーム内・Discordのどちらかで解除された場合は、その連携を無効化する。

### 11.3 プレイヤー本人確認

国家別設定と個人メンションには、MinecraftアカウントとDiscordアカウントの本人確認済み対応を使用する。

```text
Discordで /moveearth account link を実行
  ↓
BotがDiscordユーザーに一度限りの確認コードを表示
  ↓
本人がMinecraftへログインし、S2 GUIの本人確認欄へ確認コードを入力
  ↓
実行したMinecraft UUIDとDiscordユーザーIDを連携
```

- コードをゲーム内で実行したログイン中のUUIDだけを連携対象とし、プレイヤー名の手入力では本人確認しない。
- 確認コードは実行者本人だけにDiscordの非公開応答で表示する。
- 1つのDiscordアカウントとMinecraft UUIDは一対一とし、どちらかが別アカウントへ連携済みなら新しい連携を拒否する。
- プレイヤーはDiscordまたはゲーム内から自分の連携を解除できる。
- 国家のDiscord設定を変更するには、本人確認済みMinecraft UUIDがゲーム内でその国家の通知管理権限を持っていることをMod内の連携サービスで確認する。
- Minecraft側で国主交代、脱退、追放、権限変更が発生した場合、以降のDiscordスラッシュコマンド権限へ反映する。
- Discordサーバーを国家へ連携する際は、Discord側のサーバー管理権限と、本人確認済みMinecraft UUIDが持つ国家通知管理権限の両方を要求する。
- 国家連携後、そのDiscordサーバーが受信できるのは、連携した自国家が攻撃側、防衛側、旧所有国または占領当事国となるイベントだけとする。
- 他国家だけで発生したSiege、コアHP、領土、外交イベントを購読または検索する機能は提供しない。

### 11.4 Discord側のスラッシュコマンドと設定

ここで扱うスラッシュコマンドはDiscord側の操作であり、Minecraft内の通常操作をコマンド中心にするものではない。コマンド名は仮称とし、最低限次の入口を提供する。初回コマンド実行後の設定変更は、可能な範囲でDiscordのボタン、セレクトメニュー、モーダルを使い、長いサブコマンド入力を減らす。

| コマンド例 | 用途 | 必要な確認・権限 |
| --- | --- | --- |
| `/moveearth server link` | Minecraftサーバーとの新規連携を開始 | Discordサーバー管理権限＋ゲーム内管理者確認 |
| `/moveearth status` | Bot状態と対象Minecraftサーバーを確認 | Discordサーバー管理権限 |
| `/moveearth server unlink` | サーバー連携を解除 | Discordサーバー管理権限。ゲーム内へも解除通知 |
| `/moveearth account link` | DiscordアカウントとMinecraft UUIDの本人確認を開始 | ゲーム内でのコード入力 |
| `/moveearth nation link` | Discordサーバーを自国家へ連携 | Discordサーバー管理権限＋本人確認＋国家通知管理権限 |
| `/moveearth nation unlink` | 現在の国家連携を解除 | Discordサーバー管理権限＋国家通知管理権限 |
| `/moveearth nation channel` | 国家別通知チャンネルを設定 | 本人確認＋国家通知管理権限 |
| `/moveearth nation mention` | 国家へ通知するDiscordロールを設定 | 本人確認＋国家通知管理権限＋Discord側のロール管理権限 |
| `/moveearth notifications` | 通知種別、メンション、座標表示を設定 | 設定対象に応じた管理権限 |
| `/moveearth test` | 設定済みチャンネルへテスト通知 | Discordサーバー管理権限 |

最低限、次の項目をDiscordのスラッシュコマンドまたは対話コンポーネントから設定可能にする。

- このBotインスタンスが動作しているMinecraftサーバーとの連携状態
- 本人確認とゲーム内承認を終えた自国家ID
- 自国家の通知チャンネルID
- 自国家通知で使用するDiscordロールID
- 通知種別ごとのメンション有効・無効
- 自国家のDiscordサーバー内で `@everyone` または指定ロールを使用するか
- ゲーム内全体通知の有効・無効
- 通知へディメンション名や座標を含めるか

メンションは自由入力文字列ではなく、設定済みのユーザーIDまたはロールIDから生成する。Discordへの送信時は許可したメンションだけを `allowed_mentions` 相当の制御へ渡し、国家名やプレイヤー入力から意図しないメンションが発生しないようにする。

#### Discordメッセージの表示規約

- BotがDiscordへ出すメッセージは、通知、連携コード、状態表示、設定結果、成功、警告、エラー、監査向け応答を含め、原則としてすべてEmbedを使用する。
- 通常の本文だけのメッセージは、Discord API障害時の最小フォールバック、Embedを利用できないDiscord仕様上の応答、またはEmbed生成自体に失敗した場合だけに限定する。
- EmbedはMoveEarth共通デザインとし、タイトル、短い概要、必要なフィールド、発生時刻、イベントID、Minecraftサーバー名を基本要素とする。情報が存在しないフィールドは空欄で表示せず省略する。
- 色は成功・正常を緑、情報を青緑、注意を黄、Siege・敵対行為・エラーを赤、無効・解除を灰色として統一する。国家固有色を将来導入する場合も、警告レベルを判別できなくする配色にはしない。
- Siege通知は攻撃国、防衛国、段階、対象コア、残り時間をフィールドで分離し、座標表示が有効な場合だけディメンションと座標を含める。
- 連携コードなど本人だけに見せる情報は非公開応答のEmbedで表示する。確認コードを公開チャンネルのEmbedへ投稿しない。
- メンションはEmbed本文へ埋め込まず、送信メッセージ側の許可済みmention指定として付与する。自由入力値からmention構文を生成しない。
- 国家名、プレイヤー名、役職名などはMarkdownとリンク表示を無害化してからEmbedへ入れ、フィールド長、本文長、フィールド数はDiscord上限より余裕を持って制限する。
- 再試行で同じ通知を再送しても表示内容とイベントIDが変わらないよう、Embed生成は永続通知イベントから決定的に行う。

### 11.5 Siege通知方針

本格的なSiegeの節目はMinecraftサーバー内では全体通知する。Discordでは、自国家がそのSiegeの当事国である連携先にだけ通知し、無関係な国家のDiscordサーバーへは送信しない。破壊試行のたびに通知するとスパムになるため、状態の節目だけを通知する。

| 出来事 | ゲーム内通知 | Discord通知 | メンション |
| --- | --- | --- | --- |
| 5分間の初動ロック開始 | 攻撃・防衛国家 | 送信しない | なし |
| 最初の有効な攻城行為でローリングSiegeへ移行 | サーバー全体 | 当事国として連携した各Discordサーバーだけ | 受信側の自国家ロール。`@everyone`は設定次第 |
| 有効な攻城行為による30分タイマー更新 | 原則なし | 原則なし | なし |
| コアHPの警戒閾値到達 | 攻撃・防衛国家 | 当事国として連携した各Discordサーバーだけ | 受信側の自国家ロール |
| コア陥落 | サーバー全体 | 当事国として連携した各Discordサーバーだけ | 受信側の自国家ロール。`@everyone`は設定次第 |
| 反攻戦開始・成功・失敗 | サーバー全体 | 当事国として連携した各Discordサーバーだけ | 受信側の自国家ロール |
| Siege終了 | サーバー全体 | 当事国として連携した各Discordサーバーだけ | 原則なし |
| 領土喪失または占領確定 | サーバー全体 | 当事国として連携した各Discordサーバーだけ | 受信側の自国家ロール。`@everyone`は設定次第 |

初動ロックは壁を一度殴るだけでも発生するため、Minecraftの全体通知対象とDiscord通知の対象にはしない。ゲーム内の攻撃・防衛国家への警告と監査ログだけを残す。実ダメージが発生してローリングSiegeへ移行した時点を、本格的な包囲戦のゲーム内全体通知および当事国Discord通知の開始タイミングとする。

### 11.6 通知の信頼性と制限

- Minecraftのtick処理からJDAのREST完了を直接待たず、永続アウトボックスへ積んで専用配送ワーカーから非同期送信する。
- DiscordイベントリスナーやJDAコールバックからワールド、SavedData、プレイヤーを直接変更せず、必ずMinecraftサーバースレッドへ処理を渡す。
- 通知先の抽出はDiscordクライアントから指定された宛先を信用せず、Minecraft側SavedDataに保存したDiscordサーバーID、承認済み国家ID、通知チャンネルIDから行う。
- 通知イベントの当事国家IDとDiscordサーバーに紐づく承認済み国家IDが一致しない場合は、送信キューへ追加しない。
- 同じSiegeと通知種別の組み合わせに一意なイベントIDを付け、再試行時の重複投稿を防ぐ。
- Discord送信失敗時は回数と間隔に上限を設けて再試行する。
- Bot無効時、切断中、レート制限中の通知をどこまで永続キューへ保持するかを設定可能にする。
- タイマー更新やHP変化の通知にはクールダウンと閾値を設ける。
- 国家名、プレイヤー名、自由入力文はDiscord向けにエスケープし、メンションやMarkdownの悪用を防ぐ。
- メンションはJDAのID指定APIと許可済みロールだけから生成し、国家名などの表示文字列からメンションを解釈させない。
- 通知送信の成否、連携、解除、設定変更は管理ログへ残すが、Botトークンや確認コードは出力しない。

#### 現在の実装境界

- Minecraft Mod側には国家単位のゲーム内通知、Discord配送有効化、座標添付、Siegeメンション設定を扱うS2テーマGUIと `MANAGE_NOTIFICATIONS` 権限を実装済み。
- Discord配送は国家ID、通知種別、ディメンション、任意座標、制限済み引数を持つ永続アウトボックスへ積み、最大4,096件、最大8回、指数バックオフ付き再試行とする。未連携またはDiscord通知OFFの国家はキューへ積まない。標準72時間の保持期限と、国家・種別・位置から作るイベント指紋による標準15分の重複抑止を備え、配送成功・失敗・期限切れ・再試行上限を永続統計と監査ログへ記録する。
- 初動ロックは関係国家へのゲーム内通知だけとし、ローリングSiege開始から当事国家別の外部配送イベントを生成する。
- JDA 6.6.0と必要な推移依存関係のjar-in-jar同梱、Dedicated Server限定の非同期起動・停止、特権Intentを使わないスラッシュコマンド、Embed共通表示、メモリ上の一度限りコード、ゲーム内GUIでの国家承認、通知チャンネル検証、非同期アウトボックス配送を実装済み。JDA以外の推移依存パッケージはMoveEarth専用名前空間へrelocateし、Kotlin、Jackson、Gson、Error Prone等を他ModやNeoForgeと二重公開しない。ビルド時に隔離漏れと主要クラスのロードを自動検査する。
- Bot設定はNeoForgeのクライアント同期対象外であるSTARTUP configとして登録し、Botの起動時にはDedicated Server上の `moveearth_addtional-discord.toml` だけを参照する。トークンはSavedData、パケット、GUI、通常ログへ渡さない。
- Discord側の本人確認、解除、通知チャンネル・ロール・通知設定、テスト通知、状態・監査表示をスラッシュコマンドで実装済み。Discord管理権限と、本人確認済みMinecraft UUIDが現在持つ国家通知管理権限を操作ごとに再検証する。Bot・通知チャンネル・役職の削除時はリンクまたは役職設定を自動無効化する。
- コアHP警告・陥落、反攻開始・成功・失敗、Siege終了、領土喪失・占領確定、漏出・再封鎖、維持費警告を共通通知イベントへ接続済み。
- 既存アウトボックスの取得・ACK・失敗処理はJDA配送アダプターからだけ利用し、Discordイベント処理とMinecraft状態変更のスレッド境界を維持する。
- スラッシュコマンドによる連携、解除、チャンネル変更、メンション変更は監査ログへ記録する。

## 12. 悪用防止と境界条件

実装前に少なくとも次の挙動を確定する。

- Siege直前およびSiege中のログアウト
- Siege中の国主移譲と、国家に国主がいなくなる操作
- Siege中の維持費未払い
- 長期不在国家へSiegeを開始する必要があるか
- 国主が長期間不在の場合の継承
- 国家の最後の1人が脱退する場合の処理
- 複数国家への同時所属を許可するか
- 流体、クリーパー、ウィザー、他Modの採掘機械などによる閉鎖判定・補強回避
- 飛行船、車両、Createの移動体による着陸・上陸・空挺・砲撃とBastion制限の詳細。初期実装では移動自体を禁止せず、敵領土内での降板だけを暫定禁止する
- チャンク境界、ディメンション移動、再起動を利用した判定回避

## 13. 現時点での確定事項

- 国家をメンバー、権限、外交、領土の中心概念とする。
- 国家の所属メンバーを可視化する。
- 役職ベースの権限管理を導入する。
- 同盟、敵対の概念を導入する。
- プレイヤー検知ブロックのホワイトリストを国家システムへ統合する。
- 領土は領土コアで宣言する。
- 国家が最初に設置・稼働させた領土コアを首都コアとする。
- 1国家につき首都コアは1個とし、それ以降に設置する別拠点用の拠点コアは複数設置可能にする。
- 国家の完全陥落は首都コアを基準とし、拠点コアを隠すことで敗北を回避できないようにする。
- 首都コアの移転処理は保留とし、初期実装には含めない。
- 領土半径の上限は4チャンクとする。
- 領土形状は正方形とする。
- 領土のチャンク数に応じて維持費を変動させる。
- コアは補強された閉鎖空間内に設置し、Flood Fillによる閉鎖判定に合格しなければ稼働できない。
- 閉鎖判定では補強済みのドア・ハッチを開閉状態にかかわらず外殻として扱い、未補強の場合だけ通過可能として扱う。2ブロックドアは上下両方の補強を必要とする。
- 閉鎖判定の探索可能内部空間は初期値32,768ブロック、探索境界はコアから各軸32ブロックとする。
- 閉鎖状態の再判定待ちではコアを暫定露出として扱い、攻撃可能にする。再判定は変更をまとめる5秒のデバウンス後に開始し、探索状態を保持したセル予算制で複数tickへ分散する。
- 領土内では、権限のない外部プレイヤーによるブロック・流体・侵入補助物の設置と、領土内へのエンダーパール移動を禁止する。
- 飛行船、車両、Createの移動体は初期Bastion実装による一律の移動禁止対象にしないが、敵対国家の領土内での降板・下船を暫定禁止する。
- 領土コアはHPを持つ。
- 領土コアは固定最大HPとし、外殻補強の数や残存率をコアHPへ二重加算しない。
- 領土コアのHPは時間経過によって回復する。
- 溶接機とオフハンド素材によってブロックを補強する。
- 溶接機の照準HUDと補強施工モードにより、補強済み、未補強、損傷、無効、施工不可をワールド上で区別可能にする。
- 領土コアGUIから閉鎖診断モードを開始でき、未補強箇所や外部到達経路の原因候補をオーバーレイ表示する。
- 詳細な補強オーバーレイは自国領土の権限保持者だけへ提供し、敵国拠点の透視には利用できないようにする。
- 補強素材は丸石、銅、鉄、金、ダイヤとする。
- 補強耐久値の初期値は順に32、64、128、192、320とする。
- 通常TNTでは補強された壁を原則破壊できない。
- 手掘りでの突破には数十回から数百回の採掘を必要とする。
- CBCなどの攻城兵器を基本的な攻略手段とする。
- 戦争はSiegeとして扱う。
- 敵国領土の補強またはコアに対する破壊試行を検知した時点で、手段や実ダメージの有無を問わずSiegeタイマーを開始する。
- 最初の破壊試行による初動ロックは5分とする。
- 初動ロックは追加の破壊試行で延長せず、有効攻城行為なしで終了した場合は同じ国家ペアから60分間再発動できない。
- 初動ロック再発動クールダウン中でも、実ダメージが発生すればローリングSiegeを即座に開始する。
- 初動ロック後は、補強耐久値またはコアHPへ実ダメージを与える有効な攻城行為だけがSiegeタイマーを30分へ更新する。
- ダメージのない破壊試行だけではSiegeタイマーを延長できない。
- 有効な攻城行為が続く限り、Siegeの継続時間に固定上限を設けない。
- 初動ロック、再発動クールダウン、ローリングSiege、陥落後タイマー、反攻戦の確保時間、コア回復はサーバー開放中だけ進み、閉鎖中は残り時間と進捗を保持して停止する。
- ローリングSiege中はオフライン3倍防御を無効化する。
- ローリングSiege中は領土コアの時間回復を停止し、Siege終了後に回復待機時間を最初から数える。
- Siegeタイマーは、包囲戦が継続中かどうかを示す状態判定として使用する。
- Siegeタイマー中は攻撃側、防衛側ともに国家の解散と脱退を禁止する。
- Siege中は対象コアの撤去、対象領土の縮小・放棄・譲渡、首都指定変更を禁止する。加入、追放、役職・外交関係の変更は許可するが、既存Siegeの当事国判定へ影響させない。
- コア陥落後も別タイマーを走らせ、補強を外周から段階的に無効化する。
- 陥落後タイマー中もオフライン3倍防御とコア回復を無効化する。
- 陥落後タイマー中に旧所有国が領土を奪還できる反攻戦を設ける方向とする。
- 反攻戦の確保進捗は旧所有国だけが範囲内にいる場合に増加し、競合中は停止、不在時は徐々に減少する。陥落後タイマーは当事者不在でもサーバー開放中なら進行する。
- 反攻戦の確保判定は旧所有国と攻撃国だけを対象とし、同盟国・中立国・第三国の存在は進捗へ影響させない。
- 双方合意による講和を設け、最低限、捕虜返還、金貨賠償、Siege終了、再攻撃禁止期間を扱う。
- 拠点コアの陥落は対象拠点の領土だけに作用し、国家全体の完全陥落にはしない。
- 完全陥落しても国家自体は消滅させず、首都チャンクと1つの金庫チャンクを割譲不可の再建領域として残す。
- 完全陥落後および反攻戦失敗後は再攻撃クールダウンを設ける。
- オフライン襲撃は禁止しない。
- 短期的なオフライン時は実質約3倍の耐久力を持たせる。
- 1週間以上国家メンバーのオンラインがない場合、補強を段階的に弱体化する。
- 1か月間オンラインがない場合、補強効果を完全に失わせる。
- 国家、メンバー、役職、領土、補強、外交、Siege、検知、通知の通常操作は国家ハブGUIを主経路とする。
- S2 GUIは既存の `/pvp` 系画面と同じ暗色パネル、カード、タブ、アクセント色、ホバー、選択、無効状態、スクロールのデザインを採用する。
- 通常のバニラボタンやコンテナ画面の流用を減らし、S2テーマの共通GUI部品を `GuiGraphics` ベースで実装する。
- Minecraft内の詳細コマンドは管理、復旧、デバッグ、自動化用の補助経路に限定し、プレイヤー向け主要操作はGUIだけで完結可能にする。
- MoveEarth Mod jarへJDA Botを同梱し、サーバー専用configで有効化とBotトークンを設定できるようにする。
- 1つのMinecraftサーバー内Botは複数のDiscordサーバーを国家単位で分離して扱う。
- Discordの管理権限とゲーム内の管理権限を両方確認する、一度限りのコードによるサーバーペアリングを行う。
- Minecraft UUIDとDiscordユーザーIDを、ゲーム内でのコード入力によって本人確認する。
- Discord側の通知設定はスラッシュコマンドまたはDiscordの対話コンポーネントから変更でき、Minecraft側の通知・連携設定はS2 GUIから変更できるようにする。
- Discord Botが出す通知、設定結果、状態、成功、警告、エラーは原則すべてMoveEarth共通デザインのEmbedとする。
- Discordサーバーは、ゲーム内で承認された1つの自国家にだけ通知先として連携できる。
- Discordへ送信するのは、連携した自国家が当事国となる通知だけとし、他国家だけのイベントは送信しない。
- Discordの自国家通知チャンネルと自国家用メンションを設定可能にする。
- ローリングSiege開始、コア陥落、反攻戦、Siege終了、領土確定などの節目はMinecraftサーバー内で全体通知する。
- 5分間の初動ロックはゲーム内の関係国家だけへ通知し、Discordへは送信しない。
- 有効な攻城行為によってローリングSiegeへ移行した時点で、当事国家へ連携したDiscordサーバーへ初めてロールメンションを送信する。
- タイマー更新ごとの全体通知は行わない。

## 14. 今後決定する主要項目

1. 実装済みの維持費式、24/72時間ペナルティ境界、弱体化倍率のプレイテスト後の最終値
2. 実装済みのCBC弾種別ダメージ値と、CBCアドオン弾を補助弾扱いにする場合の最終調整
3. 補強の修復速度、修復コスト、特殊性能
4. 実装済みの首都2,000／拠点1,000 HP、5分待機・毎分1%回復のプレイテスト後の最終値
5. 閉鎖判定の探索上限、再判定頻度、利用可能な補強ブロック形状
6. 将来的に首都コア移転を導入するか、および導入時の条件、費用、待機時間
7. 拠点コアの最大数、設置条件、追加基礎維持費
8. 反攻戦の必要確保時間、確保範囲、将来の同盟国・第三国による正式参戦、外周領土の占領処理
9. Siegeおよび陥落後タイマーの最終値、勝敗条件、再攻撃クールダウン
10. Siege中に制限する国家操作の範囲
11. 実装済みの捕虜収容・講和返還ルール、講和賠償上限・再攻撃禁止時間のプレイテスト後の最終値
12. 実装済みの金庫再変更待機時間と、完全陥落後に留保される金庫周辺のゲームバランス
13. 完全陥落時に残存する拠点コアと拠点領土の保護状態
14. 間接攻撃を実行国家へ帰属させる技術的な判定範囲
15. オフライン判定の猶予時間とダメージ倍率
16. 長期不在時の正確な弱体化曲線と復帰時の扱い
17. 同盟によるBastion通行以外で、外交関係が建築、補強、検知、戦闘へ与える影響
18. 飛行船、車両、Createの移動体を用いた着陸・上陸・空挺・攻城とBastion制限の詳細
19. **実装済み:** JDA 6.6.0、推移依存関係の全面relocate、スラッシュコマンドに必要な非特権Intentだけを使用する構成。JDA更新時は隔離検査と実サーバー起動試験を再実施する
20. **実装済み:** Botトークンのローテーション手順と、サーバー設定ファイルの最小権限運用（`DISCORD_BOT_OPERATIONS.md`）
21. **実装済み:** Minecraft UUIDとDiscord IDを一対一とし、既存の別アカウント連携を拒否する重複連携ポリシー
22. **実装済み:** 永続キュー72時間保持、8回再試行、イベント指紋による15分間の重複配送抑止（configで調整可能）
23. 通知チャンネル、メンション、座標公開範囲の初期設定
24. 補強オーバーレイの最終的な表示半径、透明度、描画上限、色プリセット
25. 国家ハブを開く初期キー、ゲームパッド操作、ナレーション、文字倍率などのアクセシビリティ詳細
26. 管理GUIへ収める操作と、コンソール専用コマンドとして残す操作の最終的な境界
