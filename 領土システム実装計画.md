# 新・領土システム実装計画

## 1. 目標

`領土システム構想.md` を、次の二層を持つサーバー権威型システムとして実装する。

- 平時: 領土所有者の工業力から、領土保護ブロックを中心とする影響圏を形成する。
- 戦時: Sable移動体上のレイドブロックが、敵の影響力を局所的かつ一時的に低下させる。

固定Claimや独立した国家台帳は保存しない。保存するのは領土コアとレイド状態などの事実データとし、各地点の支配状態は影響力から導出する。

## 2. 最初に固定する仕様

実装前に以下を決定する。数値は設定ファイルへ出し、コードへ埋め込まない。

1. 保護段階の強度順
   - 構想文の文章と図では、チェスト、PvP、建造物、Sable保護の並びが一意ではない。
   - `ProtectionTier` と各閾値を明示し、中心から外縁へ何が解除されるかを確定する。
2. 距離モデル
   - 構想どおりの3次元球を初期値とする。
   - 地下・高高度への影響を避けたい場合に備え、円柱モデルへ設定で切り替えられる設計にする。
3. 領土所有者とメンバー
   - 領土保護ブロックを設置したプレイヤーのUUIDを、領土グループの安定した識別子として使う。
   - メンバーは `PlayerWhitelistSavedData` にある、その所有者のプレイヤー検知ブロック用ホワイトリストと完全に共通化する。
   - 所有者本人は常にメンバーとして扱い、独立した国家データ、役職、同盟データは持たない。
   - 現在のホワイトリストはプレイヤー名を保存しているため、改名追従は別課題とし、MVPでは既存仕様を維持する。
4. 工業力の扱い
   - 単純なブロック数ではなく、Createの稼働中ネットワークを一定間隔で評価する。
   - 同じネットワークの二重計上、無負荷回転による水増し、毎tick全探索を禁止する。
5. 経済力の扱い
   - 新領土システムでは当面使用しない。
   - プレイヤー検知ブロックに既存のLightman's Currency決済機能は、そのまま独立して残す。

## 3. コア設計

### 3.1 ドメイン

- `TerritoryOwnerId`: 領土保護ブロック所有者のプレイヤーUUID。
- `TerritoryMembership`: 所有者UUIDと既存ホワイトリストから都度導出するビュー。独立保存しない。
- `TerritoryCore`: ディメンション、座標、所有者UUID、有効状態。
- `InfluenceSource`: 基礎値、工業値、レイド補正を統一的に扱う入力。
- `InfluenceResult`: 勝者、最大強度、2位との差、競合状態、保護段階。
- `ProtectionAction`: チェスト操作、PvP、ブロック破壊・設置、爆発、Sable損傷など。
- `RaidEmitter`: Sable UUID、ローカル座標、攻撃側所有者UUID、稼働状態、供給SU、効果量。

### 3.2 影響力計算

初期式は単純かつテスト可能な形にする。

```text
corePower = basePower
          + industrialScore * industrialWeight

influenceAt(position) = max(0, corePower - distance * falloff)
effectiveInfluence = influenceAt(position) - activeEnemyRaidSuppression
```

- 同じ所有者の複数コアは、初期実装では最大値を採用して無制限な加算を防ぐ。
- 各地点で上位2所有者を比較し、差が `contestMargin` 未満なら競合領域とする。
- 保護は距離そのものではなく、競合後の `effectiveInfluence` の閾値で決定する。
- 同率時の設置順優先は行わない。競合扱いにする。

### 3.3 保存とキャッシュ

- 領土コアはワールド単位の `SavedData` に保存する。メンバー情報は既存の `PlayerWhitelistSavedData` を参照する。
- ディメンションごとにチャンク空間インデックスを持ち、近傍コアだけを評価する。
- 工業力は5～10秒程度の設定可能な間隔でスナップショット更新する。
- コア設置・撤去、工業スコア更新、レイド開始・終了時だけ関連キャッシュを無効化する。
- 影響マップ全体や全ブロックの結果は保存しない。

### 3.4 保護判定

イベントハンドラーへ個別ルールを散らさず、`TerritoryPolicyService#authorize` に集約する。

判定順は次とする。

1. 専用サーバーか、対象ディメンションかを確認。
2. 管理者・設定されたバイパス権限を確認。
3. PvPアリーナなど既存システムの専用領域を除外。
4. 地点の影響結果を取得し、実行者が所有者本人またはその所有者のホワイトリスト登録者かを確認。
5. 平時・戦時、保護段階、競合状態から許可または拒否。
6. 拒否理由をクールダウン付きでプレイヤーへ通知。

対象イベントは、ブロック破壊・設置、コンテナ操作、PvP、爆発、TaCZ弾によるブロック損傷、Sable移動体への損傷を含む。

## 4. 外部Mod連携

### Create

- `IndustrialPowerProvider` インターフェースの背後へCreate API依存を閉じ込める。
- 先にAPI調査用スパイクを行い、ネットワークID、供給能力、現在負荷、回転速度から採用指標を決める。
- 領土コア自体をCreate応力入力として使うため、Create 6.0.10以降を必須依存として明示する。

### Sable

- レイドブロックはSable UUIDとサブレベル内ローカル座標で識別する。
- 毎回Sableの姿勢からワールド座標へ変換し、移動前の固定座標を保存しない。
- ブロック消失、移動体削除、動力停止、攻撃側所有者の変更のいずれでもレイドを即時停止する。
- 既存の `SableAirshipController` と通常レイドは壊さず、領土レイド用アダプターを分離する。

## 5. 実装フェーズと完了条件

### Phase 0: 仕様固定とテスト土台

- 保護段階、距離モデル、競合時ルール、初期設定値を確定する。
- 影響力計算をMinecraft非依存の純粋Javaとしてテストできる構造にする。
- 完了条件: 中心、外縁、同率競合、複数コア、レイド減衰の期待値テストが定義済み。

### Phase 1: 所有者・領土コアMVP

- 領土保護ブロック、BlockEntity、SavedData、管理コマンドを追加する。
- 設置者UUIDとの紐付け、撤去、再起動後の復元を実装する。
- 所有者の既存ホワイトリストを領土メンバー判定に使う `TerritoryMembershipService` を追加する。
- 完了条件: 専用サーバー再起動後もコアが復元され、検知ブロックのホワイトリスト変更が領土権限にも反映される。

### Phase 2: 影響力エンジン

- 距離減衰、所有者間競合、保護段階、チャンクインデックス、キャッシュを実装する。
- 最初は設定された固定工業値で動作確認する。
- 完了条件: 2所有者・複数コアで境界が決定し、値の変更で境界が再計算される。

### Phase 3: 平時の保護

- チェスト、PvP、建築、爆発、TaCZ、Sableの保護判定を段階的に接続する。
- 所有者、既存ホワイトリストのメンバー、管理者、競合地、既存PvPアリーナの例外を実装する。
- 完了条件: 各段階の内側・境界・外側で許可表どおりに動作する。

### Phase 4: 工業力連携

- Create API調査結果に基づく工業スコアを接続する。
- 異常値の上限、更新間隔、キャッシュ無効化を設定化する。
- 完了条件: 外部Modの値の変化が設定時間内に境界へ反映され、Createの不足時は依存関係エラーを明示する。

実装済みの初期仕様では、Create本体と導入済みアドオンの実値を読み、
ネットワークの使用応力を発電容量比で按分する。無負荷・過負荷・クリエイティブ動力は領土工業力へ加算しない。
領土コアへ軸を直接接続した場合は、コア自身が消費した応力も所有者の工業力として加算する。

### Phase 5: 可視化と運用機能

- 管理コマンドで地点の勝者、強度内訳、2位との差、保護段階を表示する。
- プレイヤー向けに領土侵入・退出、競合地、保護段階の表示を追加する。
- 必要なら後から境界レンダーまたはマップ連携を追加する。
- 完了条件: 保護拒否の理由と影響力の内訳を管理者が追跡できる。

初期可視化として、`F7`で切り替える地表グリッドオーバーレイを実装する。表示中だけサーバーが周辺128
ブロックを16ブロック間隔で評価し、味方を緑、他領土を赤、競合を黄橙で表示する。色の濃さは影響度、
HUDは現在地点の影響度と保護段階を示す。所有者UUIDやコア一覧そのものはクライアントへ送らない。

Jade導入時は領土コアを注視すると、所有者、稼働状態、直結・周辺Create応力、工業スコア、先頭・次点の
影響度、競合状態、保護段階を表示する。判定値はJadeのサーバーデータProviderで生成し、クライアント側で
領土計算を再現しない。Jade自体は任意依存とし、未導入環境でも本Modの領土機能は動作する。
領土コア以外の一般ブロックでは、注視地点の先頭所有者、自分との関係、先頭・次点影響度、保護段階を
簡易表示する。領土コアでは一般表示を重ねず、コア専用の詳細表示を優先する。

### Phase 6: 移動式レイド

- レイドブロック、Sable所属検出、Create動力条件、局所減衰、終了条件を実装する。
- 複数レイドの重複効果には上限を設ける。
- 完了条件: 移動中も減衰中心が追従し、破壊・動力停止・撤退・再起動時に確実に解除される。

実装では移動式領土レイドコアのBlockEntityからSable所属船を毎回取得し、船の論理Poseでブロック中心を
ワールド座標へ射影する。Createの有効消費応力から局所減衰強度を算出し、通常の影響度評価へランタイム登録する。
右クリック解除、ブロック破壊、動力不足では即時解除し、船・チャンク消失時は40 tickのTTLで失効する。
レジストリは保存せずサーバー停止時にも破棄するため、再起動後はロード済みかつ条件を満たすコアだけが再登録される。
Jadeでは船所属、武装、停止理由、RPM、有効SU、減衰強度と半径を表示し、Ponderでは設置から解除条件までを
ゲーム内で確認できるようにする。

### Phase 7: 調整・移行・負荷試験

- コア数、オンライン人数、同時レイド数の想定上限でプロファイルする。
- 旧領土機能が存在する場合の移行または無効化手順を用意する。
- バランス値を設定ファイルへ分離し、運用ログを整える。
- 完了条件: `gradlew classes`、単体テスト、GameTest、専用サーバーの結合確認を通過する。

## 6. ブランチ戦略

### 長期ブランチ

- `main`: 現行の安定版。直接実装しない。
- `develop/territory-v2`: 新領土システムの統合先。各機能はここから分岐し、ここへ戻す。

### 短期ブランチと順序

1. `feat/territory-domain`
   - Phase 0、所有者・メンバー判定、共通インターフェース、計算テスト。
2. `feat/territory-core`
   - 領土保護ブロック、BlockEntity、SavedData、登録、アセット。
3. `feat/territory-influence`
   - 計算式、競合、空間インデックス、キャッシュ。
4. `feat/territory-protection`
   - 平時のイベント保護と既存PvP機能との調停。
5. `feat/territory-create`
   - Create API調査と工業力アダプター。
6. `feat/territory-visualization`
   - 管理コマンド、HUD、デバッグ表示。
7. `feat/territory-jade`
   - 領土コアのJadeツールチップとサーバー確定データ表示。
8. `feat/territory-raid-sable`
   - Sable移動式レイドとCreate動力条件。
9. `test/territory-hardening`
   - 結合、性能、再起動、異常系、バランス調整。

`territory-domain` を最初に統合し、`territory-core` と `territory-influence` を続ける。その後は `protection`、`create`、`visualization` を並行化できる。`territory-raid-sable` は影響力、Create、Sable座標変換へ依存するため後段に置く。

### マージ規則

- 1ブランチ1責務、レビュー可能な小ささを維持する。
- 分岐元は常に最新の `develop/territory-v2` とする。
- `ModBlocks`、`ModItems`、`ModBlockEntities`、`ModMessages`、翻訳ファイルは衝突点になりやすいため、登録だけのコミットを小さく保つ。
- 各ブランチで `gradlew classes` と関連テストを通してから統合する。
- Phase 7完了後に `develop/territory-v2` から `main` へまとめて統合する。

## 7. MVPの範囲

最初に遊べる版はPhase 1～3までとする。工業力は管理コマンドで設定できる仮スコアを使い、領土の形成・競合・保護が面白いかを先に検証する。Create連携と移動式レイドは、ゲームループが成立した後に接続する。

この順序なら、外部APIの調査やSable固有処理が遅れても領土エンジン自体の開発とバランステストを止めずに進められる。
