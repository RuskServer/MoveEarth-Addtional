# MoveEarth-Addtional 公開初動バグ監査

## 1. 目的と監査範囲

この文書でいう「初動」は、Minecraft サーバープロセスの起動処理ではない。

**サーバー公開直後に、初参加プレイヤーが通常のゲーム進行を開始できるか**を意味する。具体的には、初回ログイン、初期物資、スポーン選択、国家参加申請、建国、予約領土、領土コア、物資保管、再ログインまでの導線と、公開後すぐに悪用・破損し得るネットワークおよび永続化処理を対象とする。

- 監査日: 2026-09-20
- コード基準: コミット `abc65c7` + 公開初動修正作業ツリー
- 未コミットの開発中変更: 監査対象外
- 手法: コードパスの静的確認、既存テスト結果の確認、配布物検証タスクの実行
- この文書には、今回の確認だけでなく直前までの全体コードレビュー結果も統合している

ユーザー指示により、次の2項目は今回の対象から除外する。

- SQLite JDBC のサーバー用 jar への梱包可否
- 開発用 `runServer` のローカル依存 Mod 不足

これらは本書の未完了タスクおよび公開可否判定には含めない。

## 2. 結論

コード上では、次の主要導線は一通り接続されている。

> 初回ログイン → 初期物資 → 初回選択画面 → 荒野または国家への参加 → 建国 → 領土コア設置 → 予約領土内での物資保管

特に、過去に問題になった「初期物資が受け取れない」「初回スポーン探索で固まる」「建国時に領土コアが生成されない」「予約領土では収納を置けない」という種類の問題には、現在のコード上で対策が存在する。

監査で確定したコード上の問題7件は修正済みである。残る公開可否判定は、第7節の実機受入試験と同時参加負荷試験で行う。

| 優先度 | 状態 | 概要 |
|---|---|---|
| P0 | 修正済み | Lightman's Currency 1.21-2.3.0.4g 以降を必須依存として宣言 |
| P1 | 修正済み | 対象 C2S パケットで境界・距離・ロード済み判定後にだけワールドへアクセス |
| P1 | 修正済み | PvP ロードアウト数・文字列長・武器数・アタッチメント数へ上限を追加 |
| P1 | 修正済み | Siege の破損 SavedData をレコード単位で警告してスキップ |
| P2 | 修正済み | TPA の一時状態をサーバー停止イベントで明示クリア |
| P2 | 修正済み | Analytics APIをBearer認証へ限定し、設定ファイルを所有者限定へ変更 |
| P3 | 修正済み | Analytics Web専用Executorを停止時と起動失敗時に期限付き終了 |

## 3. 公開初日の主要導線

### 3.1 初期物資

**静的確認結果: 導線あり**

- `BeginnerKitEvents` がプレイヤーログイン時に配布処理を呼び出す。
- `BeginnerKitService` は配布済みフラグをプレイヤーの永続データへ保存する。
- 対象は原則としてプレイ時間8時間以内で、鉄防具、食料、TaCZ の銃と弾薬を付与する。
- TaCZ の対象銃が見つからない場合でも、防具と食料まで巻き戻す構造ではない。
- 二重配布防止の永続フラグがある。

軽微な残留事項として、失敗メッセージの一部に旧 GunPack 前提の文言が残っている。また、現在の戻り値構造では到達しにくい失敗分岐がある。公開を止める問題ではないが、運用時の誤診を防ぐため整理した方がよい。

### 3.2 初回選択と拘束画面

**静的確認結果: 導線あり**

- `NationOnboardingService` は未選択プレイヤーを一時的に拘束し、選択完了まで移動、ダメージ、破壊、設置、操作、投棄などを制限する。
- 待機状態は永続化され、途中切断後も再ログイン時に再開できる。
- ログアウト時およびサーバー停止時には一時的なメモリ状態を片付ける。
- 国家参加承認待ちでロビーを必要とせず、承認後に国家側スポーン探索へ進む構造になっている。

### 3.3 荒野へのランダムスポーン

**静的確認結果: 事前生成プール方式へ移行済み**

- `RandomSpawnHandler` の候補探索は複数 tick に分割されている。
- 初回ログイン・通常リスポーン時は、永続化された安全地点プールだけを使用する。ログインを契機とした未知チャンクのランダム探索・新規生成は行わない。
- 安全地点には30秒の選択リースと、使用後10分の再利用待ちを設け、同時参加者の重複スポーンを抑制する。
- 荒野候補はワールド境界内、設定半径内、空が見える安全な地表、国家領土外であることを確認する。
- 制限時間超過または候補不足時はバニラスポーンへフォールバックする。
- フォールバック後もオンボーディングを完了させるため、探索失敗だけで永続的な拘束状態にはならない。
- 切断時は探索状態とチャンクチケットを解放し、再ログイン時は永続状態から導線を再開する。
- 通常のチャンク読み込み時にも、安全条件を満たす地点を受動的にプールへ追加する。
- Chunky が事前生成したチャンクも通常のチャンクLoadイベントから追跡するため、Chunky APIへの固定依存なしで安全地点プールが同時に育つ。近接96ブロック以内の重複地点は追加しない。
- 必要地点数は固定値ではなく、Terrainタイルに記録された陸地面積をワールドボーダーとの交差率で補正し、陸地40,000ブロックにつき1地点（64～8,192件）として算出する。現在の約2,013万陸地ブロックを全域利用できる場合は504地点になる。
- 探索可能な最大半径も固定4,000ではなく、Terrainタイルとワールドボーダーが重なる範囲の最遠点から算出する。Terrainがない構成だけ従来の4,000ブロック上限へフォールバックする。
- 専用サーバーが無人でプールが推奨数を下回る場合、生成済みチャンク限定のマッピングを自動開始する。プレイヤーが1人でも接続すると処理とチャンクチケットを解放し、無人化後に保存済み進捗から再開する。
- 自動再点検は6時間間隔で、未生成チャンクは作らない。明示的な `map generate` だけが新規生成を許可する。
- 管理者は `/spawnpool map start [目標数]` で生成済みチャンクだけを事前走査できる。進捗はSavedDataへ保存され、再起動後も再開する。
- 未生成領域まで明示的に準備する場合だけ `/spawnpool map generate [目標数]` を使う。このモードも同時処理は1チャンク、生成間隔は5秒であり、プレイヤーのスポーン探索中は処理を譲る。
- `/spawnpool status`、`map pause`、`map resume`、`map stop`、`prune` で状態確認・運用・領土化した候補の除去ができる。

過去ログにあった「24候補中1候補しか処理できずタイムアウトする」ケースは、ログイン時に未知地点を抽選する設計そのものを廃止して回避した。公開前に必要数を事前マッピングし、重い地形生成 Mod を含む実環境では `generate` 実行中の TPS と1地点あたりの生成時間を測る必要がある。

### 3.4 国家参加と承認

**静的確認結果: 導線あり、実機確認必要**

- 参加申請は承認制として扱われる。
- 承認待ちは再ログイン後も復元される。
- 承認後は国家領土、オンラインメンバー、領土コア周辺などの候補からスポーン先を探索する。
- 適切な候補が得られない場合に、プレイヤーを永久拘束するのではなく選択画面へ戻す経路がある。

オンライン承認、オフライン中の承認、承認直後の切断を含む一連の実機試験は必要である。

### 3.5 建国、領土コア、予約領土

**静的確認結果: 導線あり**

`NationFoundationService` は、建国地点について少なくとも次を検証する。

- プレイヤーとの距離
- 視線
- チャンクのロード状態
- ワールド境界と建築高度
- 足元の支持ブロック
- 液体および配置先エンティティ
- Sable sub-level ではないこと
- 既存領土との競合

検証後に国家を作成し、選択地点へ領土コアを配置・登録する。配置または BlockEntity 登録に失敗した場合は国家作成をロールバックするため、「国家だけ存在してコアがない」というゴースト状態を避ける構造になっている。

建国時のコアは予約領土として半径1チャンクを構成し、本稼働前の建築準備を可能にする。

### 3.6 収納制限と建国直後の詰み防止

**静的確認結果: 導線あり**

- `NationStorageEvents` は収納ブロックの設置と使用を、国家所属および有効な自国領土で制限する。
- `TerritorySavedData.allowsStorage` は通常の有効領土だけでなく、建国直後の初期予約領土も明示的に許可する。
- そのため、領土コアがまだ本稼働前でも、予約領土内で建国準備用の収納を利用できる。
- 国家脱退、追放、解散などで権限を失った場合、すでに開いている制限対象メニューを閉じる追従処理がある。
- サーバー停止時には収納監視の一時状態をクリアする。

領土外に収納を設置できないこと、予約領土内では設置・使用できること、国家解散直後に既存画面が閉じることは実機試験で再確認する。

## 4. 公開前に修正すべき確定問題

### P0-1. Lightman's Currency の依存関係がメタデータで保証されていない（修正済み）

**影響:** サーバー起動、クライアント接続、ネットワーク登録の段階で `NoClassDefFoundError` 等が発生し、遊べない構成を配布し得る。

コードは次のような箇所で Lightman's Currency API 型を直接参照している。

- `PlayerDetectorBlock`
- `PlayerDetectorBlockEntity`
- `C2S_ConfigurePaymentPacket`
- `C2S_NationTreasuryPacket`
- 国家金庫・維持費関連サービス
- クライアント側の金庫および検知器画面

特にパケット型や登録処理は機能を使った時だけ遅延ロードされるとは限らない。一方、`src/main/templates/META-INF/neoforge.mods.toml` では必須依存としてローダーに保証させていない。

**推奨修正:** 現在の設計では Lightman's Currency を必須依存として `neoforge.mods.toml` に宣言し、対応バージョン範囲も固定する。任意依存へ戻すなら、API 型を共通ロードされるクラスのシグネチャから完全に隔離する必要があり、修正範囲が大きい。

**受入条件:**

- Mod 欠落時は NeoForge が依存不足として明確に停止する。
- 対応版を導入したプレイヤー用・サーバー用配布物が両方ロードできる。

**修正:** `neoforge.mods.toml` に `lightmanscurrency` 1.21-2.3.0.4g 以降の必須依存を追加した。最低対応版より古い版や欠落時は、共通クラスのロードへ進む前にNeoForgeの依存エラーとして停止する。完全一致にはせず、互換APIを維持する更新版を許可する。

### P1-1. 任意座標を使う C2S パケットがチャンクをロードし得る（修正済み）

**影響:** 改変クライアントから遠距離座標を大量送信されると、サーバーが不要なチャンクロードを行い、公開直後のラグ・メモリ圧迫・ウォッチドッグ停止につながり得る。

対象:

- `C2S_ConfigurePaymentPacket`
- `C2S_SetTerritoryCoreRadiusPacket`

現在は受信した `BlockPos` に対して BlockEntity や BlockState を取得してから、所有権などを検証する経路がある。座標はクライアント入力なので、通常 GUI から送られることを信頼してはいけない。

**推奨修正:** ワールドアクセス前に、ディメンション、ワールド境界、プレイヤーとの最大距離、`hasChunkAt`、対象機能の権限を検証する。未ロードチャンクはロードせず拒否する。

**受入条件:** 遠距離・未ロード座標を連続送信しても新しいチャンクがロードされず、サーバー tick に大きな遅延が出ない。

**修正:** 共通の `ServerPacketGuards` を追加し、建築高度、ワールド境界、8ブロック以内、`hasChunkAt` をBlockEntity取得より先に検査する。未ロード座標ではワールド参照へ進まない。

### P1-2. PvP ロードアウトの可変長デコードに上限がない（修正済み）

**影響:** 異常に大きな件数を宣言したパケットによって、大量ループ、メモリ確保、切断処理前の負荷を発生させ得る。

対象:

- `C2S_ReorderLoadoutsPacket`
- `PvpLoadoutDefinition` の武器・アタッチメント一覧

**推奨修正:** 読み取り前に件数を検証し、ロードアウト数、武器数、武器ごとのアタッチメント数、文字列長へサーバー側の定数上限を設ける。負数または上限超過は即時拒否する。

**受入条件:** 上限値、上限+1、負数、途中で切れたデータに対するデコードテストが存在し、不正データが状態変更へ到達しない。

**修正:** ロードアウト128件、ID 64文字、武器9件、武器ごとのアタッチメント16件などの上限を追加した。件数は確保・反復前に検証し、負数・上限超過は即時例外、途中切れは状態変更前にデコード失敗となる。境界検証テストを追加した。

### P1-3. Siege SavedData の一部が破損レコードに弱い（修正済み）

**影響:** 古いバージョン、手動編集、部分書き込みなどで `RetryCooldowns` または `PeaceTruces` の UUID が欠落すると、ロード時の `getUUID` が例外を投げ得る。Siege の定期 tick からロードされるため、公開時間中の戦争処理が繰り返し失敗する可能性がある。

`Active` と `Fallen` には防御的な読み込みがある一方、上記2一覧は同等の防御になっていない。

**推奨修正:** `hasUUID` と型確認を行い、レコード単位の例外をログ付きでスキップする。1件の不正データで SavedData 全体を読めなくしない。

**受入条件:** UUID 欠落、型違い、未知フィールドを含む保存データでも、有効な Siege データは復元され、サーバー tick が継続する。

**修正:** `RetryCooldowns` と `PeaceTruces` をUUID存在・残り時間・当事者妥当性で検証し、レコード単位の例外を警告付きでスキップする。未知フィールドは従来通り無視する。

## 5. 公開阻害ではないが残っている問題

### P2-1. TPA の静的状態をサーバー停止時に明示クリアしていない（修正済み）

`TpaRequestManager` は要求とクールダウンの一部をシングルトンのメモリへ保持する。通常のプロセス再起動では JVM ごと破棄されるが、同一 JVM 内で統合サーバーを開き直す場合やテストでは、前のサーバー状態が残る可能性がある。

停止イベントで全要求、保留タスク、参照を明示クリアするのが安全である。

`ServerStoppingEvent` で要求、ウォームアップ、要求クールダウン、戦闘ロックをすべて破棄するようにした。

### P2-2. Analytics トークンが URL と設定ファイルから漏れやすい（修正済み）

管理画面アクセス用トークンを `?token=` のクエリへ含める経路があり、ブラウザ履歴、アクセスログ、画面共有、Referer に残る可能性がある。また、生成設定ファイルの権限を所有者限定に強制していない。

短期的には外部公開を避け、リバースプロキシ側の認証と HTTPS を使う。長期的には Authorization ヘッダーまたは短命セッションへ移行し、秘密を保存するファイルの権限を制限する。

APIは `Authorization: Bearer` のみを受理し、ダッシュボードはトークンをタブ単位の `sessionStorage` に保持する。エクスポートも認証ヘッダー付きfetchへ変更した。設定保存後は対応ファイルシステムで所有者の読書きだけ（0600）へ制限する。

### P3-1. Analytics Web サーバーの Executor を明示終了していない（修正済み）

`AnalyticsWebServer` は固定スレッドプールを生成して HTTP サーバーへ渡すが、停止時に Executor 自体を shutdown していない。再起動やテスト反復でスレッドが残る可能性があるため、フィールドとして保持し、停止時に期限付きで終了するべきである。

専用Executorをフィールドで保持し、通常停止・起動失敗の双方で `shutdown`、2秒待機、必要時 `shutdownNow` を実行する。

## 6. 確認済みの安全側実装

以下は今回および直前のレビューでコード上の対策を確認した項目である。

| 項目 | 確認内容 |
|---|---|
| 初期物資の再配布 | 永続フラグで二重配布を防止 |
| 初回スポーン探索 | 永続安全地点プール、重複防止リース、複数 tick 化、タイムアウト、バニラスポーンへのフォールバックあり |
| オンボーディング中の切断 | 永続状態から再開可能。一時状態はログアウト・停止時に解放 |
| 建国地点の遠隔指定 | 距離、視線、ロード済みチャンク等を検証 |
| 建国途中の失敗 | コア配置・登録失敗時に国家作成をロールバック |
| 予約領土 | 建国時のコア周辺に生成され、初期準備用途として扱われる |
| 予約領土の収納 | `allowsStorage` が初期予約を許可し、建国直後の詰みを回避 |
| 収納権限喪失 | 開いている制限対象メニューを閉じる追従処理あり |
| 地形タイル欠落 | カスタム地形有効時の停止は意図した fail-fast と README に記載 |
| クライアント専用クラス | 共通側の明白な直接参照は確認できず。JEI アダプターはクライアント互換領域 |
| Discord/JDA 分離 | プレイヤー用配布物とサーバー用ランタイムの分離検証タスクが成功 |

## 7. 実機で行う公開前受入テスト

静的確認だけではイベント順序、Mod 間相互作用、同時接続時負荷までは保証できない。次の順番で専用の新規ワールドを使って確認する。

### 最優先: プレイヤーが遊び始められるか

1. 完全新規プレイヤーで接続し、初期物資が一度だけ付与される。
2. 荒野を選び、探索成功またはタイムアウト後に拘束が解除される。
3. 探索中に切断して再接続し、画面と処理を再開できる。
4. 国家参加を申請し、オンライン承認とオフライン中承認の双方で正常にスポーンできる。
5. ガイド条件を満たして建国し、指定位置へ領土コアと予約領土が生成される。
6. 予約領土内ではチェストを設置・使用でき、領土外では拒否される。
7. コア起動後も収納と補強の権限が正常に継続する。
8. 再起動後も国家、コア、予約領土、初期物資配布済み状態が維持される。

### 公開時の人数と失敗系

1. 20～30人相当の初回参加・スポーン探索を重ね、MSPT、TPS、チャンク生成数を記録する。
2. インベントリ満杯、TaCZ 銃 ID 不在、国家コアなし、全候補不適格などの異常条件で永久拘束されないことを確認する。
3. 無効な建国地点で国家だけが残らず、再試行できることを確認する。
4. 国家脱退・追放・解散直後に、収納 GUI と設置権限が即時更新されることを確認する。

### 修正後の防御テスト

1. Lightman's Currency 欠落時に NeoForge が明確な必須依存エラーを表示する。
2. 未ロードの遠距離座標を C2S パケットで送ってもチャンクがロードされない。
3. 異常件数のロードアウトパケットが低コストで拒否される。
4. 一部を破損させた Siege SavedData でも正常レコードと戦争 tick が継続する。
5. サーバー停止・再開後に古い TPA 要求が残らない。

## 8. 実行済み検証

初回監査ではコミット `496c807` の分離スナップショットを使用した。公開初動修正後は現在の作業ツリーで再検証した。

- `tools/gradle-local.sh assemble --offline`: 成功
- `verifyPlayerDistribution`: 成功
- `verifyDiscordRuntimeIsolation`: 成功
- `verifyPackagedDiscordRuntime`: 成功
- 修正後の全テスト強制再実行: 492件成功、失敗0、エラー0、スキップ0
- 追加した境界検証: 可変長デコード5件、Siege破損レコード採否3件
- Analytics Web回帰テスト: 10件成功（Bearer認証、query token拒否、停止・再起動を含む）
- 既存 JSON リソース: 構文エラーなし

これはビルドと自動テストの結果であり、上記の実機受入テストを代替するものではない。

## 9. 次の確認順

1. 新規ワールドで「初期物資から予約領土収納まで」の実機受入テストを通す。
2. Lightman's Currencyあり・なし・版違いの配布構成で依存エラーを確認する。
3. 未ロード遠距離座標と異常ロードアウトパケットを使った専用サーバー試験を行う。
4. 一部を破損させたSiege SavedDataを専用サーバーへ読み込ませる。
5. 20～30人相当の同時初回参加負荷試験を行う。

コード上の重大リスクは除去済みである。以後は実際のMod構成、イベント順序、同時参加負荷を含む専用サーバー試験で公開可否を確定する。
