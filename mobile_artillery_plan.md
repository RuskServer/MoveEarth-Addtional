# 機動砲兵・攻城役割分担

更新: 2026-09-19

## 目的

補強外殻への火力、突破口の確保、次の射線への火力輸送を分担する。
対歩兵ダメージや遮蔽無視によって機動兵器を必須化しない。
固定砲は準備した射線で有効、機動砲は射線の変更で有利にする。

## 第一段階（実装）

### コアへの攻撃効率

- 既存のCBC弾種別補強ダメージと、機関砲・破片のコア無効を維持。
- 領土コアと同じブロックを着弾点とする重砲攻撃に追加倍率を適用。
  爆風範囲内にあるだけのコア、車両コア、プレイヤーへの追加倍率は適用しない。
- `cbcDamage.directTerritoryCoreMultiplier`: 既定2.5。徹甲弾96×2.5=240、
  既定首都HP2000なら9発。露出判定・オフライン防衛による軽減を引き続き適用する。
- WarnauticsのC4・爆弾の既存設定は維持する。

### 歩兵の破壊工作

- 露出した敵領土コアに、溶接機を持ち、オフハンドにTNTを4個以上入れてしゃがみ右クリック。
- 20秒、コアから4ブロック以内でコアを見ながらしゃがんで設置。開始地点から0.5ブロックを
  超える移動、被弾、工具変更、ダウン・拘束などで中止。設置完了まで資材は消費しない。
- 完了時にTNTを4個消費し40秒の作動待ち。周囲32ブロックにBossbarと毎秒の警告音。
- 所有国・同盟国のプレイヤーは溶接機でしゃがみ右クリックし、5秒同じ操作を維持して解除。
- 同時に1コア1件、攻撃者1人1件。作動時に最大HPの20%を基礎ダメージとして通常の
  オフライン防衛・陥落処理に通す。無軽減・妨害なしなら5回、約5分とTNT20個が必要。
- 閉鎖、停戦、所有権変更、開放時間終了、チャンク未ロード、攻撃者の退出・別次元移動では
  安全側に中止する。工作は再起動を跨いで保持しない。設置後に中止されたTNTは返却しない。
- `siege.coreSabotageEnabled`で無効化できる。初期の時間・材料・割合は固定。

### 修復待ち

- `cbcDamage.breachRepairDelaySeconds`: 既定60秒。CBC・Warnautics・採掘で補強に
  有効ダメージが入ると、その位置への手動修理を待機させる。再被弾で延長。
- 補強破壊後も位置の待機情報を保存。置き直しで消えず、Sable組立先にもコピーする。
- 新規補強は「修復待ち終了 → 既存の30秒起動待ち → HP増加」。既存の起動残時間表示を利用。
- 建設中の有効な補強が被弾した場合は自動HP増加を終了し、以後は手動修理を必要とする。
- 通常ブロックによる応急遮蔽は可能。補強壁越しのダメージを通す特例は作らない。

## 第二段階

### 重砲の射撃準備

ユーザー方針により実装を見送る（2026-09-19）。以下は検討履歴であり実装予定ではない。

- 新設・砲本体の再組立後に90秒の準備時間を設ける候補。
- 走行、飛行、砲塔旋回、ヒンジ・スイベル動作で準備を失わないこと。
- 現在のCBC連携はProjectileDamageEventを読む着弾フック。
  準備時間は発射前に扱う必要があるため、このフックでは実装しない。
- 運用CBCバイナリの砲組立・発射経路を確認し、砲の識別、分解時の失効、Sableへの移送、
  再起動、複数砲の区別を設計してから導入する。

### 車両修理

実装済み。自国車両コアに溶接機を持ってしゃがみ右クリック、オフハンドの鉄インゴットを
成功時に1個、溶接機耐久を1消費する。補強管理権限・維持費による保護の有効性・実空間距離6ブロック以内を確認。

- コアまたは接続車体の補強が被弾した後120秒間は応急修理。5秒間隔、5HP、最大HPの50%まで。
- 120秒被弾がなければ本格修理。2秒間隔、30HP、最大HPまで。
- HP0の無効化されたコアは応急修理では復帰せず、無被弾期間後に修理可能。
- 被弾による待機期限と次回修理期限を車両IDに紐付けて保存。複数人、移動、組立、再起動で間隔を回避できない。
- 失敗・満タン・待機中は資材や工具耐久を消費しない。コマンドや新規パケットは追加しない。
- `core.vehicleRepairQuietSeconds`、`vehicleEmergencyRepairHp`、`vehicleNormalRepairHp`、
  `vehicleEmergencyRepairCapPercent`、`vehicleEmergencyRepairIntervalSeconds`、
  `vehicleNormalRepairIntervalSeconds`でサーバー側から調整する。

## 実機確認

1. CBC徹甲弾の補強壁への着弾は壁HPのみを削り、背後のコア・兵士を貫通しない。
2. コア直撃と付近の爆風で追加倍率の有無が分かれ、同一弾の複数イベントで二重加算されない。
3. 同じコアへの工作同時開始、設置中の移動・被弾、解除競合、ダウン、捕虜、停戦を確認。
4. 工作でHPがゼロになった場合に通常のSiege陥落と復興へ進むこと。
5. 被弾補強を破壊・置き直し・保存再読込・Sable組立しても残り待機時間を回避できないこと。
6. 煙幕の遮蔽効果、塹壕、CBCの対人爆風、PlayerReviveの救出は実機で別途確認。
7. 同人数・同予算の歩兵のみ／固定砲支援／機動砲支援で、突破所要時間、弾薬費、装備損失を比較。

機動車両の優位性は実機比較で確認する。射撃準備の制限は実装しない。
