# 倉庫拠点 PvE/PvP コンテンツ実装計画

状態: 第4弾・報酬/製作ゲート/JEI導線と、新規ワールド生成時のバニラ構造物配置まで実装（2026-09-22）。実機生成試験は未完了。

新規ワールド生成方針（2026-09-22、従来の「既生成チャンクへ後置き」方針を置換）: ワールドを再生成するため、無人時の候補探索・チャンクチケット取得・貼り付けスケジューラは撤去。`warehouse_region` 構造物配置と `warehouse.nbt` のテンプレートピースでチャンク生成と同時に建てる。生成済みチャンクへ遡及配置はしない。構造物開始チャンクのロード時にSavedDataへ拠点登録し、保護・PvEへ接続する。テスト項目は地方あたり件数、木・村との重なり、NBTブロックエンティティ、Youer環境での再起動後の登録・保護、Chunkyでの事前生成順序に依存しないこと。

固定配置（同日）: `build/terrain/moveearth_terrain.tar.gz` の8192×8192・1024セル/8ブロック・8地方のタイルを原本に、8地方の倉庫X/Z最小座標を固定した。以前の`run/config`にある512セル/16ブロック・5地方の試験タイルとは異なる。`tile.json`、`region.bin`、`height.bin`、`river_segments.json` のSHA-256が原本と一致しない場合、誤配置を避けて倉庫生成を無効化し警告する。Yのみ実地地形から決める。実地で海面+4未満または9点の高低差が10ブロック超なら不採用。低い側を石レンガ基礎で支え、村の構造物開始チャンクから7チャンク以内は除外する。**1地方最大1棟、タイル全体で最大8棟**。管理者は`/warehouse status`で固定候補と登録状態、`/warehouse locate`で最寄り候補を確認できる。バニラの`/locate`は独自の配置方式を探索しない。

| 地方 | 倉庫の最小X | 倉庫の最小Z | 原本タイルの地表高（概算） |
|---:|---:|---:|---:|
| 1 | 4722 | 1484 | 94–97 |
| 2 | 2450 | 1916 | 131–133 |
| 3 | 4178 | 3548 | 95–96 |
| 4 | 4690 | 4956 | 91–92 |
| 5 | 3954 | 3932 | 92–93 |
| 6 | 1586 | 5596 | 104–107 |
| 7 | 2450 | 5820 | 109–112 |
| 8 | 2930 | 6428 | 83–85 |

NBT切替メモ（同日）: 新たな `warehouse.nbt` はストラクチャーブロック形式、DataVersion 3955、37×11×17、6,919ブロックデータ、1,759ブロックエンティティ、エンティティ0。最上段 y=10 は空気で、屋根は y=9 以下に存在する。x=0 に保存用ストラクチャーブロックが1個あり、バニラ `BlockIgnoreProcessor.STRUCTURE_BLOCK` で設置から除外する。インベントリNBTは空だが、実行時に非空の `Items` やLootTable/Commandが混入したファイルを拒否する。NBTを標準の `data/moveearth_addtional/structure/warehouse.nbt` として同梱し、`StructureTemplate.placeInWorld` で配置するためWorldEditは不要。古い `warehouse.schem` は自動配置に使わない。**NBT化しただけでは木・既存建築・斜面との衝突は自動解決しない**ので、後述の安全候補判定は維持する。

旧・後置き自動配置案（廃止）: ランダムスポーン候補を使う方式は、ワールド再生成方針により不要となった。チャンクチケット取得・無人時貼り付け・配置ジャーナルを使う自動処理は実行しない。管理者の手動修復コマンドは残す。**実機でのCopycats/輸送コンテナの復元、平坦候補の実出現率、Youer互換性は未確認**。

実装メモ（同日）: 第1段階のうち、WorldEdit手動貼り付け後の管理者登録、1地方1件の永続記録、既存領土との衝突検査、登録済み拠点の基本保護を実装中。ボス/NPC、報酬、JEI、地図表示、候補自動探索はまだ未実装。MOD独自の地形改変経路も網羅できていない。現時点のコードを完成版コンテンツとして公開しない。

第2弾メモ（同日）: 登録済み拠点へのCBC直撃ブロック損傷、Warnauticsの爆破対象リスト・チップ破壊を保護判定に接続した。建物本体のバニラ扉・トラップドア・フェンスゲート・ボタン・レバーだけを右クリック保護の例外とし、保護帯や収納物は閉じたままにする。これらは入口の基本操作であり、ボス戦用ギミックの許可リストとは別。Create/Sable等の独自地形改変、火災やMob、境界条件を含む実機破壊試験は未完了であり、保護が完全とはまだみなさない。

第3弾メモ（同日）: 倉庫単位の永続戦闘サイクル、ボスUUIDによる討伐/異常消失の区別、警備員・警備隊長の試験スポーン、Bossbar、HP50%以下で一度だけの全体チャット/タイトル通知、増援、NPC通常ドロップの抑止を追加した。警備員・隊長は現段階では専用タグ付きバニラPillagerであり、TaCZ射撃AIや専用エンティティ・装甲フェーズは未実装。戦利品庫と固有報酬がまだないため、**一般プレイヤーの接近による自動起動は無効**。運営者のみ `/warehouse testencounter <地方ID>` と `/warehouse resetencounter <地方ID>` で検証する。討伐は `DEFEATED` で停止し、次サイクルへの自動移行は第4弾の報酬/クールダウン実装後に有効化する。実機での戦闘時間、TaCZ・PlayerRevive・PvPとの整合、敵の地形越え/経路は未確認。

第4弾メモ（翌日）: 空襲兵から射撃AI・TaCZ装備ロジックを継承した専用 `warehouse_raider` エンティティへ変更。空襲ID/死亡会計・空襲ドロップを切り離し、48ブロック以上の追跡を抑制した。通常プレイヤーの建物内接近で戦闘を開始し、討伐時に1サイクル1回だけ共有の9スロット戦利品を確定・保存。輸送コンテナの右クリックで誰でも同じ在庫を開き、開放10分→開放時間8時間のクールダウン→次サイクルへ移る。固有素材「精密射撃機構」2個とネザー素材1種類を生成し、TaCZの実銃カテゴリが `sniper` 系のガンスミスレシピに固有素材1個を追加。JEIには固有素材とネザー副産物の入手元説明を登録した。古い試験版の `DEFEATED/FAILED` 状態は報酬を生成せず休眠へ移行する。

残る確認事項: 実機でTaCZガンパックの実カテゴリ、ガンスミスUI/JEIに追加素材が**入力スロットとして**同期表示されること、バニラNBT配置後のCopycats・輸送コンテナのNBTと見た目、複数人同時回収/サーバー再起動時の在庫、空襲AIが建物内で射線・経路を正しく取ること、PlayerRevive/PvP/大型爆発との整合。今回の固有素材のアイコンは仮の鉄インゴット画像で、専用アートが必要。運営の実機テスト前に完成版として配布しない。

現在の管理者確認手順: 運用タイルの一致ログを確認して新規ワールドを生成する。`/warehouse status`で固定8候補と生成済み登録を確認し、`/warehouse locate`で最寄りを確認する。Chunkyで該当チャンクを生成してもよい。村との近接や実地高低差で除外された場合はstatusに理由が出る。`/warehouse preview`と`/warehouse register`は手動修復用。新規生成にWorldEditは不要。

## 1. 目的と確定した方針

- `warehouse.nbt` を実際の恒久的な建造物としてオーバーワールドに配置する。インスタンスダンジョンにはしない。
- 1地方に **最大1拠点**。条件を満たす土地がなければ0件とし、無理に村・既存領土・地形を潰さない。
- 敵対NPCとボスを置く。ボスのHPが初めて50%以下になった時に全サーバーへ通知し、別勢力が現地へ介入できる公開戦闘にする。
- 倉庫固有素材をスナイパー銃の製作に要求する。マークスマン銃の現行レシピ・入手経路は変えない。
- ネザー専用素材の一部を少量入手できるようにする。ただしネザー進行の全面代替・無限養殖にはしない。
- 討伐後はサーバー開放時間で計測するクールダウンを経て敵と報酬が復活する。建物そのものは消去・再生成しない。
- 倉庫と周辺では建国・領土拡張、プレイヤーによる建築・破壊、機械や爆発を使う地形改変を防ぐ。プレイヤー同士の戦闘と進入は許す。
- JEIから固有素材の入手先とスナイパーの製作経路を追えるようにする。導線はゲーム内GUI・地図を優先し、コマンド必須にしない。

## 2. 現物・既存コードの確認結果

| 対象 | 確認結果・実装への影響 |
|---|---|
| `warehouse.nbt` | バニラのストラクチャーブロック形式 / MC DataVersion 3955。37×11×17ブロック。87種類のブロック状態、1,759件のブロックエンティティ、エンティティ定義は0件。NPCのスポーン点は別途定義する必要がある。旧 `warehouse.schem` は配置に使用しない。 |
| 構成MOD | `copycats`、`createdeco`、`dndecor`、`farmersdelight`、`createdieselgenerators`、`create` とバニラを使用。特にCopycatsの見た目はブロックエンティティの `material_data` に依存する。 |
| 既存の地方 | `RegionResolver.regionAt(x,z)` がタイル上の地方IDを返す。海上にも最寄り陸地の地方IDを返し得るため、拠点配置には **陸地・地形条件を別途確認** する。 |
| 既存NPC | `AirshipRaiderEntity` と `RaiderShootGoal` にTaCZ銃を使う敵の実装がある。ただしそのまま倉庫へ流用すると空襲ID・戦利品・AIの意味が混ざるため、射撃処理だけを共通化するか倉庫専用エンティティを作る。 |
| 既存の時間軸 | `OpenTimeService.now(server)` はサーバー開放中のみ進む永続時計。倉庫クールダウンもこれを使う。 |
| 既存の国家/領土 | `NationFoundationService`、`TerritorySavedData.validateRegistration/updateRadius` が建国・領土予約を扱う。倉庫禁止区域の判定を双方へ追加する必要がある。 |
| 既存JEI | `MoveEarthJeiPlugin` は現時点では技術画面からJEIのレシピを開く用途。倉庫の入手元表示は追加実装が必要。 |
| TaCZレシピ | `TaczWorkbenchRecipeHandler` が既存のワークベンチレシピ置換を行う。手元のTaCZ 1.1.8 jarでは `CommonGunIndex.getType()` / `GunIndexPOJO.getType()` が存在するため、銃IDの手作業allowlistではなくガンパックのカテゴリを判定軸にできる。 |

`.schem` とバニラストラクチャーNBTは別形式だが、現在は提供された `warehouse.nbt` を原本として使う。Copycats・輸送コンテナの復元は本番Youer環境で読込・配置・再起動試験を行う。開発用素材の原点座標や既存コンテナのNBTをそのまま本番の報酬源にしない。

## 3. プレイの流れ

1. 地方マップに「未発見の倉庫候補」を表示。探索して接近すると拠点名・状態・保護範囲が分かる。未発見でも外観から発見できる。
2. プレイヤーが倉庫に入ると警備NPCが反応し、ボス戦が開始。戦闘は同一オーバーワールド上で進み、他勢力も出入りできる。
3. ボスHPが50%をまたいだ瞬間に1サイクル1回だけ「倉庫襲撃中」を全体通知。地方名とおおまかな位置を提示し、地図に一時マーカーを出す。正確なブロック座標は近づいて発見できる。通知はチャットに残し、短い音・タイトルも付ける。
4. 後半戦はNPC増援と防衛行動で**現地勢力に約8〜12分の交戦余地**を作る。単純な長時間無敵ではなく、装甲/援軍/遮蔽とHPで調整し、別地方から来る参加者が着く前に終わる場合は通知時刻・到着時間を計測して調整する。
5. ボス討伐で建物内の専用戦利品庫が短時間だけ解錠。誰でもアクセスでき、戦利品の奪取・護送を巡ってPvPが起こり得る。ボスのラストヒットやNPCへの与ダメージだけで報酬所有権を確定しない。
6. 戦利品庫が閉じると休眠。クールダウン満了後、次のプレイヤー接近時に敵・ボス・戦利品庫のみを再構成する。遠方での常時チケット/AI稼働はしない。

公開通知でPvPを誘発する設計なので、戦闘可能時間外にボスだけ削って待つ抜け道を作らない。既存のPvP/Combat Timer/Siegeルールとの優先順位を明記して実装する。倉庫そのものは国家所有物にならず、Siegeの開戦・陥落判定とは切り離す。

## 4. 拠点の割り当てと安全な配置

### 4.1 候補選定

- 永続キーは **ディメンション + 地方ID**。原本タイルから選んだ固定X/Zを各地方に1件だけ割り当てる。ワールドシードや生成順序で座標を変えない。
- タイル不一致、固定位置の地方ID不一致、実地の極端な高低差、村の近接では生成しない。地表や他MOD建造物との干渉は実機で検証する。
- 建物の37×17 footprintが同じ地方の陸地であることを起動時に確認し、実地の地表高を生成時に検査する。
- 新規ワールド生成専用。既生成チャンクやプレイヤー編集済みチャンクには自動上書きしない。構造物配置はチャンク生成の一部であり、遠方チャンクを別途同期生成しない。
- 原本タイルのSHA-256と固定座標を照合する。Chunkyの生成順序やプレイヤー人数によって候補を変えない。
- 地方ごとに座標は1つ。実地の地形条件が合わなければ0件とし、別地点へ飛ばさない。

地方資源システムのタイルを後から変更した時は、既設拠点を勝手に移動しない。保存済み地方IDとの不一致を検出し、管理者に移設/継続の判断を求める。

### 4.2 ストラクチャー導入

- `warehouse.nbt` を原本とし、バニラの `StructureTemplate` で初回のみ配置する。WorldEditや独自形式変換器は不要。
- 本MODのカスタム構造物配置が新規チャンクの生成時に建てる。既生成チャンクへの貼り付けは行わず、プレイヤーにコマンドを要求しない。
- サーバー起動時に寸法、パレットのブロック登録、非空インベントリ、不要なエンティティ/コマンドNBTを監査する。ストラクチャーブロック自身は配置しない。
- Copycatsの `material_data` を含む見た目を実機で確認する。Create Decoの連結コンテナ等も隣接再計算が必要か調べる。装飾コンテナは開けない設定にする。
- バニラAPIの配置もワールド変更を伴うため、事前バックアップ、配置後検査、失敗時の復旧手順を用意する。配置開始の保存後に停止しても「部分設置を攻略可能」とせず、管理者が同一位置を復旧してから `READY` にできるようにする。
- 建物は毎サイクル貼り直さない。保護と専用設備の状態復元で維持する。

### 4.3 保護範囲と国家予約

初期値として、建物AABBから水平方向64ブロック以内を**建築・破壊禁止区域**、128ブロック以内を**国家コア/予約領土の禁止区域**とする。数値は建物の実機導線と地域密度を見て設定可能にする。円形中心判定では建物の長辺を取りこぼすため、AABBからの距離で判定する。

- 建国時の最初のコア、通常コア設置、予約領土の半径変更、領土拡張の全経路で禁止区域との交差をサーバー側で拒否する。既存領土と衝突する候補は配置しない。
- 設置・採掘・右クリック使用、バケツ/流体、ピストン、TNT/CBC/Warnautics爆発、Create機械の破壊・設置、Sable車両の組立/解体・ブロック移動、火災やMobの地形改変まで、**地形変更の結果地点**を共通ポリシーで判定する。通常イベントだけではMOD経路を網羅できないため、実機テストで穴を確認する。
- 物資保管、他人を閉じ込めるブロック、スポナー移動、補強/溶接、チェスト・タンクへの出し入れを禁止する。建物の扉・スイッチなど攻略用インタラクトだけ明示許可する。
- NPC/プレイヤー/銃弾へのダメージは通すが、倉庫建材の破壊は不可。CBC・車両は周辺戦闘に参加できても拠点そのものを更地にできない。境界外からの爆発でも保護内のブロックだけを保持する。
- 権限のある管理者のみプレビュー・保守・撤去を行える。保護拒否時には距離と理由をUI/短いメッセージで返す。

## 5. 敵・ボス・状態管理

- NPCは倉庫専用の警備員として小隊単位で出す。既存TaCZ射撃AIは再利用候補だが、空襲ID・通常ドロップ・プレイヤー追跡範囲を流用しない。建物内の短・中距離が主で、外への追跡距離と同時存在数を制限する。
- ボスは固有ID・Bossbar・難易度/装甲フェーズを持つサーバー管理エンティティ。50%通知は `previousHp > 0.5*maxHp && newHp <= 0.5*maxHp` の境界通過で判定し、永続フラグで重複を防止する。大量ダメージで50%を飛び越しても通知する。
- 状態は `DORMANT -> ACTIVE -> ALERTED -> LOOT_OPEN -> COOLDOWN -> DORMANT`。プレイヤー不在・ボス消失・チャンクアンロード・再起動ごとの復帰規則を保存する。異常消失では報酬を生成せず、リセット後に再挑戦可能とする。
- 離脱でHPをリセットして通知を連打したり、毎回半分まで削って報酬なしで他勢力を釣ったりできないよう、同一サイクルの再挑戦/失敗に短いロックアウトと監査ログを設ける。
- NPCの射撃・ボス攻撃は壁越しヒットを許さない。TaCZ、PlayerRevive、Combat Timer、弾薬消費/装備ドロップとの整合を確認する。
- 上限例: 同時警備NPC 6体、同時ボス1体/拠点、近隣にプレイヤーがいない時はAI停止・チャンク常時ロードなし。人数が多いほどHPだけを上げる単純なスケーリングは避け、上限付き増援数で調整する。

## 6. 報酬とスナイパーの製作ゲート

### 6.1 固有素材

仮称 `精密射撃機構`（アイテムID案: `moveearth_addtional:precision_firing_assembly`）を追加する。**スナイパー銃の指定レシピに1個**要求し、マークスマン/DMRとピストル・AR・LMG等には要求しない。倉庫1回の完全攻略で確定2個を基本値とし、実戦データから供給数を調整する。装備そのものをボスから直接落とさず、産業素材と組み合わせて製作する設計にする。

**ガンパックの銃カテゴリで判定する**。TaCZの `CommonGunIndex.getType()` を正規化し、`sniper`（実データの表記に合わせて設定可能な同義カテゴリを含む）の銃だけに固有素材を要求する。`marksman`/`dmr` は除外する。表示名や銃IDの手作業allowlistは主判定にしない。データパック/ガンパック再読込時にカテゴリと対象レシピを再評価し、未知カテゴリ・参照不能の銃は勝手に変更せず警告する。カテゴリと実際の銃種にズレがあるガンパックだけ、例外設定で上書き可能にする。外部レシピとの置換優先順位、JEI表示、既存在庫の扱いも検証する。

### 6.2 ネザー素材

暫定候補はネザーウォート、クォーツ、グロウストーンダスト、少量のブレイズロッド/ガストの涙。1回の倉庫攻略で**1〜2種類を少量**、確定枠ではなく副産物枠とする。ネザライトスクラップ/インゴット、ウィザースケルトンの頭、ネザースターは初期ドロップに入れない。特に既存PvP報酬との重複と、Createの加熱・CBC製造の進行ゲートを確認してから確定する。

クールダウン初期値は**8サーバー開放時間**。開放時間外とサーバー停止中は進めず、1拠点あたりの供給上限を明確にする。`19:00–23:00` 開放なら連続で約2開放日に相当するが、運営が時間帯を変えても「開放8時間」という意味は変わらない。

### 6.3 戦利品の不正対策

- 一回の討伐につき報酬生成は一度だけ。サーバー保存前後で二重生成しないトランザクション/状態順序を設計する。
- 専用戦利品庫は討伐時に内容をサーバー側で確定し、開始前のコンテナやNPCから固有素材が取れないようにする。
- 一定時間後の未回収品は消すのではなく「次サイクルへ持ち越さず、運営ログに残す」方針を明示。実際の消失・回収猶予はテストプレイで決める。
- PvP争奪の価値を残す一方、討伐側が回収前に敵対勢力へ即横取りされるだけの体験にならないか計測する。必要なら数十秒の解錠演出や防衛側への位置情報を調整し、個別の安全な報酬付与にはしない。

## 7. JEIとゲーム内導線

- 固有素材にJEIの「倉庫襲撃で入手」情報を登録。地方単位で最大1件、ボス討伐後に公開戦利品庫から取得、開放8時間クールダウン、確定数を表示する。単なるクラフトレシピと誤認させない。
- ネザー素材には通常の入手経路を消さず、倉庫入手を代替手段として示す。ドロップ率・数量はサーバー設定/データから生成し、JEI文言と実際の抽選が乖離しないようにする。
- スナイパー側のJEIレシピに固有素材が**実際の入力スロット**として出ることを確認する。TaCZのレシピ表示が外部JEIプラグイン任せの場合は連携API/同期順を検証し、表示できない時は独自カテゴリで補完する。
- 地方マップ/案内GUIには、発見済み倉庫の場所、`休眠/警戒/戦利品公開/再稼働まで` の状態、保護範囲を表示。クライアントが知らなくてもサーバー側の保護・報酬判定は成立させる。
- JEI未導入でもゲームが動き、ゲーム内ガイドから入手先を確認できることを必須とする。

## 8. 実装順序と完了条件

1. **素材と互換性の確定**: バニラ `warehouse.nbt` を隔離ワールドへ配置し、Copycats/コンテナの見た目・NBT・必要MODを確認。TaCZの `getType()` が返す実カテゴリとレシピをログで確認。失敗条件を先に作る。
2. **地方配置・保護**: 決定論的な最大1件割り当て、プレビュー、バニラNBT自動配置と登録後検査、保護マスク、国家コア/半径変更ブロック、マップ表示を実装。既存ワールドを無承認で上書きしないテストを通す。
3. **戦闘・公開通知**: 倉庫専用NPC/ボス、Bossbar、50%一回通知、増援、再起動/アンロード復帰、TPS上限を実装。通知から別地方勢力が到着できる時間を実機で測る。
4. **報酬・レシピ・JEI**: 固有素材、対象スナイパーのみの製作ゲート、少量ネザー素材、戦利品庫、開放時間クールダウン、JEI入手先/レシピ表示を実装。デュープとリログを重点検証。
5. **公開テスト**: 2〜3勢力・数時間のテストで「攻略所要時間、通知から到着まで、NPCによる負荷、固有素材の1日供給数、横取り成立率」を記録し、数値のみ調整する。

最低限の自動テスト: 地方ごと最大1件、候補0件の許容、ワールド再起動で位置不変、既存領土/建築/海岸との衝突拒否、すべてのコア登録/半径変更経路の拒否、爆発/機械改変の保護、50%通知一回、失敗時の報酬0、成功時の報酬一回、開放時間外のクールダウン停止、JEIの表示データと報酬/レシピの一致。

### 実装前に確定する小さな判断

1. 対象ガンパックで `getType()` が返すスナイパー/マークスマンのカテゴリ文字列。銃ID一覧は検証ログとして出すが手動allowlistにはしない。
2. ネザー素材の最終ドロップ表。ブレイズロッドを含めるかはCreate進行への影響確認後に決める。
3. 倉庫の既存ワールドへの初回設置を「管理者承認必須」にするか、未生成チャンク限定で自動化するか。安全側の初期実装は承認必須。
4. 公開通知の位置精度と後半戦の目標時間。最初は地方名＋概略座標、8〜12分を仮置きする。
