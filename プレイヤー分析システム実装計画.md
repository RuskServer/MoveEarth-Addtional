# プレイヤー分析システム実装計画

## 1. 目的

建国サーバーの運営判断に必要な、次の情報を継続的に確認できる管理者向け分析ツールを作る。

- どの地域・ディメンションに活動が集中しているか
- ログイン人数ではなく、実際に行動している人数と時間
- 採掘、農業、建築、戦闘、クラフト、Jobsなどの活動構成
- プレイヤー検知ブロックを中心としたグループごとの活動状況
- 新規プレイヤーの定着、休眠、復帰傾向
- 収集処理がサーバーTPSやストレージへ与える負荷

初期版は運営管理者向けとする。プレイヤーやグループ所有者への公開は、位置情報が索敵・拠点特定に使われない遅延・集計・権限制御を実装した後に追加する。

## 2. 推奨構成

ゲーム内GUIだけでヒートマップや時系列比較を扱うのは制約が大きいため、次の二層構成を採用する。

1. MoveEarth-Addtionalがサーバー側でテレメトリを収集・集計する
2. 管理者専用Webダッシュボードが集計済みデータを表示する

Minecraftクライアント側の変更を必須にしない。ゲーム内には権限レベル2以上の`/analytics`コマンドを用意し、収集状態の確認、集計、エクスポート、ダッシュボードURLの案内だけを担当させる。

ダッシュボードは初期状態で`127.0.0.1`のみにバインドし、外部公開する場合はリバースプロキシ、HTTPS、認証を必須にする。Minecraftの通常ポートから分析APIを公開しない。

## 3. 分析上のグループ定義

明示的な国家データは新設せず、既存のプレイヤー検知ブロックから「検知グループ」を派生させる。

- グループID: 検知ブロック所有者のUUID
- メンバー: 所有者本人＋その所有者のプレイヤー検知ホワイトリスト
- 拠点範囲: その所有者が持つ、稼働中の各プレイヤー検知ブロックから半径100ブロック
- グループ表示名: 当面は`<所有者名>グループ`

同じプレイヤーが複数所有者のホワイトリストに入ることは許容する。この場合は複数グループ所属として扱い、グループ間の人数を単純加算してサーバー全体人数にしない。

本当に一人一国家へ限定したランキングが必要になった時点でのみ、`primary_group_owner_uuid`のような最小限の主所属データを追加する。

### 3.1 先行して直す必要がある点

現状の`PlayerWhitelistSavedData`はプレイヤー名を保存しており、呼び出したディメンションのSavedDataへ書き込まれる。分析の所属元として使うには不安定なため、収集実装前に次を行う。

- ホワイトリストの識別子をプレイヤー名からUUIDへ移行する
- 最終確認名は表示用として別に保持する
- 保存先をオーバーワールドのサーバー共通SavedDataへ統一する
- 既存の名前データは、ログイン履歴またはユーザーキャッシュから解決できたものだけUUIDへ移行する
- 解決不能な名前は削除せず「未解決メンバー」として管理者へ表示する

検知ブロック座標はディメンションごとで問題ないため、既存の`DetectorBlockPositionSavedData`を利用する。分析時にチャンクを強制ロードしてはならない。

## 4. KPI設計

### 4.1 主要KPI

| KPI | 定義 | 用途 |
|---|---|---|
| 開放日アクティブ人数 | 開放日中にAFKを除く有効活動を10分以上行ったユニークプレイヤー数 | 実際に遊んだ人口を把握する |
| アクティブプレイヤー時間 | AFKを除いた活動時間の合計 | 人数だけでは分からない総活動量を見る |
| 活動グループ数 | 開放日中に2人以上または合計30分以上の有効活動があった検知グループ数 | 建国・共同体活動の広がりを見る |

数値目標は実測データがない段階では設定しない。最初の4開放日をベースライン期間とし、その後に維持・改善目標を決める。

### 4.2 診断指標

- 同時接続数と実アクティブ数
- 新規、継続、復帰プレイヤー数
- 1開放日後・4開放日後の再訪率
- セッション回数、中央値セッション時間、中央値アクティブ時間
- AFK率
- オーバーワールド、ネザー、エンド、PvPアリーナ別の活動時間
- 32×32ブロックセル単位の滞在時間とユニーク人数
- 採掘、設置、農業、クラフト、戦闘、移動、Jobs XPの活動構成
- Jobsごとの獲得XPと活動人数
- 検知範囲内のメンバー滞在、外部訪問、侵入検知セッション
- TPA成功回数と移動元・移動先の区分。正確な座標は保存しない
- PvP参加者数、試合数、試合時間。通常ワールド活動とは分離する

### 4.3 ガードレール

- 収集処理の平均・最大実行時間
- 非同期書き込みキュー長と破棄件数
- 最終正常書き込み時刻
- DBサイズと日次増加量
- 収集欠損率
- 位置情報の最小集計人数と公開遅延

## 5. イベントと収集方法

### 5.1 位置サンプリング

- オンラインプレイヤーを30秒ごとにサンプリングする
- 全員を同じtickで処理せず、UUIDを基準に処理tickを分散する
- スペクテイターは除外する
- PvPアリーナは通常ワールドと別カテゴリへ記録する
- 保存前に座標を32×32ブロックのセルへ丸める
- Y座標は正確な値ではなく`地下深部`、`地下`、`地表`、`高高度`程度の帯にする
- 5分単位でメモリ上集計し、個々の生座標列は保存しない

位置サンプルには、ディメンション、セル座標、時間枠、プレイヤーUUID、活動中かAFKか、検知範囲区分だけを持たせる。

### 5.2 AFK・活動判定

最後に次のいずれかが発生した時刻をプレイヤーごとに保持する。

- 2ブロック以上の有効移動
- ブロック破壊・設置
- クラフト
- Mobまたはプレイヤーとの戦闘
- Jobs XPの獲得
- TPAまたはディメンション移動

5分間どの活動もなければAFKとする。視点移動やチャットだけでは活動状態を延長しない。チャット本文、コマンド本文、インベントリ内容は収集しない。

### 5.3 イベント集計

既存イベントへ薄い計測フックを追加する。

- `PlayerLoggedInEvent` / `PlayerLoggedOutEvent`: セッション
- `BlockEvent.BreakEvent` / `EntityPlaceEvent`: 破壊・設置。ブロックIDは個別保存せずカテゴリ集計を基本とする
- `PlayerEvent.ItemCraftedEvent`: クラフト回数とカテゴリ
- `LivingDeathEvent`: PvE/PvPキル・死亡
- `JobService`: Jobs XPをジョブID別に集計
- `TpaRequestManager`: 成功したTPAのみ集計
- `PvpMatchManager`: 試合参加・終了・勝敗を通常活動と分離して集計
- プレイヤー検知処理: 同一侵入者を5秒ごとに重複記録せず、入域から退域までを1セッションとして集計

すべてのゲームイベントはサーバースレッド上で小さな不変レコードへ変換するだけにし、DB書き込みは行わない。

## 6. 保存方式

高頻度データをMinecraftのNBT SavedDataへ蓄積するとワールド保存が肥大化するため、分析専用SQLite DBをワールドディレクトリ配下へ置く。

推奨パス:

```text
<world>/moveearth/analytics/analytics.db
```

### 6.1 書き込み方針

- SQLite WALモード
- 専用の単一ライタースレッド
- サーバースレッドからは上限付きキューへ投入するだけ
- 30秒または一定件数ごとにトランザクションでまとめて書く
- キュー満杯時はサーバーを止めず、位置サンプルを優先的に破棄して欠損数を記録する
- サーバー停止時は制限時間付きでflushする
- DBスキーマには明示的なバージョンとマイグレーションを持たせる

SQLite JDBCをMOD本体へ同梱する場合は、サイズ、ネイティブライブラリ、ライセンスを確認し、`THIRD_PARTY_NOTICES.md`へ追記する。問題が大きい場合は、初期版のみ日次の集計済みJSONLへ切り替えられる抽象化を用意する。

### 6.2 最小テーブル

```text
player_identity
  player_uuid, last_known_name, first_seen_at, last_seen_at

player_session
  session_id, player_uuid, joined_at, left_at,
  online_seconds, active_seconds, afk_seconds

player_activity_5m
  bucket_at, player_uuid, dimension, group_owner_uuid,
  active_seconds, distance_blocks, breaks, places, crafts,
  pve_kills, pvp_kills, deaths, jobs_xp, tpa_successes

spatial_activity_5m
  bucket_at, dimension, cell_x, cell_z, y_band,
  group_owner_uuid, relation, active_samples, unique_players

detector_activity_5m
  bucket_at, dimension, detector_pos_hash, group_owner_uuid,
  member_minutes, visitor_minutes, intrusion_sessions,
  distinct_members, distinct_visitors

collector_health
  recorded_at, queue_depth, dropped_events, flush_ms, db_bytes
```

`detector_pos_hash`は内部の安定識別用とし、通常APIでは正確な検知ブロック座標を返さない。

### 6.3 保持期間

- 5分粒度: 90日
- 日次集計: 1年
- セッション概要: 1年
- 管理者の明示的な調査用データ: 初期版では作らない
- 削除・日次集約はサーバー低負荷時間に小分けで実行する

同時接続50人を30秒間隔で取得した場合、最大14.4万位置サンプル/日になる。生サンプルを残さず5分集計すれば、実データ量は大きく減らせる。最初の4開放日で実測し、保持期間を調整する。

## 7. 権限・プライバシー

### 7.1 管理者

- 権限レベル2以上
- 32×32セルのヒートマップ
- プレイヤー別の粗い活動履歴
- 全検知グループの比較
- エクスポートと収集状態の確認

### 7.2 グループ所有者向け将来機能

- 自分の検知グループだけ
- 最低3人以上の集計セルのみ表示
- 6時間以上遅延
- 個人名、個人軌跡、正確な検知ブロック座標は非表示

### 7.3 公開統計向け将来機能

- サーバー全体の人数・活動量のみ
- 24時間以上遅延
- 地図は128×128ブロック以上へ粗粒度化
- グループ別軍事活動、侵入、個人情報は非公開

APIトークンは設定ファイルへ平文表示せず、初回生成後はハッシュを保存する。管理操作とエクスポートは監査ログへ残す。

## 8. ダッシュボード構成

### 8.1 概要

- 現在オンライン / 活動中 / AFK
- 今開放日のアクティブ人数
- アクティブプレイヤー時間
- 活動グループ数
- 過去開放日との推移
- 新規 / 継続 / 復帰の構成

### 8.2 活動マップ

- ディメンション別ヒートマップ
- 時間範囲、活動種別、検知グループ、メンバー/訪問者のフィルター
- 滞在時間、ユニーク人数、イベント量の表示切り替え
- ライブのプレイヤードットは表示しない

### 8.3 グループ分析

- メンバー数と実活動人数
- メンバーの合計活動時間
- Jobs・建築・採掘・農業・戦闘の構成
- 自領域内 / 他領域 / 荒野の活動比率
- 訪問者数と侵入セッション数
- 複数所属者がいる場合の注意表示

### 8.4 プレイヤー分析

- 初回参加、最終参加、総オンライン時間、総アクティブ時間
- 開放日ごとの活動推移
- Jobs構成と主要活動カテゴリ
- 粗粒度の活動地域
- 管理者専用。一般プレイヤー検索は提供しない

### 8.5 収集状態

- データ最終更新時刻
- キュー遅延・欠損イベント
- DB容量
- 収集処理時間
- 直近のマイグレーション・書き込みエラー

共通フィルターは、期間、開放日、ディメンション、検知グループ、活動種別に限定する。

## 9. コマンド案

```text
/analytics status
/analytics dashboard
/analytics export <from> <to>
/analytics retention
/analytics admin pause
/analytics admin resume
/analytics admin purge-before <date>
```

すべて権限レベル2以上とする。`purge-before`は確認トークンを要求し、対象期間と削除件数を事前表示する。

## 10. 実装フェーズ

### Phase 0: 仕様固定と所属基盤

- 収集対象、公開範囲、保持期間、セルサイズを設定化
- ホワイトリストをUUID・サーバー共通保存へ移行
- 検知グループ解決サービスを作る
- 活動カテゴリとAFK判定を定義する

完了条件: 同じプレイヤーについて、どのディメンションでも同じ所属結果になり、既存ホワイトリストを失わない。

### Phase 1: 収集基盤

- セッションとAFK状態
- 30秒位置サンプリング
- ブロック、クラフト、戦闘、Jobs、TPA、PvPのイベント集計
- 侵入検知のセッション化
- サーバースレッド上でDB I/Oをしないキュー

完了条件: 収集有効/無効でTPS差が測定誤差程度で、キュー溢れを故意に発生させてもサーバーが停止しない。

### Phase 2: 永続化と集約

- SQLiteスキーマとマイグレーション
- WAL・バッチ書き込み・停止時flush
- 5分粒度から日次粒度への集約
- 保持期間削除とDBヘルス指標
- 集計値の再計算テスト

完了条件: 強制終了後もDBが開け、同じ入力から同じ集計値が得られる。

### Phase 3: 管理APIとコマンド

- 読み取り専用の集計API
- トークン認証、レート制限、ローカルバインド
- `/analytics status`、`dashboard`、`export`
- CSV/JSONエクスポート

完了条件: 未認証アクセスでデータが取得できず、API問い合わせがMinecraftサーバースレッドを待たせない。

### Phase 4: 管理者ダッシュボード

- 概要、活動マップ、グループ、プレイヤー、収集状態
- 時間・ディメンション・グループ・活動フィルター
- データ鮮度と欠損表示
- 大期間問い合わせの上限とキャッシュ

完了条件: 直近開放日の主要KPIが相互に一致し、マップ・カード・表の合計差が説明可能である。

### Phase 5: 運用検証

- 4開放日のベースライン収集
- TPS、ヒープ、DB増加量、欠損率の測定
- AFK閾値、セルサイズ、保持期間の調整
- バックアップ・復元手順
- 管理者向け運用ドキュメント

完了条件: 連続4開放日で重大なデータ欠損がなく、運用者がダッシュボードから人口・活動地域・グループ活動を説明できる。

### Phase 6: 限定公開（任意）

- グループ所有者向けビュー
- 遅延、最低人数、粗粒度化
- アクセス監査
- 公開統計ビュー

完了条件: 単独プレイヤーの現在地や移動経路を推測できないことをテストで確認する。

## 11. ブランチ構成

```text
feat/analytics-identity
feat/analytics-collector
feat/analytics-storage
feat/analytics-api
feat/analytics-dashboard
feat/analytics-group-views
```

各ブランチは上の順に`main`へPRで取り込む。最初の実装対象は`feat/analytics-identity`とし、ホワイトリスト移行だけを独立してレビュー・ロールバックできるようにする。

## 12. テスト計画

- UUID移行、未解決名、複数所属、改名
- AFK境界、ログアウト、クラッシュ時の未flush
- セル境界、負座標、ディメンション切り替え
- PvPアリーナと通常活動の分離
- 侵入検知の入域・継続・退域・再入域
- キュー上限とイベント破棄優先順位
- DBマイグレーションと破損時の読み取り専用退避
- 日次集計と5分集計の一致
- API認証、権限、レート制限、巨大期間問い合わせ
- 0人、1人、50人以上を想定した負荷試験

## 13. 初期版で行わないこと

- ライブの正確なプレイヤー座標表示
- 全移動軌跡の保存
- チャット・コマンド本文の保存
- インベントリ内容や通貨残高の収集
- AIによる自動処罰・不正判定
- プレイヤー向け国家ランキング
- 明示的な国家データモデルの追加

## 14. 実装開始時の推奨決定

- 管理者専用Webダッシュボードから開始する
- 位置は30秒間隔、32×32セル、5分集計とする
- 生の正確な座標は保存しない
- AFK閾値は5分とする
- 詳細90日、日次1年を初期保持期間とする
- 国家ではなく「検知グループ」という表現を内部で使う
- 最初の4開放日は目標判定ではなくベースライン収集に使う

