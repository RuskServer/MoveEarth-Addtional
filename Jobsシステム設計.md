# Jobsシステム設計（MVP）

## 目的

プレイヤーが最大2つの職業を選び、対応する作業で職業XPを得てレベルアップする。レベルアップ時には全職業共通のジョブポイントを獲得する。経済・ポイント交換・永続バフは初期版には含めない。

## 実装済みの範囲

- 採掘師: 石材、ネザー系地質ブロック、天然鉱石の破壊でXPを付与する。鉱石は種類と深層版で報酬を分け、エメラルド鉱石を最高報酬とする。
- 木こり: プレイヤーが設置していない原木の破壊でXPを付与する。
- 農家: 最大成長した作物、自然生成または成長したカボチャ・スイカの収穫と家畜の繁殖でXPを付与する。
- 狩人: プレイヤー自身が対象の敵対Mobを倒した場合にXPを付与する。
- クラフター: プレイヤー操作による装備、機械部品、高度な設備のクラフトでXPを付与する。自動クラフトは対象外とする。
- 職業レベルは1から50、レベルアップごとに1ポイントを付与する。
- XP曲線と対象ブロック・獲得XPはデータパックから変更可能にする。
- `/jobs` 系コマンドで選択、解除、確認、管理を行う。
- 進捗はワールドの `SavedData` にUUID単位で保存する。

## XP曲線

次レベルに必要なXPは以下とする。`L` は現在レベル。

`100 + 25 * (L - 1) + 10 * (L - 1)^2`

数値は `data/<namespace>/jobs/*.json` の `xp_curve` で上書きできる。

## 不正取得対策

- 報酬対象ブロックを設置した座標はディメンションごとに保存し、破壊時にXPを付与しない。ピストン移動後も設置情報を追従し、爆発で消失した情報は除去する。
- 石材を建築に大量使用しても保存容量が線形に膨らみにくいよう、設置情報は16×16×16区間ごとの4096ビット集合として圧縮保存する。旧座標配列は読込時に移行する。
- 適正ツールが必要なブロックは、適正ツールで破壊した場合のみ対象とする。
- クリエイティブ・スペクテイター・PvPアリーナ内ではXPを付与しない。
- 職業ごとに1分500XPまでは等倍、それを超えた分は10%に減衰する。
- 判定とデータ更新はサーバー側だけで行う。
- XPは小数第2位まで設定・保存・同期・表示できる。Jobs Reborn形式の参考値を取り込む場合、経済報酬と行動ごとのポイントは使わず、3列目のXPだけを採用する。
- スポナー、スポーンエッグ、ディスペンサー、コマンド由来のMobには永続フラグを付け、狩人XPを付与しない。
- クラフト報酬は完成品スタック数ではなくクラフト完了イベント1回につき1回とし、一度に複数個完成するレシピによる倍率稼ぎを防ぐ。可逆変換や木材加工などの安価な往復レシピは初期タグに含めない。

## コマンド

- `/jobs`: Jobs統合GUIを開く。職業一覧、ポイント、レベル、XP、選択と解除を1画面で操作する。
- `/jobs status`: 自分のポイント、選択中の職業、レベル、XPをチャットに表示する。
- `/jobs list`: 利用可能な職業を表示する。
- `/jobs join <job>`: 職業を選択する。
- `/jobs leave <job>`: 職業を解除する。進捗は保持する。
- `/jobs info <job>`: 職業の基本情報を表示する。
- `/jobs top <job>`: 職業別ランキングの上位10名をチャットに表示する。
- `/jobs admin addxp <player> <job> <amount>`: XPを付与する（権限レベル2）。
- `/jobs admin addpoints <player> <amount>`: 共通ポイントを増減する（権限レベル2）。
- `/jobs admin reset <player>`: 職業データをリセットする（権限レベル2）。

権限レベル2のプレイヤーには同じGUI内に管理画面を表示する。オンラインプレイヤーを切り替え、XP付与、ポイント増減、全職業データのリセットを操作できる。リセットは二段階確認とし、すべての変更をサーバーログに記録する。管理サブコマンドはコンソールと復旧用途のため残す。

## ランキング

- `/jobs` GUIのランキング表示から職業ごとの上位10名を確認できる。
- オフラインプレイヤーも対象にし、最終確認したプレイヤー名をUUIDと一緒に保存する。
- レベル、現在レベル内XP、累計XPの降順で比較し、同値の場合はプレイヤー名とUUIDで順序を固定する。
- XPを一度も得ていない初期状態のデータはランキング対象外とする。

## ジョブポイントショップ

- `/jobs` GUIのショップ画面で、ジョブポイントを消費して管理者定義の商品を購入できる。
- 商品は在庫無制限のテンプレート方式とし、登録時に管理者がメインハンドに持っているスタックの個数、エンチャント、耐久値、銃カスタムを含むデータコンポーネントを複製する。登録しても管理者の現物は消費しない。
- 権限レベル2の管理画面から、価格、1人あたり購入上限（0は無制限）、販売状態を設定し、商品の更新・削除を行える。
- クライアントは商品登録時にアイテムデータを送信しない。サーバーが操作時の管理者のメインハンドを直接読み、権限、価格、購入上限、商品数を検証する。
- 購入時はサーバー上の価格、販売状態、購入履歴、保有ポイントと通常インベントリ36枠の容量を再検証する。全スタックを収容できない場合はポイントを消費しない。
- PvP参加中は一時インベントリへの購入による消失と一時装備の商品化を防ぐため、購入・商品管理ともに不可とする。商品管理操作と購入結果はサーバーログへ記録する。

## 次フェーズ

1. Create技師・TaCZ/FMICS系職業の安全な加点条件を設計する。
2. ジョブポイントの追加用途またはスキルツリーを設計する。
